# Which representation should I try?

Video ID: `representation-decision`  
Episode content identity: `7f76b9abb51d49ee81009904a462ffdd29e5e037bd7a53a274fc4adcddd03bd1`  
Status: **complete narration draft; production candidate pending content approval**

## c01 — Question and definition: representation decision flow

What should you ask before choosing CM, CSE-flat, packed evaluation, BDD, or SAT? This episode has one job: Representation choice should follow the required output, live support, reuse, update pattern, exact operation, preparation budget, and evidence status. We build on Configuration and feature-model workloads, Circuit workloads: structure, truth, and exact controls, Policy and rule systems with related revisions, Why one blended fastest-method chart is dishonest; their definitions stay fixed while this lesson adds a new layer. Choose by the answer required, the work reused, the structure updated, and the costs included.

Watch for the label required output; it stays attached to the artifact this episode means. When we say reuse pattern, the highlight identifies its layer before we interpret it. The term update pattern receives its own visual state so it cannot drift into a neighboring concept. We will keep preparation budget visible whenever its definition controls the inference.

A persistent evidence status label marks exactly where that object enters the example.

## c02 — Worked example: Versioned feature model

The next sequence uses Versioned feature model as a conceptual teaching example, not as measured benchmark evidence. Our stable example is Versioned feature model. A conceptual product family with Core, Cloud, Local, Analytics, and Export features plus requires/excludes constraints and three adjacent revisions. Teach constraints, revisions, invalidation, and repeated queries without borrowing an unsupported product claim.

We now construct the Versioned feature model and name every part before interpreting it. Now isolate representation decision flow inside Versioned feature model. The example definition and output meaning stay fixed while only the state for representation decision flow changes. That matched before-and-after view assigns the visible consequence to representation decision flow, not to a substituted workload.

An animated decision tree asks one concrete question per branch and shows why each answer matters. Core, Cloud, Local, Analytics, and Export remain the same named features across all three conceptual revisions. Holding that element fixed lets this episode isolate representation decision flow. Repeated queries can reuse an exact retained artifact only when the revision and invalidation contract permit it.

Any reuse claim must charge the work needed to detect, validate, and persist the reusable region. The final comparison names output contract, revision distance, and reuse count before discussing efficiency. The starting state preserves the stable definition: A conceptual product family with Core, Cloud, Local, Analytics, and Export features plus requires/excludes constraints and three adjacent revisions. After the change, the composition makes representation decision flow inspectable: An animated decision tree asks one concrete question per branch and shows why each answer matters.

The invariant is the example's meaning; the variable is representation decision flow. If the same output is reached through a different path, the diagram must still identify whether representation decision flow changed.

## c03 — Mechanism: task-first selection factors; evidence-status fallback

Walk a complete-vector circuit request through the decision questions and land on eligible exact evaluators. Walk a versioned configuration restriction request through the same tree and land on a different shortlist. Refuse an unsupported branch and return to measurement rather than forcing a recommendation. Now isolate task-first selection factors inside Versioned feature model.

The example definition and output meaning stay fixed while only the state for task-first selection factors changes. That matched before-and-after view assigns the visible consequence to task-first selection factors, not to a substituted workload. Two persistent workload tokens traverse different branches and retain their reasoning trail. Now isolate evidence-status fallback inside Versioned feature model.

The example definition and output meaning stay fixed while only the state for evidence-status fallback changes. That matched before-and-after view assigns the visible consequence to evidence-status fallback, not to a substituted workload. Unsupported evidence ends at a MEASURE/ABSTAIN node rather than a guessed winner. Representation choice should begin with required output, live support, reuse, update pattern, exact operations, preparation cost, and evidence scope rather than a method label.

Read that statement only within this scope: cross-contract decision guidance. Its declared measurement boundary is decision policy. The uncertainty field says must remain evidence scoped. An explicit dense CM and CM-IR are different artifacts with different construction, storage, and evaluation boundaries.

Read that statement only within this scope: current implementation. Its declared measurement boundary is representation and evaluation. The uncertainty field says none. The corrected focused k=13–15 study did not justify a universal width-only selector change.

Read that statement only within this scope: corrected selector-gap study. Its declared measurement boundary is selection policy. The uncertainty field says reused validation is not untouched held-out evidence. Current evidence does not support a universal claim that CM beats CSE-flat.

Read that statement only within this scope: cross-study synthesis. Its declared measurement boundary is multiple boundaries kept separate. The uncertainty field says scopes are intentionally not pooled. The nearest confusing lesson is What the CRSE research program asks; it owns current CRSE program map, while this episode owns representation decision flow.

Requires and excludes edges define valid configurations; they are constraints, not measured product behavior. Holding that element fixed lets this episode isolate task-first selection factors. A revision changes a bounded part of the constraint graph while the unchanged region keeps its identity. Holding that element fixed lets this episode isolate evidence-status fallback.

A changed-region highlight shows what must be recomputed instead of treating the entire model as new. The decision boundary separates valid from invalid assignments after every revision under the same feature ordering. The example is deliberately conceptual, so it teaches representation choice without implying a product benchmark. A fallback route remains visible when a revision invalidates more of the model than the reuse path can safely preserve.

After the change, the composition makes task-first selection factors inspectable: Two persistent workload tokens traverse different branches and retain their reasoning trail. The invariant is the example's meaning; the variable is task-first selection factors. If the same output is reached through a different path, the diagram must still identify whether task-first selection factors changed.

## c04 — Boundary, retrieval, and transfer

The decision flow narrows eligible approaches; it does not replace measurement on a new workload. Route two new workloads through the tree and explain the first branch where their choices diverge. The right representation is conditional on the task you actually need to complete. A common mistake is this: One property such as width or nominal variable count can universally select the best representation.

Repair it by returning to the owned distinction: representation decision flow.

*[Three-second retrieval pause.]*

Use the episode's central distinction to answer: The right representation is conditional on the task you actually need to complete. A good recommendation explains why a method fits this task and why the others answer a different question.
