# Why width alone did not select the engine

Video ID: `selector-width-limit`  
Episode content identity: `34424f981d6a93148931821e136e44578ecd4bae804d555b429a7f9b44f4436f`  
Status: **complete narration draft; production candidate pending content approval**

## c01 — Question and definition: width-only selector

If machine words sound natural above a certain width, why not switch at that number? This episode has one job: A simple width-only flat-versus-words rule failed its declared regret gate, showing that support width is one feature rather than a universal selector. We build on Packed truth vectors: big integers, machine words, and masks, Preparation, kernel, wrapper, and end-to-end time; their definitions stay fixed while this lesson adds a new layer. A selector chooses an engine from pre-execution features and must control regret on the declared evaluation set.

Watch for the label selector; it stays attached to the artifact this episode means. When we say regret, the highlight identifies its layer before we interpret it. The term validation reuse receives its own visual state so it cannot drift into a neighboring concept. We will keep promotion gate visible whenever its definition controls the inference.

## c02 — Worked example: Four-variable truth layout

The next sequence uses Four-variable truth layout as a conceptual teaching example, not as measured benchmark evidence. Our stable example is Four-variable truth layout. F(A,B,C,D)=(A AND B) XOR (C OR D), row variables A,B, column variables C,D, MSB-first ordering. Carry assignments, live support, explicit CM, packed truth vectors, and exact output across episodes.

We now construct the Four-variable truth layout and name every part before interpreting it. Now isolate width-only selector inside Four-variable truth layout. The example definition and output meaning stay fixed while only the state for width-only selector changes. That matched before-and-after view assigns the visible consequence to width-only selector, not to a substituted workload.

A width axis routes cases toward bigint or word lanes. The example fixes F of A, B, C, and D as A AND B, exclusive-or C OR D. Holding that element fixed lets this episode isolate width-only selector. Folding the truth-table output column into a four-by-four grid changes the layout, not the function.

A packed truth vector stores the same exact outputs in a different physical arrangement from the displayed matrix. An exact equality check across the views is the gate before any timing ratio is allowed to matter. The starting state preserves the stable definition: F(A,B,C,D)=(A AND B) XOR (C OR D), row variables A,B, column variables C,D, MSB-first ordering. After the change, the composition makes width-only selector inspectable: A width axis routes cases toward bigint or word lanes.

The invariant is the example's meaning; the variable is width-only selector. If the same output is reached through a different path, the diagram must still identify whether width-only selector changed. The accompanying view makes this step concrete: A width axis routes cases toward bigint or word lanes.

## c03 — Mechanism: regret gate; counterexample and no-change decision

Draw the proposed width threshold and let most cases appear to follow it. Now introduce the catastrophic-regret row, recompute the predeclared gate, and retain the existing selector. Now isolate regret gate inside Four-variable truth layout. The example definition and output meaning stay fixed while only the state for regret gate changes.

That matched before-and-after view assigns the visible consequence to regret gate, not to a substituted workload. Per-case regret bars reveal one severe counterexample that aggregate medians would hide. Now isolate counterexample and no-change decision inside Four-variable truth layout. The example definition and output meaning stay fixed while only the state for counterexample and no-change decision changes.

That matched before-and-after view assigns the visible consequence to counterexample and no-change decision, not to a substituted workload. The promotion gate turns red and the deployed-rule box remains unchanged. The corrected focused k=13–15 study did not justify a universal width-only selector change. Read that statement only within this scope: corrected selector-gap study.

Its declared measurement boundary is selection policy. The uncertainty field says reused validation is not untouched held-out evidence. Representation choice should begin with required output, live support, reuse, update pattern, exact operations, preparation cost, and evidence scope rather than a method label. Read that statement only within this scope: cross-contract decision guidance.

Its declared measurement boundary is decision policy. The uncertainty field says must remain evidence scoped. The nearest confusing lesson is Which representation should I try?; it owns representation decision flow, while this episode owns width-only selector. A and B select the row, C and D select the column, and MSB-first order fixes both index conventions.

Holding that element fixed lets this episode isolate regret gate. Every four-bit assignment names exactly one of the sixteen cells, and every cell returns one exact output bit. Holding that element fixed lets this episode isolate counterexample and no-change decision. Reading a cell forward evaluates an assignment; reading it backward recovers the row and column bits that selected it.

Live support can remove an input from active computation even when the ambient layout still reserves its axis. The explicit matrix is dense by contract, so compactness and speed require separate evidence rather than definition alone. Holding variable order fixed makes matrix, packed-vector, and program views comparable without an indexing ambiguity. After the change, the composition makes regret gate inspectable: Per-case regret bars reveal one severe counterexample that aggregate medians would hide.

The invariant is the example's meaning; the variable is regret gate. If the same output is reached through a different path, the diagram must still identify whether regret gate changed. After the change, the composition makes counterexample and no-change decision inspectable: The promotion gate turns red and the deployed-rule box remains unchanged. The invariant is the example's meaning; the variable is counterexample and no-change decision.

If the same output is reached through a different path, the diagram must still identify whether counterexample and no-change decision changed. The accompanying view makes this step concrete: The promotion gate turns red and the deployed-rule box remains unchanged. The accompanying view makes this step concrete: Per-case regret bars reveal one severe counterexample that aggregate medians would hide.

## c04 — Boundary, retrieval, and transfer

The focused study reused validation and failed its gate; it did not justify a universal width threshold. Choose whether a selector with a good median but one catastrophic row passes the declared gate. Width informs the decision; it does not make the decision alone. A common mistake is this: A support-width cutoff can universally choose between bigint and word-packed evaluation.

Repair it by returning to the owned distinction: width-only selector.

*[Three-second retrieval pause.]*

Use the episode's central distinction to answer: Width informs the decision; it does not make the decision alone. One cheap feature is useful only when its worst mistakes remain acceptable.
