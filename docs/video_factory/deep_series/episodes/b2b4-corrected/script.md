# Corrected B2/B4 V3 kernel result

Video ID: `b2b4-corrected`  
Episode content identity: `228a673f19debe0b59566c967e6f7b47520c9b11ec57d6dd2e23db6f821512ea`  
Status: **complete narration draft; production candidate pending content approval**

## c01 — Question and definition: B2/B4 V3 formula-balanced result

What survives after the comparator, balancing, exactness, and boundary problems are corrected? This episode has one job: The corrected B2/B4 V3 study supports a workload-scoped bare-kernel CM/CSE-flat reduction, a narrower k=16 gap, and a slower public wrapper—not universal dominance. We build on Why scopes and boundaries matter, Plain CSE versus sharing-aware CSE-flat; their definitions stay fixed while this lesson adds a new layer. On this B2/B4 V3 workload, bare CM/CSE-flat was about 0.8906 overall and 0.9612 at live support sixteen, while the public wrapper was slower.

Watch for the label formula-balanced estimate; it stays attached to the artifact this episode means. When we say formula-cluster interval, the highlight identifies its layer before we interpret it. The term support stratum receives its own visual state so it cannot drift into a neighboring concept.

## c02 — Worked example: Shared Boolean expression

The next sequence uses Shared Boolean expression as a conceptual teaching example, not as measured benchmark evidence. Our stable example is Shared Boolean expression. S=A AND B; F=(S AND C AND D) OR (S AND E), with binary source syntax and stable variable order A,B,C,D,E. Carry AST, CSE, CSE-flat, CM-IR, lowering, and operation accounting across episodes.

We now construct the Shared Boolean expression and name every part before interpreting it. Now isolate B2/B4 V3 formula-balanced result inside Shared Boolean expression. The example definition and output meaning stay fixed while only the state for B2/B4 V3 formula-balanced result changes. That matched before-and-after view assigns the visible consequence to B2/B4 V3 formula-balanced result, not to a substituted workload.

A formula-balanced interval plot constructs overall and k=16 points progressively. Begin with A and B, compute the shared subexpression S once, and keep that identity color on every outgoing use. Holding that element fixed lets this episode isolate B2/B4 V3 formula-balanced result. A raw syntax view may draw S twice; a shared graph draws one node with two outgoing edges.

Changing from bigint bits to word lanes changes storage and execution layout, not the meaning of S or F. The output check closes both paths against the same exact truth behavior before performance is interpreted. The starting state preserves the stable definition: S=A AND B; F=(S AND C AND D) OR (S AND E), with binary source syntax and stable variable order A,B,C,D,E. After the change, the composition makes B2/B4 V3 formula-balanced result inspectable: A formula-balanced interval plot constructs overall and k=16 points progressively.

The invariant is the example's meaning; the variable is B2/B4 V3 formula-balanced result. If the same output is reached through a different path, the diagram must still identify whether B2/B4 V3 formula-balanced result changed.

## c03 — Mechanism: k=16 narrowing; wrapper contrast

Now introduce the strongest CSE-flat comparator and exact-output gate before revealing a timing point. Watch us build the overall and k=16 ratio intervals, then place the wrapper result on a separate boundary row. Now isolate k=16 narrowing inside Shared Boolean expression. The example definition and output meaning stay fixed while only the state for k=16 narrowing changes.

That matched before-and-after view assigns the visible consequence to k=16 narrowing, not to a substituted workload. Now isolate wrapper contrast inside Shared Boolean expression. The example definition and output meaning stay fixed while only the state for wrapper contrast changes. That matched before-and-after view assigns the visible consequence to wrapper contrast, not to a substituted workload.

Kernel and wrapper rows stay aligned but cannot merge; their favorable directions visibly differ. On the exactly counterbalanced B2/B4 V3 workload, formula-balanced bare CM/CSE-flat was 0.8906 with a formula-cluster 95% interval [0.8741, 0.9073]. Read that statement only within this scope: B2/B4 V3 formulas on one Windows run. Its declared measurement boundary is compiled evaluator kernel after compilation.

The uncertainty field says formula-cluster bootstrap; does not model between-run or between-machine variation. The B2/B4 V3 bare CM/CSE-flat advantage narrowed toward k=16: formula-balanced ratio 0.9612 [0.9290, 0.9942]. Read that statement only within this scope: B2/B4 V3 live support k=16. The uncertainty field says formula-cluster bootstrap within one run.

The public CM wrapper was slower than CSE-flat in the corrected comparison. Read that statement only within this scope: corrected B2/B4 comparison. Its declared measurement boundary is public wrapper complete truth-output call. The uncertainty field says local formula-cluster interval plus descriptive pod replication.

Corrected benchmark rows required frozen truth verification and equality across eligible timed arms before performance evidence was accepted. Read that statement only within this scope: corrected benchmark protocols. Its declared measurement boundary is correctness gate outside performance claim. The uncertainty field says hash/equality gate.

The nearest confusing lesson is Three-pod B2/B4 replication; it owns three-pod descriptive replication, while this episode owns B2/B4 V3 formula-balanced result. The left branch combines S with C and D, while the right branch combines the same S with E. Holding that element fixed lets this episode isolate k=16 narrowing. Both branches meet at the final OR, so the shared node changes program structure without changing the Boolean output.

Holding that element fixed lets this episode isolate wrapper contrast. Lowering the graph assigns dependency-ordered slots, but slot numbers are not new Boolean operations. Instruction count, primitive execution count, live buffers, and packed storage therefore remain separate ledgers. The stable variable order A through E prevents a convenient reordering from hiding inside a method comparison.

Any claimed reduction must point to the repeated S path or another named transformation actually present here. After the change, the composition makes k=16 narrowing inspectable: A formula-balanced interval plot constructs overall and k=16 points progressively. The invariant is the example's meaning; the variable is k=16 narrowing. If the same output is reached through a different path, the diagram must still identify whether k=16 narrowing changed.

## c04 — Boundary, retrieval, and transfer

These estimates are conditional on this workload, machine, run, comparator, and boundary. Choose the only allowed headline from kernel, k=16, and wrapper panels. The bare-kernel result survived correction; the universal speed claim did not. A common mistake is this: The corrected kernel ratio establishes that the public CM API is faster end to end.

Repair it by returning to the owned distinction: B2/B4 V3 formula-balanced result.

*[Three-second retrieval pause.]*

Use the episode's central distinction to answer: The bare-kernel result survived correction; the universal speed claim did not. The corrected result is modest, scoped, and stronger precisely because it is narrower.
