# D8 Linux confirmation: exact but unprofitable

Video ID: `recognition-d8`  
Episode content identity: `4a46dd80264963627a4d8c6b4707bf82d1b224aec68236ea8991a68c83a4019f`  
Status: **complete narration draft; production candidate pending content approval**

## c01 — Question and definition: D8 frozen Linux transfer

Can an experiment reproduce perfectly and still fail its reason for deployment? This episode has one job: D8 reproduced exact outputs and rule incidence on Linux but reversed the Windows profitability direction, so unconditional one-pass rewriting was not promoted. We build on Milestones D-D7: task routing, proved rules, caching, and normalization; their definitions stay fixed while this lesson adds a new layer. D8 froze the one-pass rewrite package and checked exactness, rule incidence, and whole-task profitability on Linux.

Watch for the label frozen confirmation; it stays attached to the artifact this episode means. When we say rule incidence, the highlight identifies its layer before we interpret it. The term profitability transfer receives its own visual state so it cannot drift into a neighboring concept. We will keep negative control visible whenever its definition controls the inference.

## c02 — Worked example: Versioned policy rules

The next sequence uses Versioned policy rules as a conceptual teaching example, not as measured benchmark evidence. Our stable example is Versioned policy rules. A conceptual access policy over role, region, resource, and risk, shown across three source revisions. Teach related rules, exact reuse, changed-region invalidation, and charged rewrite overhead.

We now construct the Versioned policy rules and name every part before interpreting it. Freeze the Windows package, carry identical rules and inputs to Linux, and verify exact outputs and application counts first. Reveal the reversed overhead-inclusive timing only after correctness and incidence match. Now isolate exactness versus profitability separation inside Versioned policy rules.

The example definition and output meaning stay fixed while only the state for exactness versus profitability separation changes. That matched before-and-after view assigns the visible consequence to exactness versus profitability separation, not to a substituted workload. Exactness and rule-incidence checks turn green in both lanes. The frozen one-pass rewrite result changed from 1.050x on Windows to 0.929x on Linux, so unconditional one-pass rewriting was not promoted.

Read that statement only within this scope: Milestone D8 frozen Linux confirmation. Its declared measurement boundary is overhead-inclusive one-pass rewrite. The uncertainty field says one bounded cross-machine confirmation. The nearest confusing lesson is D9 abstention policy: safe, charged, not promoted; it owns D9 calibrated policy, while this episode owns D8 frozen Linux transfer.

Named rule nodes combine those inputs into one exact allow-or-deny result under a declared ordering. Holding that element fixed lets this episode isolate exactness versus profitability separation. A changed-region trace distinguishes reusable structure from rules that must be recomputed. Detection, rewrite, persistence, and extraction costs remain visible instead of disappearing into a reuse label.

The example teaches a revision workload and does not claim measured behavior for a deployed policy engine. The final decision check compares exact outputs before any preparation or wrapper ratio is interpreted.

## c03 — Boundary, retrieval, and transfer

Exact transfer passed, but profitability did not; the negative result is the final promotion authority. Choose the correct promotion decision when exactness and incidence pass but overhead-inclusive speed loses. D8 is an exact engineering success and a profitability failure at the same time. Now isolate D8 frozen Linux transfer inside Versioned policy rules.

The example definition and output meaning stay fixed while only the state for D8 frozen Linux transfer changes. That matched before-and-after view assigns the visible consequence to D8 frozen Linux transfer, not to a substituted workload. Windows and Linux lanes share frozen package, input, rule, and digest identities. Role, region, resource, and risk remain the stable inputs to the conceptual access decision.

Holding that element fixed lets this episode isolate D8 frozen Linux transfer. Three adjacent revisions change bounded rule regions while unchanged nodes retain their identities. Reuse is valid only when source identity, revision identity, and the output contract all still match. A safe fallback evaluates the full exact policy whenever the incremental contract cannot be proved.

Repeated queries and adjacent revisions are separate reuse axes, so their break-even points must be measured separately. The starting state preserves the stable definition: A conceptual access policy over role, region, resource, and risk, shown across three source revisions. A common mistake is this: Reproducing exact outputs and rule counts is sufficient to promote a performance optimization. Repair it by returning to the owned distinction: D8 frozen Linux transfer.

*[Three-second retrieval pause.]*

Use the episode's central distinction to answer: D8 is an exact engineering success and a profitability failure at the same time. Reproduction includes the failed criterion, not only the successful checks.
