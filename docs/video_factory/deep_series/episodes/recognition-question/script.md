# What the CRSE research program asks

Video ID: `recognition-question`  
Episode content identity: `7bfbbc68c3da208475b769c13a7a542cf43763e0fdf8e6bbc2bbacffee5164c5`  
Status: **complete narration draft; production candidate pending content approval**

## c01 — The falsifiable CRSE question

Can a learned system suggest useful Boolean structure without becoming the authority on correctness? This episode has one job: CRSE is a multi-track experimental program whose initial learned comparisons and later heuristic proposals remain subordinate to exact verification, charged fallback, frozen evaluation, and explicit promotion decisions. We build on Conceptual animation versus measured result, CM-IR nodes, sharing, and roots, Truth digests, alternating schedules, clustering, and intervals; their definitions stay fixed while this lesson adds a new layer. The project's CRSE program tests learned and deterministic proposals behind exact verification, fallback, and explicit promotion gates.

Watch for the label CRSE project label; it stays attached to the artifact this episode means. When we say proposal path, the highlight identifies its layer before we interpret it. The term exact verifier receives its own visual state so it cannot drift into a neighboring concept. We will keep fallback visible whenever its definition controls the inference.

A persistent promotion decision label marks exactly where that object enters the example.

## c02 — Stable proposal, verifier, fallback, and promotion graph

The next sequence uses Conceptual recognition/decomposition graph as a conceptual teaching example, not as measured benchmark evidence. Our stable example is Conceptual recognition/decomposition graph. A tiny labeled expression graph with a proposed partition, an exact witness path, a rejection/fallback path, and a promotion gate; it is never presented as a measured trace. Keep proposal, exact verification, fallback, and promotion visually stable across CRSE episodes.

We now construct the Conceptual recognition/decomposition graph and name every part before interpreting it. Now isolate current CRSE program map inside Conceptual recognition/decomposition graph. The example definition and output meaning stay fixed while only the state for current CRSE program map changes. That matched before-and-after view assigns the visible consequence to current CRSE program map, not to a substituted workload.

An 18-track program map groups routing, recognition, rewrites, exact dispatch, guidance, and negative controls. Now isolate proposal-verifier-fallback-promotion pipeline inside Conceptual recognition/decomposition graph. The example definition and output meaning stay fixed while only the state for proposal-verifier-fallback-promotion pipeline changes. That matched before-and-after view assigns the visible consequence to proposal-verifier-fallback-promotion pipeline, not to a substituted workload.

Proposal and exact-verification lanes use distinct colors and never merge before acceptance. A labeled input graph enters a proposal stage whose job is to suggest a partition or route, not to certify it. Holding that element fixed lets this episode isolate current CRSE program map. An accepted path produces an exact witness; a rejected path moves to the safe fallback without changing correctness.

Holding that element fixed lets this episode isolate proposal-verifier-fallback-promotion pipeline. Proposal, verifier, witness, rejection, fallback, and promotion keep stable shapes across every CRSE episode. Changing the proposal mechanism never removes the exact verifier or the fallback path from the contract. Task routing may select a different exact path while the admitted result and verification gate remain fixed.

## c03 — A/B foundations and the initial matrix/graph/fused/retrieval baseline

Locate the A/B foundation and initial C matrix, graph, fused, and retrieval arms; keep generated-data signal, failed retrieval, and poor EPFL transfer as separate outcomes. Map the current research branches without expanding CRSE into an unsupported phrase. Send one learned proposal through exact verification, rejection/fallback, measured cost, and a separate promotion gate. Contrast engineering success, exact scientific validity, generalization, and production promotion as four different statuses.

Now isolate initial matrix/graph/fused/retrieval learning baseline inside Conceptual recognition/decomposition graph. The example definition and output meaning stay fixed while only the state for initial matrix/graph/fused/retrieval learning baseline changes. That matched before-and-after view assigns the visible consequence to initial matrix/graph/fused/retrieval learning baseline, not to a substituted workload. Matrix MLP, CNN, source-DAG GNN, fused, and retrieval tokens reach separate generated, retrieval, and EPFL outcome panels.

The CRSE recognition program is experimental; no learned model is promoted. Read that statement only within this scope: current recognition program. Its declared measurement boundary is scientific promotion. The uncertainty field says per-milestone frozen splits and criteria.

The initial Milestone C compared matched matrix MLP, matrix CNN, source-DAG GNN, fused, and contrastive-retrieval models: graph/fused arms learned the generated representation signal, retrieval missed its gate, real EPFL transfer was poor, exact rejection preserved outputs, and no model was promoted. Read that statement only within this scope: initial CRSE Milestone C generated and EPFL transfer slices. Its declared measurement boundary is learned proposal plus exact acceptance. The uncertainty field says bounded generated functions and all-negative EPFL slice.

The current CRSE program spans learned proposals, exact subclass recognition, task routing, proved rules, representation dispatch, BDD/SAT guidance, exact verification, negative controls, and explicit promotion decisions. Read that statement only within this scope: current recognition register. Its declared measurement boundary is research-program structure. The uncertainty field says track completion varies.

Teaching diagrams, proposed mechanisms, and hypotheses must be visibly labeled and may not be presented as measured traces. Read that statement only within this scope: video evidence contract. Its declared measurement boundary is editorial/visual status. The uncertainty field says not applicable.

The nearest confusing lesson is Which representation should I try?; it owns representation decision flow, while this episode owns current CRSE program map. The candidate partition remains visibly provisional until the exact verifier checks its witness conditions. Holding that element fixed lets this episode isolate initial matrix/graph/fused/retrieval learning baseline. A conceptual badge stays on the graph even when a later panel introduces separately sourced measurements.

## c04 — Eighteen tracks with exactness, transfer, cost, and promotion gates

CRSE is a project label in the authoritative sources; no production model or invented acronym expansion is allowed. Place proposal, exact verification, charged fallback, measured result, and promotion in the correct order. CRSE studies when guidance helps while exact computation remains the safety authority. Now isolate 18-track scope inside Conceptual recognition/decomposition graph.

The example definition and output meaning stay fixed while only the state for 18-track scope changes. That matched before-and-after view assigns the visible consequence to 18-track scope, not to a substituted workload. The promotion gate is separate from exact verification because a correct result need not satisfy a performance promotion rule. Holding that element fixed lets this episode isolate 18-track scope.

Negative and not-promoted outcomes remain first-class states rather than disappearing from the research timeline. A common mistake is this: A milestone that passes exact verification automatically proves learned generalization or production readiness. Repair it by returning to the owned distinction: current CRSE program map.

*[Three-second retrieval pause.]*

Use the episode's central distinction to answer: CRSE studies when guidance helps while exact computation remains the safety authority. A proposal may help find an answer, but the exact checker decides whether the answer is admitted.
