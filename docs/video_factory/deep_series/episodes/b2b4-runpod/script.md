# Three-pod B2/B4 replication

Video ID: `b2b4-runpod`  
Episode content identity: `b79aab4a4d27cec3c2fed13579185a12f0d9f0e4de383b25aab8927eba6fa0b6`  
Status: **complete narration draft; production candidate pending content approval**

## c01 — Question and definition: three-pod descriptive replication

What does three-machine agreement add, and what does it still not prove? This episode has one job: Three guarded Linux CPU runs reproduced the B2/B4 direction descriptively, but they do not become one pooled inferential interval or erase the local study's scope. We build on Corrected B2/B4 V3 kernel result; their definitions stay fixed while this lesson adds a new layer. Replication checks whether a scoped direction appears again under separately recorded machines and runs.

Watch for the label replication; it stays attached to the artifact this episode means. When we say descriptive machine result, the highlight identifies its layer before we interpret it. The term within-run interval receives its own visual state so it cannot drift into a neighboring concept. We will keep between-machine variation visible whenever its definition controls the inference.

## c02 — Worked example: Shared Boolean expression

The next sequence uses Shared Boolean expression as a conceptual teaching example, not as measured benchmark evidence. Our stable example is Shared Boolean expression. S=A AND B; F=(S AND C AND D) OR (S AND E), with binary source syntax and stable variable order A,B,C,D,E. Carry AST, CSE, CSE-flat, CM-IR, lowering, and operation accounting across episodes.

We now construct the Shared Boolean expression and name every part before interpreting it. Place the accepted local estimate and three pod estimates in separate machine columns with identical ratio labels. Attempt and refuse an unjustified pooled interval, then state what direction replication does support. Now isolate three-pod descriptive replication inside Shared Boolean expression.

The example definition and output meaning stay fixed while only the state for three-pod descriptive replication changes. That matched before-and-after view assigns the visible consequence to three-pod descriptive replication, not to a substituted workload. Four machine columns build independently from verified raw results. Now isolate local inference versus machine replication inside Shared Boolean expression.

The example definition and output meaning stay fixed while only the state for local inference versus machine replication changes. That matched before-and-after view assigns the visible consequence to local inference versus machine replication, not to a substituted workload. Across three guarded CPU pods, row-weighted bare CM/CSE-flat was 0.9026–0.9126 overall and 0.9746–0.9766 at k=16. Read that statement only within this scope: B2/B4 corrected three-pod CPU replication.

Its declared measurement boundary is compiled evaluator kernel after compilation. The uncertainty field says descriptive across three machines; post-hoc per-pod formula intervals are separate. Every ratio must state numerator, denominator, workload, measurement boundary, and uncertainty; below one favors CM only for CM/comparator. Read that statement only within this scope: video evidence contract.

Its declared measurement boundary is all numeric scenes. The uncertainty field says declared per ratio. The nearest confusing lesson is Truth digests, alternating schedules, clustering, and intervals; it owns truth-digest exactness, while this episode owns three-pod descriptive replication. Begin with A and B, compute the shared subexpression S once, and keep that identity color on every outgoing use.

Holding that element fixed lets this episode isolate three-pod descriptive replication. The left branch combines S with C and D, while the right branch combines the same S with E. Holding that element fixed lets this episode isolate local inference versus machine replication. Both branches meet at the final OR, so the shared node changes program structure without changing the Boolean output.

A raw syntax view may draw S twice; a shared graph draws one node with two outgoing edges. Lowering the graph assigns dependency-ordered slots, but slot numbers are not new Boolean operations. Instruction count, primitive execution count, live buffers, and packed storage therefore remain separate ledgers. Changing from bigint bits to word lanes changes storage and execution layout, not the meaning of S or F.

## c03 — Boundary, retrieval, and transfer

The three pod values are descriptive; they are not a predeclared pooled confidence interval. Identify which uncertainty belongs within one run and which would require a between-machine design. Replication adds another scope; it does not erase scope. A common mistake is this: Three similar pod ratios can automatically be averaged into the local study's confidence interval.

Repair it by returning to the owned distinction: three-pod descriptive replication.

*[Three-second retrieval pause.]*

Use the episode's central distinction to answer: Replication adds another scope; it does not erase scope. Repeated direction strengthens portability evidence without broadening the scientific claim for free.
