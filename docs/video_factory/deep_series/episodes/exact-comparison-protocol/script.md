# Truth digests, alternating schedules, clustering, and intervals

Video ID: `exact-comparison-protocol`  
Episode content identity: `38cead434c6a66b21576795717ff1c4cfe16e30a02d7f431042f8f4f5a1fa5b5`  
Status: **complete narration draft; production candidate pending content approval**

## c01 — Four validity threats, four safeguards

What can make a precise timing number wrong even when the timer itself is accurate? This episode has one job: Exactness gates, order alternation, cluster-aware inference, and uncertainty intervals protect against different validity threats and must be shown as separate safeguards. We build on Corrected B2/B4 V3 kernel result, How to read a CM/comparator ratio; their definitions stay fixed while this lesson adds a new layer. Correctness, execution order, dependence, and sampling uncertainty are different problems with different guards.

Watch for the label truth digest; it stays attached to the artifact this episode means. When we say schedule alternation, the highlight identifies its layer before we interpret it. The term cluster receives its own visual state so it cannot drift into a neighboring concept. We will keep bootstrap interval visible whenever its definition controls the inference.

## c02 — Exact-output admission

The next sequence uses Shared Boolean expression as a conceptual teaching example, not as measured benchmark evidence. Our stable example is Shared Boolean expression. S=A AND B; F=(S AND C AND D) OR (S AND E), with binary source syntax and stable variable order A,B,C,D,E. Carry AST, CSE, CSE-flat, CM-IR, lowering, and operation accounting across episodes.

We now construct the Shared Boolean expression and name every part before interpreting it. Now isolate truth-digest exactness inside Shared Boolean expression. The example definition and output meaning stay fixed while only the state for truth-digest exactness changes. That matched before-and-after view assigns the visible consequence to truth-digest exactness, not to a substituted workload.

A timing ribbon alternates arm order while exact output digests remain outside the timed bracket. Begin with A and B, compute the shared subexpression S once, and keep that identity color on every outgoing use. Holding that element fixed lets this episode isolate truth-digest exactness. Lowering the graph assigns dependency-ordered slots, but slot numbers are not new Boolean operations.

Any claimed reduction must point to the repeated S path or another named transformation actually present here. The starting state preserves the stable definition: S=A AND B; F=(S AND C AND D) OR (S AND E), with binary source syntax and stable variable order A,B,C,D,E. After the change, the composition makes truth-digest exactness inspectable: A timing ribbon alternates arm order while exact output digests remain outside the timed bracket. The invariant is the example's meaning; the variable is truth-digest exactness.

If the same output is reached through a different path, the diagram must still identify whether truth-digest exactness changed. The accompanying view makes this step concrete: Four red validity threats enter four lanes and meet distinct guards. Read truth-digest exactness from input to consequence through one matched view: A timing ribbon alternates arm order while exact output digests remain outside the timed bracket; every surrounding entity keeps its prior meaning. The diagram treats truth-digest exactness as a located state, artifact, or boundary rather than as an unexplained method label.

Before the consequence appears, predict what truth-digest exactness can change and what the stable example requires it to leave untouched. When evidence enters, its status badge binds the statement about truth-digest exactness to a source and scope, not to the conceptual drawing alone. The nearest neighboring lesson stays out of focus because its owned question is not needed to explain truth-digest exactness in this sequence. The transfer rule carries truth-digest exactness forward only with the same input definition, exactness gate, and declared output contract.

Use the truth digest label as a checkpoint for truth-digest exactness: if the label moves to another layer, the narration has changed the question rather than explained the mechanism. Use the schedule alternation label as a checkpoint for truth-digest exactness: if the label moves to another layer, the narration has changed the question rather than explained the mechanism. Use the cluster label as a checkpoint for truth-digest exactness: if the label moves to another layer, the narration has changed the question rather than explained the mechanism.

## c03 — Order and timing design

Now introduce wrong-output, warm-order, pseudoreplication, and point-estimate threats one at a time. Now isolate alternating/counterbalanced schedules inside Shared Boolean expression. The example definition and output meaning stay fixed while only the state for alternating/counterbalanced schedules changes. That matched before-and-after view assigns the visible consequence to alternating/counterbalanced schedules, not to a substituted workload.

Corrected benchmark rows required frozen truth verification and equality across eligible timed arms before performance evidence was accepted. Read that statement only within this scope: corrected benchmark protocols. Its declared measurement boundary is correctness gate outside performance claim. The uncertainty field says hash/equality gate.

Every ratio must state numerator, denominator, workload, measurement boundary, and uncertainty; below one favors CM only for CM/comparator. Read that statement only within this scope: video evidence contract. Its declared measurement boundary is all numeric scenes. The uncertainty field says declared per ratio.

On the exactly counterbalanced B2/B4 V3 workload, formula-balanced bare CM/CSE-flat was 0.8906 with a formula-cluster 95% interval [0.8741, 0.9073]. Read that statement only within this scope: B2/B4 V3 formulas on one Windows run. Its declared measurement boundary is compiled evaluator kernel after compilation. The uncertainty field says formula-cluster bootstrap; does not model between-run or between-machine variation.

The left branch combines S with C and D, while the right branch combines the same S with E. Holding that element fixed lets this episode isolate alternating/counterbalanced schedules. Instruction count, primitive execution count, live buffers, and packed storage therefore remain separate ledgers. The output check closes both paths against the same exact truth behavior before performance is interpreted.

After the change, the composition makes alternating/counterbalanced schedules inspectable: A timing ribbon alternates arm order while exact output digests remain outside the timed bracket. The invariant is the example's meaning; the variable is alternating/counterbalanced schedules. If the same output is reached through a different path, the diagram must still identify whether alternating/counterbalanced schedules changed. The accompanying view makes this step concrete: Repeated rows gather into formula clusters before resampling and interval construction.

Read alternating/counterbalanced schedules from input to consequence through one matched view: A timing ribbon alternates arm order while exact output digests remain outside the timed bracket; every surrounding entity keeps its prior meaning. The diagram treats alternating/counterbalanced schedules as a located state, artifact, or boundary rather than as an unexplained method label. Before the consequence appears, predict what alternating/counterbalanced schedules can change and what the stable example requires it to leave untouched. When evidence enters, its status badge binds the statement about alternating/counterbalanced schedules to a source and scope, not to the conceptual drawing alone.

The nearest neighboring lesson stays out of focus because its owned question is not needed to explain alternating/counterbalanced schedules in this sequence. The transfer rule carries alternating/counterbalanced schedules forward only with the same input definition, exactness gate, and declared output contract.

## c04 — Formula clusters and uncertainty

Pair each threat with its guard, then run one row through exact verification, alternation, formula clustering, and interval construction. Now isolate cluster-aware inference inside Shared Boolean expression. The example definition and output meaning stay fixed while only the state for cluster-aware inference changes. That matched before-and-after view assigns the visible consequence to cluster-aware inference, not to a substituted workload.

Repeated rows gather into formula clusters before resampling and interval construction. Now isolate cluster-aware interval construction inside Shared Boolean expression. The example definition and output meaning stay fixed while only the state for cluster-aware interval construction changes. That matched before-and-after view assigns the visible consequence to cluster-aware interval construction, not to a substituted workload.

The nearest confusing lesson is Why one blended fastest-method chart is dishonest; it owns anti-podium argument, while this episode owns truth-digest exactness. Both branches meet at the final OR, so the shared node changes program structure without changing the Boolean output. Holding that element fixed lets this episode isolate cluster-aware inference. A raw syntax view may draw S twice; a shared graph draws one node with two outgoing edges.

Holding that element fixed lets this episode isolate cluster-aware interval construction. Changing from bigint bits to word lanes changes storage and execution layout, not the meaning of S or F. The stable variable order A through E prevents a convenient reordering from hiding inside a method comparison. After the change, the composition makes cluster-aware inference inspectable: Repeated rows gather into formula clusters before resampling and interval construction.

The invariant is the example's meaning; the variable is cluster-aware inference. If the same output is reached through a different path, the diagram must still identify whether cluster-aware inference changed. After the change, the composition makes cluster-aware interval construction inspectable: Repeated rows gather into formula clusters before resampling and interval construction. The invariant is the example's meaning; the variable is cluster-aware interval construction.

If the same output is reached through a different path, the diagram must still identify whether cluster-aware interval construction changed. Read cluster-aware inference from input to consequence through one matched view: Repeated rows gather into formula clusters before resampling and interval construction; every surrounding entity keeps its prior meaning. The diagram treats cluster-aware inference as a located state, artifact, or boundary rather than as an unexplained method label. Before the consequence appears, predict what cluster-aware inference can change and what the stable example requires it to leave untouched.

When evidence enters, its status badge binds the statement about cluster-aware inference to a source and scope, not to the conceptual drawing alone. The nearest neighboring lesson stays out of focus because its owned question is not needed to explain cluster-aware inference in this sequence. The transfer rule carries cluster-aware inference forward only with the same input definition, exactness gate, and declared output contract. Read cluster-aware interval construction from input to consequence through one matched view: Repeated rows gather into formula clusters before resampling and interval construction; every surrounding entity keeps its prior meaning.

The diagram treats cluster-aware interval construction as a located state, artifact, or boundary rather than as an unexplained method label. Before the consequence appears, predict what cluster-aware interval construction can change and what the stable example requires it to leave untouched. When evidence enters, its status badge binds the statement about cluster-aware interval construction to a source and scope, not to the conceptual drawing alone. The nearest neighboring lesson stays out of focus because its owned question is not needed to explain cluster-aware interval construction in this sequence.

The transfer rule carries cluster-aware interval construction forward only with the same input definition, exactness gate, and declared output contract.

## c05 — Controlled threats and remaining limits

The retained interval models formula clustering within the declared run; it does not model every machine or future workload. Match four threats to digest checking, schedule alternation, clustering, or interval construction. Each protocol guard earns one part of the claim and no more. The accompanying view makes this step concrete: A final audit card lists which threats are controlled and which uncertainties remain out of scope.

A common mistake is this: Passing exact-output equality automatically removes timing-order, dependence, and external-validity problems. Repair it by returning to the owned distinction: truth-digest exactness.

*[Three-second retrieval pause.]*

Use the episode's central distinction to answer: Each protocol guard earns one part of the claim and no more. A trustworthy comparison is a chain of safeguards, not one stopwatch reading.
