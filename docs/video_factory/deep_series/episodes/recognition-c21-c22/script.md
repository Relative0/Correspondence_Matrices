# C21-C22 task-matched GF(2): seven methods and a frozen source-packed portfolio

Video ID: `recognition-c21-c22`  
Episode content identity: `3fb295f92016e37660be9fa4a863ffb6c0b280abfeb47d04c793fe3c163b0d3f`  
Status: **complete narration draft; production candidate pending content approval**

## c01 — One task contract: the exhaustive-best GF(2) artifact

What changes when seven methods must return the best exact artifact instead of any reconstructible factor? This episode has one job: C21 charges seven methods for the same exhaustive-best GF(2) artifact and finds a narrow source-packed representation advantage with little routing headroom; C22 freezes that arm behind exhaustive fallback and shadow checks without adding fresh evaluation or production promotion. We build on C17-C20 exact policies: dispatch, transfer, fitting, and compilation, Truth digests, alternating schedules, clustering, and intervals, From DAGs to flat instructions: operations, storage, and execution; their definitions stay fixed while this lesson adds a new layer. A task-matched comparison charges representation, proposal, exact completion, checks, cleanup, and wrappers needed to deliver one common output contract.

Watch for the label task-matched artifact; it stays attached to the artifact this episode means. When we say screened completion, the highlight identifies its layer before we interpret it. The term proposal abstention receives its own visual state so it cannot drift into a neighboring concept. We will keep per-case oracle headroom visible whenever its definition controls the inference.

A persistent source-packed ANF label marks exactly where that object enters the example. Watch for the label shadow mode; it stays attached to the artifact this episode means.

## c02 — Seven exact methods and where their work is charged

The next sequence uses Conceptual recognition/decomposition graph as a conceptual teaching example, not as measured benchmark evidence. Our stable example is Conceptual recognition/decomposition graph. A tiny labeled expression graph with a proposed partition, an exact witness path, a rejection/fallback path, and a promotion gate; it is never presented as a measured trace. Keep proposal, exact verification, fallback, and promotion visually stable across CRSE episodes.

We now construct the Conceptual recognition/decomposition graph and name every part before interpreting it. Now isolate C21 seven-method whole-task table inside Conceptual recognition/decomposition graph. The example definition and output meaning stay fixed while only the state for C21 seven-method whole-task table changes. That matched before-and-after view assigns the visible consequence to C21 seven-method whole-task table, not to a substituted workload.

A stacked whole-task ledger separates input decode, representation, proposal, exact completion, checking, cleanup, and wrapper cost for every method. The candidate partition remains visibly provisional until the exact verifier checks its witness conditions. Holding that element fixed lets this episode isolate C21 seven-method whole-task table. A conceptual badge stays on the graph even when a later panel introduces separately sourced measurements.

Every measured promotion panel names its workload, boundary, uncertainty, and source independently of this teaching graph. After the change, the composition makes C21 seven-method whole-task table inspectable: A stacked whole-task ledger separates input decode, representation, proposal, exact completion, checking, cleanup, and wrapper cost for every method. The invariant is the example's meaning; the variable is C21 seven-method whole-task table. If the same output is reached through a different path, the diagram must still identify whether C21 seven-method whole-task table changed.

The accompanying view makes this step concrete: One exhaustive-best artifact silhouette sits above seven method lanes; each lane must deliver the same identity before its timing cell unlocks. Read C21 seven-method whole-task table from input to consequence through one matched view: A stacked whole-task ledger separates input decode, representation, proposal, exact completion, checking, cleanup, and wrapper cost for every method; every surrounding entity keeps its prior meaning. The diagram treats C21 seven-method whole-task table as a located state, artifact, or boundary rather than as an unexplained method label. Before the consequence appears, predict what C21 seven-method whole-task table can change and what the stable example requires it to leave untouched.

When evidence enters, its status badge binds the statement about C21 seven-method whole-task table to a source and scope, not to the conceptual drawing alone.

## c03 — Packed source ANF, screened CM, and the narrow fixed-path result

Bind exhaustive CM, screened CM, compiled screened CM, truth-ANF priority, source-packed ANF, fresh ROBDD, and source-interaction proposals to one charged exhaustive-best-artifact boundary, so a valid but non-best factor cannot substitute for the requested computation. Keep this separation visible: source-packed truth construction from component-proposal success: the representation path runs on every case, while the proposal abstains on 86 of 96 cases and screened completion remains authoritative. Now isolate source-packed representation versus proposal effect inside Conceptual recognition/decomposition graph. The example definition and output meaning stay fixed while only the state for source-packed representation versus proposal effect changes.

That matched before-and-after view assigns the visible consequence to source-packed representation versus proposal effect, not to a substituted workload. The source-packed lane forks into representation and proposal signals: ten proposal tokens advance, eighty-six abstain, and all ninety-six still enter screened completion. C21 compared seven methods under one exhaustive-best GF(2) artifact contract: packed source ANF was narrowly fastest at 3.007x over exhaustive and 1.006x over screened CM, while the per-case oracle left only 1.059x headroom before routing cost; C22 froze that arm behind exact fallback and shadow checks but added no fresh timing or promotion evidence. Read that statement only within this scope: C21 retrospective task-matched table and C22 implementation boundary.

Its declared measurement boundary is best-exact-artifact whole-task comparison. The uncertainty field says 96 retrospective LogikBench cones on one Windows machine; C22 has no fresh timing. The CRSE recognition program is experimental; no learned model is promoted. Read that statement only within this scope: current recognition program.

Its declared measurement boundary is scientific promotion. The uncertainty field says per-milestone frozen splits and criteria. An accepted path produces an exact witness; a rejected path moves to the safe fallback without changing correctness. Holding that element fixed lets this episode isolate source-packed representation versus proposal effect.

Changing the proposal mechanism never removes the exact verifier or the fallback path from the contract. After the change, the composition makes source-packed representation versus proposal effect inspectable: The source-packed lane forks into representation and proposal signals: ten proposal tokens advance, eighty-six abstain, and all ninety-six still enter screened completion. The invariant is the example's meaning; the variable is source-packed representation versus proposal effect. If the same output is reached through a different path, the diagram must still identify whether source-packed representation versus proposal effect changed.

The accompanying view makes this step concrete: The source-packed lane forks into representation and proposal signals: ten proposal tokens advance, eighty-six abstain, and all ninety-six still enter screened completion. The accompanying view makes this step concrete: A stacked whole-task ledger separates input decode, representation, proposal, exact completion, checking, cleanup, and wrapper cost for every method.

## c04 — Proposal abstention, BDD lifecycle failure, and routing headroom

Compare the narrow best-fixed advantage with the unattainable per-case oracle headroom before any routing cost, then retain fresh single-query BDD as a lifecycle-specific negative control. Now isolate C21 exhaustive-best task contract inside Conceptual recognition/decomposition graph. The example definition and output meaning stay fixed while only the state for C21 exhaustive-best task contract changes. That matched before-and-after view assigns the visible consequence to C21 exhaustive-best task contract, not to a substituted workload.

One exhaustive-best artifact silhouette sits above seven method lanes; each lane must deliver the same identity before its timing cell unlocks. The nearest confusing lesson is C23 fresh Yosys transfer: exact confirmation without routing headroom; it owns C23 task-complete fresh-source freeze, while this episode owns C21 exhaustive-best task contract. A labeled input graph enters a proposal stage whose job is to suggest a partition or route, not to certify it. Holding that element fixed lets this episode isolate C21 exhaustive-best task contract.

Proposal, verifier, witness, rejection, fallback, and promotion keep stable shapes across every CRSE episode. Task routing may select a different exact path while the admitted result and verification gate remain fixed. The starting state preserves the stable definition: A tiny labeled expression graph with a proposed partition, an exact witness path, a rejection/fallback path, and a promotion gate; it is never presented as a measured trace. After the change, the composition makes C21 exhaustive-best task contract inspectable: One exhaustive-best artifact silhouette sits above seven method lanes; each lane must deliver the same identity before its timing cell unlocks.

The invariant is the example's meaning; the variable is C21 exhaustive-best task contract. If the same output is reached through a different path, the diagram must still identify whether C21 exhaustive-best task contract changed. The accompanying view makes this step concrete: The C22 portfolio contract connects source-packed screened selection to exhaustive advice-off/refusal fallback and a parallel shadow identity checker; the fresh-evidence cell remains empty and labeled pending. Read C21 exhaustive-best task contract from input to consequence through one matched view: One exhaustive-best artifact silhouette sits above seven method lanes; each lane must deliver the same identity before its timing cell unlocks; every surrounding entity keeps its prior meaning.

The diagram treats C21 exhaustive-best task contract as a located state, artifact, or boundary rather than as an unexplained method label. Before the consequence appears, predict what C21 exhaustive-best task contract can change and what the stable example requires it to leave untouched. When evidence enters, its status badge binds the statement about C21 exhaustive-best task contract to a source and scope, not to the conceptual drawing alone. The nearest neighboring lesson stays out of focus because its owned question is not needed to explain C21 exhaustive-best task contract in this sequence.

The transfer rule carries C21 exhaustive-best task contract forward only with the same input definition, exactness gate, and declared output contract.

## c05 — C22 frozen source-packed portfolio with exact fallback

Freeze C22 to the source-packed screened arm with advice-off exhaustive restoration, refusal fallback, and shadow artifact identity while marking fresh timing and promotion as absent. C21 is retrospective one-machine evidence, and C22 is implementation readiness without fresh timing, so the narrow source-packed lead is not a production claim. Classify each C21 cost as representation, proposal, completion, check, cleanup, or wrapper, then identify what evidence C22 still lacks. C21-C22 justify a guarded source-packed candidate for fresh testing, not a learned router or deployed default.

Now isolate C22 frozen source-packed exact portfolio inside Conceptual recognition/decomposition graph. The example definition and output meaning stay fixed while only the state for C22 frozen source-packed exact portfolio changes. That matched before-and-after view assigns the visible consequence to C22 frozen source-packed exact portfolio, not to a substituted workload. The C22 portfolio contract connects source-packed screened selection to exhaustive advice-off/refusal fallback and a parallel shadow identity checker; the fresh-evidence cell remains empty and labeled pending.

The promotion gate is separate from exact verification because a correct result need not satisfy a performance promotion rule. Holding that element fixed lets this episode isolate C22 frozen source-packed exact portfolio. Negative and not-promoted outcomes remain first-class states rather than disappearing from the research timeline. After the change, the composition makes C22 frozen source-packed exact portfolio inspectable: The C22 portfolio contract connects source-packed screened selection to exhaustive advice-off/refusal fallback and a parallel shadow identity checker; the fresh-evidence cell remains empty and labeled pending.

The invariant is the example's meaning; the variable is C22 frozen source-packed exact portfolio. If the same output is reached through a different path, the diagram must still identify whether C22 frozen source-packed exact portfolio changed. A common mistake is this: Because source-packed ANF was the fastest fixed arm, its component proposal usually pruned the exact search or C22 is already production-ready. Repair it by returning to the owned distinction: C21 exhaustive-best task contract.

*[Three-second retrieval pause.]*

Use the episode's central distinction to answer: C21-C22 justify a guarded source-packed candidate for fresh testing, not a learned router or deployed default. A faster representation path and a successful structural proposal are different mechanisms and must be measured separately.
