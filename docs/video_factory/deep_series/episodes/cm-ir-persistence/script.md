# CM-IR persistence and version identity

Video ID: `cm-ir-persistence`  
Episode content identity: `ef5e8fadddb1949797896bddc94cf74e3f48d3df490eed0542960f57cfe17dbc`  
Status: **complete narration draft; production candidate pending content approval**

## c01 — Question and contract: CM-IR persistent identity

When is a graph saved yesterday still the right graph for today's source? This episode has one job: Persistent reuse is safe only when canonical artifact identity, source/version identity, invalidation, reload, and exact-output checks remain connected. We build on Canonicalization, interning, and normalization; their definitions stay fixed while this lesson adds a new layer. Persistent reuse requires a stable artifact identity plus the source and version identity that made it valid.

Watch for the label persistent cache; it stays attached to the artifact this episode means. When we say artifact identity, the highlight identifies its layer before we interpret it. The term invalidation receives its own visual state so it cannot drift into a neighboring concept. We will keep reload verification visible whenever its definition controls the inference.

## c02 — Worked example: Versioned policy rules

The next sequence uses Versioned policy rules as a conceptual teaching example, not as measured benchmark evidence. Our stable example is Versioned policy rules. A conceptual access policy over role, region, resource, and risk, shown across three source revisions. Teach related rules, exact reuse, changed-region invalidation, and charged rewrite overhead.

We now construct the Versioned policy rules and name every part before interpreting it. Now isolate CM-IR persistent identity inside Versioned policy rules. The example definition and output meaning stay fixed while only the state for CM-IR persistent identity changes. That matched before-and-after view assigns the visible consequence to CM-IR persistent identity, not to a substituted workload.

Canonical DAG becomes bytes, identity, and a versioned cache entry in one continuous chain. Role, region, resource, and risk remain the stable inputs to the conceptual access decision. Holding that element fixed lets this episode isolate CM-IR persistent identity. A changed-region trace distinguishes reusable structure from rules that must be recomputed.

A safe fallback evaluates the full exact policy whenever the incremental contract cannot be proved. The final decision check compares exact outputs before any preparation or wrapper ratio is interpreted. The starting state preserves the stable definition: A conceptual access policy over role, region, resource, and risk, shown across three source revisions. After the change, the composition makes CM-IR persistent identity inspectable: Canonical DAG becomes bytes, identity, and a versioned cache entry in one continuous chain.

The invariant is the example's meaning; the variable is CM-IR persistent identity. If the same output is reached through a different path, the diagram must still identify whether CM-IR persistent identity changed. The accompanying view makes this step concrete: Canonical DAG becomes bytes, identity, and a versioned cache entry in one continuous chain. Read CM-IR persistent identity from input to consequence through one matched view: Canonical DAG becomes bytes, identity, and a versioned cache entry in one continuous chain; every surrounding entity keeps its prior meaning.

The diagram treats CM-IR persistent identity as a located state, artifact, or boundary rather than as an unexplained method label. Before the consequence appears, predict what CM-IR persistent identity can change and what the stable example requires it to leave untouched. When evidence enters, its status badge binds the statement about CM-IR persistent identity to a source and scope, not to the conceptual drawing alone. The nearest neighboring lesson stays out of focus because its owned question is not needed to explain CM-IR persistent identity in this sequence.

The transfer rule carries CM-IR persistent identity forward only with the same input definition, exactness gate, and declared output contract. Use the persistent cache label as a checkpoint for CM-IR persistent identity: if the label moves to another layer, the narration has changed the question rather than explained the mechanism. Use the artifact identity label as a checkpoint for CM-IR persistent identity: if the label moves to another layer, the narration has changed the question rather than explained the mechanism. Use the invalidation label as a checkpoint for CM-IR persistent identity: if the label moves to another layer, the narration has changed the question rather than explained the mechanism.

Use the reload verification label as a checkpoint for CM-IR persistent identity: if the label moves to another layer, the narration has changed the question rather than explained the mechanism.

## c03 — Mechanism I: cache hit/miss/invalidation

Serialize a canonical DAG identity, record source/version inputs, and demonstrate one exact cache hit. Now isolate cache hit/miss/invalidation inside Versioned policy rules. The example definition and output meaning stay fixed while only the state for cache hit/miss/invalidation changes. That matched before-and-after view assigns the visible consequence to cache hit/miss/invalidation, not to a substituted workload.

A source edit travels only through affected dependency edges and converts one hit into a miss. Persistent CM-IR reuse is keyed by canonical identity and must verify version/input identity before accepting a cached artifact. Read that statement only within this scope: current process-local persistent cache and versioned research contracts. Its declared measurement boundary is persistence and invalidation.

The uncertainty field says cache scope is implementation-specific. A video production artifact is reproducible only when source locators, source hashes, claim IDs, script/storyboard identities, render jobs, outputs, and approval identities remain linked. Read that statement only within this scope: CM video factory and RunPod safety contract. Its declared measurement boundary is production provenance.

The uncertainty field says hash identity does not itself establish scientific truth. Named rule nodes combine those inputs into one exact allow-or-deny result under a declared ordering. Holding that element fixed lets this episode isolate cache hit/miss/invalidation. Reuse is valid only when source identity, revision identity, and the output contract all still match.

The example teaches a revision workload and does not claim measured behavior for a deployed policy engine. After the change, the composition makes cache hit/miss/invalidation inspectable: A source edit travels only through affected dependency edges and converts one hit into a miss. The invariant is the example's meaning; the variable is cache hit/miss/invalidation. If the same output is reached through a different path, the diagram must still identify whether cache hit/miss/invalidation changed.

The accompanying view makes this step concrete: Reloaded graph and fresh compilation meet at an exact-output digest gate. Read cache hit/miss/invalidation from input to consequence through one matched view: A source edit travels only through affected dependency edges and converts one hit into a miss; every surrounding entity keeps its prior meaning. The diagram treats cache hit/miss/invalidation as a located state, artifact, or boundary rather than as an unexplained method label. Before the consequence appears, predict what cache hit/miss/invalidation can change and what the stable example requires it to leave untouched.

When evidence enters, its status badge binds the statement about cache hit/miss/invalidation to a source and scope, not to the conceptual drawing alone. The nearest neighboring lesson stays out of focus because its owned question is not needed to explain cache hit/miss/invalidation in this sequence. The transfer rule carries cache hit/miss/invalidation forward only with the same input definition, exactness gate, and declared output contract. Use the persistent cache label as a checkpoint for cache hit/miss/invalidation: if the label moves to another layer, the narration has changed the question rather than explained the mechanism.

Use the artifact identity label as a checkpoint for cache hit/miss/invalidation: if the label moves to another layer, the narration has changed the question rather than explained the mechanism. Use the invalidation label as a checkpoint for cache hit/miss/invalidation: if the label moves to another layer, the narration has changed the question rather than explained the mechanism. Use the reload verification label as a checkpoint for cache hit/miss/invalidation: if the label moves to another layer, the narration has changed the question rather than explained the mechanism.

## c04 — Mechanism II: reload verification; reload verification

Change one policy revision, propagate invalidation to the affected identity, reload the valid artifact, and compare exact outputs. Now isolate reload verification inside Versioned policy rules. The example definition and output meaning stay fixed while only the state for reload verification changes. That matched before-and-after view assigns the visible consequence to reload verification, not to a substituted workload.

Reloaded graph and fresh compilation meet at an exact-output digest gate. The nearest confusing lesson is CM-IR nodes, sharing, and roots; it owns CMNode structure, while this episode owns CM-IR persistent identity. Three adjacent revisions change bounded rule regions while unchanged nodes retain their identities. Holding that element fixed lets this episode isolate reload verification.

Detection, rewrite, persistence, and extraction costs remain visible instead of disappearing into a reuse label. Repeated queries and adjacent revisions are separate reuse axes, so their break-even points must be measured separately. After the change, the composition makes reload verification inspectable: Reloaded graph and fresh compilation meet at an exact-output digest gate. The invariant is the example's meaning; the variable is reload verification.

If the same output is reached through a different path, the diagram must still identify whether reload verification changed. Read reload verification from input to consequence through one matched view: Reloaded graph and fresh compilation meet at an exact-output digest gate; every surrounding entity keeps its prior meaning. The diagram treats reload verification as a located state, artifact, or boundary rather than as an unexplained method label. Before the consequence appears, predict what reload verification can change and what the stable example requires it to leave untouched.

When evidence enters, its status badge binds the statement about reload verification to a source and scope, not to the conceptual drawing alone. The nearest neighboring lesson stays out of focus because its owned question is not needed to explain reload verification in this sequence. The transfer rule carries reload verification forward only with the same input definition, exactness gate, and declared output contract. Use the persistent cache label as a checkpoint for reload verification: if the label moves to another layer, the narration has changed the question rather than explained the mechanism.

Use the artifact identity label as a checkpoint for reload verification: if the label moves to another layer, the narration has changed the question rather than explained the mechanism. Use the invalidation label as a checkpoint for reload verification: if the label moves to another layer, the narration has changed the question rather than explained the mechanism. Use the reload verification label as a checkpoint for reload verification: if the label moves to another layer, the narration has changed the question rather than explained the mechanism.

## c05 — Evidence boundary, retrieval, and transfer

A hash match supports identity checking; it does not by itself establish scientific truth or permit stale reuse. Choose whether three changed-source scenarios are a cache hit, miss, or explicit invalidation. Persistence is an identity and invalidation contract, not merely saving bytes. The accompanying view makes this step concrete: A collision/equal-output warning demonstrates why output equality alone is not cache authority.

A common mistake is this: Equal output on one check is sufficient authority to reuse an artifact from changed source. Repair it by returning to the owned distinction: CM-IR persistent identity.

*[Three-second retrieval pause.]*

Use the episode's central distinction to answer: Persistence is an identity and invalidation contract, not merely saving bytes. A cache is trustworthy only when it knows why an artifact is still valid.
