# Explicit dense CM versus CM-IR

Video ID: `explicit-cm-vs-cm-ir`  
Episode content identity: `f04b1e6e61b8308070beca6f5a463c4946d200c19cf9c8d32e3c90e494d2268c`  
Status: **complete narration draft; production candidate pending content approval**

## c01 — Question and definition: dense CM versus CM-IR artifact distinction

When a benchmark says CM, is it timing a matrix, a program graph, or a wrapper that creates both? This episode has one job: The explicit CM is a dense truth-layout artifact; CM-IR is a canonicalized shared program graph that may later produce dense, flat, or packed outputs. We build on What a correspondence matrix is, What CM does not claim to be; their definitions stay fixed while this lesson adds a new layer. The explicit CM is the dense truth layout; CM-IR is the reusable canonical computation graph.

Watch for the label explicit dense CM; it stays attached to the artifact this episode means. When we say CM-IR, the highlight identifies its layer before we interpret it. The term materialization receives its own visual state so it cannot drift into a neighboring concept.

## c02 — Worked example: Four-variable truth layout

The next sequence uses Four-variable truth layout as a conceptual teaching example, not as measured benchmark evidence. Our stable example is Four-variable truth layout. F(A,B,C,D)=(A AND B) XOR (C OR D), row variables A,B, column variables C,D, MSB-first ordering. Carry assignments, live support, explicit CM, packed truth vectors, and exact output across episodes.

We now construct the Four-variable truth layout and name every part before interpreting it. Now isolate dense CM versus CM-IR artifact distinction inside Four-variable truth layout. The example definition and output meaning stay fixed while only the state for dense CM versus CM-IR artifact distinction changes. That matched before-and-after view assigns the visible consequence to dense CM versus CM-IR artifact distinction, not to a substituted workload.

The same expression forks into a dense grid and an interned DAG with different identity labels. The example fixes F of A, B, C, and D as A AND B, exclusive-or C OR D. Holding that element fixed lets this episode isolate dense CM versus CM-IR artifact distinction. Every four-bit assignment names exactly one of the sixteen cells, and every cell returns one exact output bit.

Reading a cell forward evaluates an assignment; reading it backward recovers the row and column bits that selected it. A packed truth vector stores the same exact outputs in a different physical arrangement from the displayed matrix. Holding variable order fixed makes matrix, packed-vector, and program views comparable without an indexing ambiguity. The starting state preserves the stable definition: F(A,B,C,D)=(A AND B) XOR (C OR D), row variables A,B, column variables C,D, MSB-first ordering.

After the change, the composition makes dense CM versus CM-IR artifact distinction inspectable: The same expression forks into a dense grid and an interned DAG with different identity labels. The invariant is the example's meaning; the variable is dense CM versus CM-IR artifact distinction. If the same output is reached through a different path, the diagram must still identify whether dense CM versus CM-IR artifact distinction changed. The accompanying view makes this step concrete: The same expression forks into a dense grid and an interned DAG with different identity labels.

Read dense CM versus CM-IR artifact distinction from input to consequence through one matched view: The same expression forks into a dense grid and an interned DAG with different identity labels; every surrounding entity keeps its prior meaning. The diagram treats dense CM versus CM-IR artifact distinction as a located state, artifact, or boundary rather than as an unexplained method label.

## c03 — Mechanism: construction/evaluation/materialization separation

Compile the same expression into a shared CM-IR DAG while the explicit matrix remains empty. Evaluate or materialize the DAG into several outputs and show which costs belong to graph construction versus output construction. Now isolate construction/evaluation/materialization separation inside Four-variable truth layout. The example definition and output meaning stay fixed while only the state for construction/evaluation/materialization separation changes.

That matched before-and-after view assigns the visible consequence to construction/evaluation/materialization separation, not to a substituted workload. The DAG feeds point, flat/packed, and dense materialization exits; the grid remains the dense result. CM-IR is a canonicalized, interned shared DAG intermediate representation. Read that statement only within this scope: current implementation.

Its declared measurement boundary is representation. The uncertainty field says none. An explicit dense CM and CM-IR are different artifacts with different construction, storage, and evaluation boundaries. Its declared measurement boundary is representation and evaluation.

CM, CM-IR, SAT, BDD, minimization, and symbolic algebra answer different questions and must be compared under matched output contracts. Read that statement only within this scope: implemented backend and task contracts. Its declared measurement boundary is output contract. The uncertainty field says scope dependent.

The nearest confusing lesson is CM-IR nodes, sharing, and roots; it owns CMNode structure, while this episode owns dense CM versus CM-IR artifact distinction. A and B select the row, C and D select the column, and MSB-first order fixes both index conventions. Holding that element fixed lets this episode isolate construction/evaluation/materialization separation. Folding the truth-table output column into a four-by-four grid changes the layout, not the function.

Live support can remove an input from active computation even when the ambient layout still reserves its axis. The explicit matrix is dense by contract, so compactness and speed require separate evidence rather than definition alone. An exact equality check across the views is the gate before any timing ratio is allowed to matter. After the change, the composition makes construction/evaluation/materialization separation inspectable: The DAG feeds point, flat/packed, and dense materialization exits; the grid remains the dense result.

The invariant is the example's meaning; the variable is construction/evaluation/materialization separation. If the same output is reached through a different path, the diagram must still identify whether construction/evaluation/materialization separation changed. The accompanying view makes this step concrete: Matched axes compare artifact, storage, reuse, and charged boundary without declaring a winner. The accompanying view makes this step concrete: The DAG feeds point, flat/packed, and dense materialization exits; the grid remains the dense result.

## c04 — Boundary, retrieval, and transfer

A CM-IR kernel result is not automatically a dense-CM or public-wrapper result. Classify four displayed artifacts as explicit CM, CM-IR, lowered program, or packed output. The matrix is an output layout; CM-IR is a program that can produce outputs. A common mistake is this: CM and CM-IR are two names for the same in-memory object.

Repair it by returning to the owned distinction: dense CM versus CM-IR artifact distinction.

*[Three-second retrieval pause.]*

Use the episode's central distinction to answer: The matrix is an output layout; CM-IR is a program that can produce outputs. Name the artifact and the boundary every time the label CM appears.
