# Preparation, kernel, wrapper, and end-to-end time

Video ID: `measurement-boundaries`  
Episode content identity: `cbda952b7881d6cdbf23d53e3eaad18f521f408616fcc248bef77ad3706438ab`  
Status: **complete narration draft; production candidate pending content approval**

## c01 — Question and definition: preparation, kernel, wrapper, extraction, persistence, and end-to-end timing boundaries

How can a kernel be faster while the easy public call is slower? This episode has one job: A timing claim is meaningful only after every included and excluded stage is placed on a common pipeline. We build on From DAGs to flat instructions: operations, storage, and execution; their definitions stay fixed while this lesson adds a new layer. A measurement boundary specifies exactly which stages the timer includes.

Watch for the label preparation; it stays attached to the artifact this episode means. When we say evaluator kernel, the highlight identifies its layer before we interpret it. The term public wrapper receives its own visual state so it cannot drift into a neighboring concept. We will keep end-to-end task visible whenever its definition controls the inference.

## c02 — Worked example: Shared Boolean expression

The next sequence uses Shared Boolean expression as a conceptual teaching example, not as measured benchmark evidence. Our stable example is Shared Boolean expression. S=A AND B; F=(S AND C AND D) OR (S AND E), with binary source syntax and stable variable order A,B,C,D,E. Carry AST, CSE, CSE-flat, CM-IR, lowering, and operation accounting across episodes.

We now construct the Shared Boolean expression and name every part before interpreting it. The accompanying view makes this step concrete: One persistent horizontal pipeline replaces separate cards for every boundary.

## c03 — Mechanism: preparation, kernel, wrapper, extraction, persistence, and end-to-end timing boundaries

Move one request through preparation, artifact construction, repeated kernel, wrapper, extraction, persistence, and task completion. Slide timing brackets over different stage subsets and change the allowed claim wording in lockstep. Now isolate preparation, kernel, wrapper, extraction, persistence, and end-to-end timing boundaries inside Shared Boolean expression. The example definition and output meaning stay fixed while only the state for preparation, kernel, wrapper, extraction, persistence, and end-to-end timing boundaries changes.

That matched before-and-after view assigns the visible consequence to preparation, kernel, wrapper, extraction, persistence, and end-to-end timing boundaries, not to a substituted workload. Colored brackets expand from kernel-only to wrapper and end-to-end scopes. On the exactly counterbalanced B2/B4 V3 workload, formula-balanced bare CM/CSE-flat was 0.8906 with a formula-cluster 95% interval [0.8741, 0.9073]. Read that statement only within this scope: B2/B4 V3 formulas on one Windows run.

Its declared measurement boundary is compiled evaluator kernel after compilation. The uncertainty field says formula-cluster bootstrap; does not model between-run or between-machine variation. The public CM wrapper was slower than CSE-flat in the corrected comparison. Read that statement only within this scope: corrected B2/B4 comparison.

Its declared measurement boundary is public wrapper complete truth-output call. The uncertainty field says local formula-cluster interval plus descriptive pod replication. EPFL CM preparation was 4.11x CSE-flat preparation; 55 of 129 cases never broke even under the retained calculation. Read that statement only within this scope: accepted EPFL workload.

Its declared measurement boundary is preparation and modeled reuse break-even. The uncertainty field says descriptive retained calculation. Current evidence does not support a universal claim that CM beats CSE-flat. Read that statement only within this scope: cross-study synthesis.

Its declared measurement boundary is multiple boundaries kept separate. The uncertainty field says scopes are intentionally not pooled. The nearest confusing lesson is How to read a CM/comparator ratio; it owns ratio direction, while this episode owns preparation, kernel, wrapper, extraction, persistence, and end-to-end timing boundaries. Begin with A and B, compute the shared subexpression S once, and keep that identity color on every outgoing use.

Holding that element fixed lets this episode isolate preparation, kernel, wrapper, extraction, persistence, and end-to-end timing boundaries. The left branch combines S with C and D, while the right branch combines the same S with E. Both branches meet at the final OR, so the shared node changes program structure without changing the Boolean output. A raw syntax view may draw S twice; a shared graph draws one node with two outgoing edges.

Lowering the graph assigns dependency-ordered slots, but slot numbers are not new Boolean operations. Instruction count, primitive execution count, live buffers, and packed storage therefore remain separate ledgers. Changing from bigint bits to word lanes changes storage and execution layout, not the meaning of S or F. The stable variable order A through E prevents a convenient reordering from hiding inside a method comparison.

Any claimed reduction must point to the repeated S path or another named transformation actually present here. The output check closes both paths against the same exact truth behavior before performance is interpreted. The starting state preserves the stable definition: S=A AND B; F=(S AND C AND D) OR (S AND E), with binary source syntax and stable variable order A,B,C,D,E. After the change, the composition makes preparation, kernel, wrapper, extraction, persistence, and end-to-end timing boundaries inspectable: Colored brackets expand from kernel-only to wrapper and end-to-end scopes.

The invariant is the example's meaning; the variable is preparation, kernel, wrapper, extraction, persistence, and end-to-end timing boundaries. If the same output is reached through a different path, the diagram must still identify whether preparation, kernel, wrapper, extraction, persistence, and end-to-end timing boundaries changed. The accompanying view makes this step concrete: The B2/B4 kernel advantage and wrapper loss occupy different brackets simultaneously. The accompanying view makes this step concrete: Colored brackets expand from kernel-only to wrapper and end-to-end scopes.

Read preparation, kernel, wrapper, extraction, persistence, and end-to-end timing boundaries from input to consequence through one matched view: Colored brackets expand from kernel-only to wrapper and end-to-end scopes; every surrounding entity keeps its prior meaning. The diagram treats preparation, kernel, wrapper, extraction, persistence, and end-to-end timing boundaries as a located state, artifact, or boundary rather than as an unexplained method label. Before the consequence appears, predict what preparation, kernel, wrapper, extraction, persistence, and end-to-end timing boundaries can change and what the stable example requires it to leave untouched. When evidence enters, its status badge binds the statement about preparation, kernel, wrapper, extraction, persistence, and end-to-end timing boundaries to a source and scope, not to the conceptual drawing alone.

## c04 — Boundary, retrieval, and transfer

Numbers from different boundaries may be shown together only when they remain visibly separate. Choose which stages belong inside kernel, wrapper, and end-to-end brackets. The boundary is part of the result, not a footnote to it. A common mistake is this: A faster compiled kernel automatically makes preparation, wrapper, and end-to-end execution faster.

Repair it by returning to the owned distinction: preparation, kernel, wrapper, extraction, persistence, and end-to-end timing boundaries.

*[Three-second retrieval pause.]*

Use the episode's central distinction to answer: The boundary is part of the result, not a footnote to it. Before comparing times, compare the work inside the timers.
