# Milestones D-D7: task routing, proved rules, caching, and normalization

Video ID: `recognition-d-tasks`  
Episode content identity: `de5337741a33da882cb0256e850c1852653127ce32a1fd8286287383c994c2b7`  
Status: **complete narration draft; production candidate pending content approval**

## c01 — D: four task contracts and mixed routing

What happens when exact routing and rewriting are judged by the complete task rather than their inner kernel? This episode has one job: D-D7 expand from task-aware exact routing into proved rules, versioned caches, profitability gates, natural incidence, real revisions, and bounded normalization; exact engineering improves more consistently than end-to-end profitability. We build on What the CRSE research program asks, Preparation, kernel, wrapper, and end-to-end time, CM-IR persistence and version identity; their definitions stay fixed while this lesson adds a new layer. D-D7 separate task outputs, proved rules, cache identity, reuse, natural incidence, revisions, and bounded normalization.

Watch for the label task routing; it stays attached to the artifact this episode means. When we say proved metavariable rule, the highlight identifies its layer before we interpret it. The term versioned rule cache receives its own visual state so it cannot drift into a neighboring concept. We will keep profitability gate visible whenever its definition controls the inference.

A persistent bounded normalization label marks exactly where that object enters the example.

## c02 — D2: one proved rule and exact control

The next sequence uses Versioned policy rules as a conceptual teaching example, not as measured benchmark evidence. Our stable example is Versioned policy rules. A conceptual access policy over role, region, resource, and risk, shown across three source revisions. Teach related rules, exact reuse, changed-region invalidation, and charged rewrite overhead.

We now construct the Versioned policy rules and name every part before interpreting it. The accompanying view makes this step concrete: Four task tokens traverse separate cost pipelines rather than one blended benchmark.

## c03 — D3-D4: versioned rule packs and profitability gates

Route complete-vector, point, restriction, and repeated-vector tasks through exact arms with all construction and audit costs present. Grow the rule pack from a proved identity to versioned cache and profitability gate, preserving proof provenance. Now isolate D2-D4 proved/versioned/profitability rule pack inside Versioned policy rules. The example definition and output meaning stay fixed while only the state for D2-D4 proved/versioned/profitability rule pack changes.

That matched before-and-after view assigns the visible consequence to D2-D4 proved/versioned/profitability rule pack, not to a substituted workload. A rule-pack timeline adds proof rows, deterministic priority, cache identity, and gates at D2-D4. Milestone D task routing helped restrictions and repeated work but slowed complete-vector requests; dense-CM construction and per-instance rewrite proof were negative. Read that statement only within this scope: Milestone D generated task-computation study.

Its declared measurement boundary is end-to-end task boundaries. The uncertainty field says bounded generated workload. D2-D7 moved from one proved macro through versioned rule packs, profitability gates, natural incidence, real revisions, and bounded normalization; exactness advanced more consistently than profitability. Read that statement only within this scope: Milestones D2-D7 bounded rule studies.

Its declared measurement boundary is overhead-inclusive rule task. The uncertainty field says no universal rewrite policy. Policy/rule systems with related revisions can reuse proved structure only when matching, proof, invalidation, and overhead are charged at the declared task boundary. Read that statement only within this scope: bounded CRSE rule and revision studies.

Its declared measurement boundary is overhead-inclusive task boundary. The uncertainty field says no general deployment claim. Named rule nodes combine those inputs into one exact allow-or-deny result under a declared ordering. Holding that element fixed lets this episode isolate D2-D4 proved/versioned/profitability rule pack.

Reuse is valid only when source identity, revision identity, and the output contract all still match. The example teaches a revision workload and does not claim measured behavior for a deployed policy engine. After the change, the composition makes D2-D4 proved/versioned/profitability rule pack inspectable: A rule-pack timeline adds proof rows, deterministic priority, cache identity, and gates at D2-D4. The invariant is the example's meaning; the variable is D2-D4 proved/versioned/profitability rule pack.

If the same output is reached through a different path, the diagram must still identify whether D2-D4 proved/versioned/profitability rule pack changed. The accompanying view makes this step concrete: Natural incidence heatmaps and revision diffs ground D5-D6 in retained workloads. The accompanying view makes this step concrete: A rule-pack timeline adds proof rows, deterministic priority, cache identity, and gates at D2-D4. Read D2-D4 proved/versioned/profitability rule pack from input to consequence through one matched view: A rule-pack timeline adds proof rows, deterministic priority, cache identity, and gates at D2-D4; every surrounding entity keeps its prior meaning.

The diagram treats D2-D4 proved/versioned/profitability rule pack as a located state, artifact, or boundary rather than as an unexplained method label. Before the consequence appears, predict what D2-D4 proved/versioned/profitability rule pack can change and what the stable example requires it to leave untouched. When evidence enters, its status badge binds the statement about D2-D4 proved/versioned/profitability rule pack to a source and scope, not to the conceptual drawing alone. The nearest neighboring lesson stays out of focus because its owned question is not needed to explain D2-D4 proved/versioned/profitability rule pack in this sequence.

The transfer rule carries D2-D4 proved/versioned/profitability rule pack forward only with the same input definition, exactness gate, and declared output contract. Use the task routing label as a checkpoint for D2-D4 proved/versioned/profitability rule pack: if the label moves to another layer, the narration has changed the question rather than explained the mechanism. Use the proved metavariable rule label as a checkpoint for D2-D4 proved/versioned/profitability rule pack: if the label moves to another layer, the narration has changed the question rather than explained the mechanism.

## c04 — D5-D6: natural incidence and real revisions

Move into natural circuits and revisions, then add bounded multi-pass normalization and retain its negative second-pass economics. Now isolate D task contracts inside Versioned policy rules. The example definition and output meaning stay fixed while only the state for D task contracts changes. That matched before-and-after view assigns the visible consequence to D task contracts, not to a substituted workload.

Four task tokens traverse separate cost pipelines rather than one blended benchmark. Now isolate D5-D7 natural incidence, revisions, and normalization inside Versioned policy rules. The example definition and output meaning stay fixed while only the state for D5-D7 natural incidence, revisions, and normalization changes. That matched before-and-after view assigns the visible consequence to D5-D7 natural incidence, revisions, and normalization, not to a substituted workload.

Natural incidence heatmaps and revision diffs ground D5-D6 in retained workloads. The nearest confusing lesson is D8 Linux confirmation: exact but unprofitable; it owns D8 frozen Linux transfer, while this episode owns D task contracts. Role, region, resource, and risk remain the stable inputs to the conceptual access decision. Holding that element fixed lets this episode isolate D task contracts.

Three adjacent revisions change bounded rule regions while unchanged nodes retain their identities. Holding that element fixed lets this episode isolate D5-D7 natural incidence, revisions, and normalization. A changed-region trace distinguishes reusable structure from rules that must be recomputed. Detection, rewrite, persistence, and extraction costs remain visible instead of disappearing into a reuse label.

A safe fallback evaluates the full exact policy whenever the incremental contract cannot be proved. Repeated queries and adjacent revisions are separate reuse axes, so their break-even points must be measured separately. The final decision check compares exact outputs before any preparation or wrapper ratio is interpreted. The starting state preserves the stable definition: A conceptual access policy over role, region, resource, and risk, shown across three source revisions.

After the change, the composition makes D task contracts inspectable: Four task tokens traverse separate cost pipelines rather than one blended benchmark. The invariant is the example's meaning; the variable is D task contracts. If the same output is reached through a different path, the diagram must still identify whether D task contracts changed. After the change, the composition makes D5-D7 natural incidence, revisions, and normalization inspectable: Natural incidence heatmaps and revision diffs ground D5-D6 in retained workloads.

The invariant is the example's meaning; the variable is D5-D7 natural incidence, revisions, and normalization. If the same output is reached through a different path, the diagram must still identify whether D5-D7 natural incidence, revisions, and normalization changed. The accompanying view makes this step concrete: A bounded normalization loop shows strict decrease, termination, extra reductions, and charged loss. Read D task contracts from input to consequence through one matched view: Four task tokens traverse separate cost pipelines rather than one blended benchmark; every surrounding entity keeps its prior meaning.

The diagram treats D task contracts as a located state, artifact, or boundary rather than as an unexplained method label. Before the consequence appears, predict what D task contracts can change and what the stable example requires it to leave untouched. When evidence enters, its status badge binds the statement about D task contracts to a source and scope, not to the conceptual drawing alone. The nearest neighboring lesson stays out of focus because its owned question is not needed to explain D task contracts in this sequence.

The transfer rule carries D task contracts forward only with the same input definition, exactness gate, and declared output contract. Read D5-D7 natural incidence, revisions, and normalization from input to consequence through one matched view: Natural incidence heatmaps and revision diffs ground D5-D6 in retained workloads; every surrounding entity keeps its prior meaning. The diagram treats D5-D7 natural incidence, revisions, and normalization as a located state, artifact, or boundary rather than as an unexplained method label. Before the consequence appears, predict what D5-D7 natural incidence, revisions, and normalization can change and what the stable example requires it to leave untouched.

When evidence enters, its status badge binds the statement about D5-D7 natural incidence, revisions, and normalization to a source and scope, not to the conceptual drawing alone. The nearest neighboring lesson stays out of focus because its owned question is not needed to explain D5-D7 natural incidence, revisions, and normalization in this sequence. The transfer rule carries D5-D7 natural incidence, revisions, and normalization forward only with the same input definition, exactness gate, and declared output contract. Use the task routing label as a checkpoint for D task contracts: if the label moves to another layer, the narration has changed the question rather than explained the mechanism.

Use the proved metavariable rule label as a checkpoint for D task contracts: if the label moves to another layer, the narration has changed the question rather than explained the mechanism. Use the versioned rule cache label as a checkpoint for D task contracts: if the label moves to another layer, the narration has changed the question rather than explained the mechanism. Use the profitability gate label as a checkpoint for D task contracts: if the label moves to another layer, the narration has changed the question rather than explained the mechanism. Use the bounded normalization label as a checkpoint for D task contracts: if the label moves to another layer, the narration has changed the question rather than explained the mechanism.

## c05 — D7: bounded normalization and charged loss

Exactness and engineering completion do not guarantee overhead-inclusive profitability. Place routing, proof, cache, gate, natural incidence, revision, and normalization milestones on the D-D7 sequence. The system became more exact and accountable faster than it became profitable. A common mistake is this: A proved rewrite or cache hit is automatically beneficial after its matching and validation costs are charged.

Repair it by returning to the owned distinction: D task contracts.

*[Three-second retrieval pause.]*

Use the episode's central distinction to answer: The system became more exact and accountable faster than it became profitable. The D sequence asks whether safe structure saves enough whole-task work to matter.
