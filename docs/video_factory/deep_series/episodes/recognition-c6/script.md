# C6-C8 exact source ANF: packed cores and transfer

Video ID: `recognition-c6`  
Episode content identity: `7beb2b1fe2d2d45c07a321e4225521adf40b4108a5cb7af57a5a3d526bedf6c0`  
Status: **complete narration draft; production candidate pending content approval**

## c01 — From learned proposals to an exact source core

What can advance when a deterministic exact core improves but one frozen gate still misses? This episode has one job: C6 advanced a deterministic packed exact source-ANF core; C7-C8 then preserved exact identities on an independent family and Linux while showing that the fastest exact representation remained workload- and machine-sensitive. We build on C3-C5 natural cuts: improvements without held-out promotion, Packed truth vectors: big integers, machine words, and masks; their definitions stay fixed while this lesson adds a new layer. C6-C8 study exact source-ANF representations, their guarded cost, and transfer across source families and machines.

Watch for the label algebraic normal form; it stays attached to the artifact this episode means. When we say source-DAG ANF, the highlight identifies its layer before we interpret it. The term packed coefficient vector receives its own visual state so it cannot drift into a neighboring concept. We will keep cross-machine exact transfer visible whenever its definition controls the inference.

## c02 — C6 packed source ANF

The next sequence uses Conceptual recognition/decomposition graph as a conceptual teaching example, not as measured benchmark evidence. Our stable example is Conceptual recognition/decomposition graph. A tiny labeled expression graph with a proposed partition, an exact witness path, a rejection/fallback path, and a promotion gate; it is never presented as a measured trace. Keep proposal, exact verification, fallback, and promotion visually stable across CRSE episodes.

We now construct the Conceptual recognition/decomposition graph and name every part before interpreting it. Now isolate C6 packed exact source ANF inside Conceptual recognition/decomposition graph. The example definition and output meaning stay fixed while only the state for C6 packed exact source ANF changes. That matched before-and-after view assigns the visible consequence to C6 packed exact source ANF, not to a substituted workload.

A source DAG becomes a coefficient map and then a packed integer with bit identities visible. A labeled input graph enters a proposal stage whose job is to suggest a partition or route, not to certify it. Holding that element fixed lets this episode isolate C6 packed exact source ANF. The promotion gate is separate from exact verification because a correct result need not satisfy a performance promotion rule.

Changing the proposal mechanism never removes the exact verifier or the fallback path from the contract. Every measured promotion panel names its workload, boundary, uncertainty, and source independently of this teaching graph. The starting state preserves the stable definition: A tiny labeled expression graph with a proposed partition, an exact witness path, a rejection/fallback path, and a promotion gate; it is never presented as a measured trace. After the change, the composition makes C6 packed exact source ANF inspectable: A source DAG becomes a coefficient map and then a packed integer with bit identities visible.

The invariant is the example's meaning; the variable is C6 packed exact source ANF. If the same output is reached through a different path, the diagram must still identify whether C6 packed exact source ANF changed. The accompanying view makes this step concrete: A source DAG becomes a coefficient map and then a packed integer with bit identities visible. Read C6 packed exact source ANF from input to consequence through one matched view: A source DAG becomes a coefficient map and then a packed integer with bit identities visible; every surrounding entity keeps its prior meaning.

The diagram treats C6 packed exact source ANF as a located state, artifact, or boundary rather than as an unexplained method label. Before the consequence appears, predict what C6 packed exact source ANF can change and what the stable example requires it to leave untouched. When evidence enters, its status badge binds the statement about C6 packed exact source ANF to a source and scope, not to the conceptual drawing alone. The nearest neighboring lesson stays out of focus because its owned question is not needed to explain C6 packed exact source ANF in this sequence.

The transfer rule carries C6 packed exact source ANF forward only with the same input definition, exactness gate, and declared output contract. Use the algebraic normal form label as a checkpoint for C6 packed exact source ANF: if the label moves to another layer, the narration has changed the question rather than explained the mechanism. Use the source-DAG ANF label as a checkpoint for C6 packed exact source ANF: if the label moves to another layer, the narration has changed the question rather than explained the mechanism. Use the packed coefficient vector label as a checkpoint for C6 packed exact source ANF: if the label moves to another layer, the narration has changed the question rather than explained the mechanism.

Use the cross-machine exact transfer label as a checkpoint for C6 packed exact source ANF: if the label moves to another layer, the narration has changed the question rather than explained the mechanism.

## c03 — C7 independent-family transfer

Transform a tiny source graph into exact ANF coefficients and pack them while preserving the witness path. Keep this separation visible: C6 core advancement from its missed gate, then carry identical source cases into C7 and C8 transfer lanes. Now isolate C7 independent-source transfer inside Conceptual recognition/decomposition graph. The example definition and output meaning stay fixed while only the state for C7 independent-source transfer changes.

That matched before-and-after view assigns the visible consequence to C7 independent-source transfer, not to a substituted workload. Windows, independent Yosys source, and Linux lanes compare exact identities before any timing. Milestone C6's packed exact source-ANF core advanced, but its gate and production path did not. Read that statement only within this scope: Milestone C6 frozen EPFL splits.

Its declared measurement boundary is exact recognition core and gate. The uncertainty field says bounded one-program slice. C7 and C8 retained exact source-ANF identities across an independent Yosys-derived family and Linux, while backend profitability remained representation- and machine-sensitive. Read that statement only within this scope: Milestones C7-C8 exact transfer studies.

Its declared measurement boundary is exact recognition and cross-machine execution. The uncertainty field says bounded source families and machines. Packed bigint and machine-word evaluators store the same ordered truth vector in different exact execution layouts with explicit masks and tail handling. Read that statement only within this scope: current packed evaluator implementations.

Its declared measurement boundary is execution representation. The uncertainty field says backend choice remains workload dependent. The candidate partition remains visibly provisional until the exact verifier checks its witness conditions. Holding that element fixed lets this episode isolate C7 independent-source transfer.

Proposal, verifier, witness, rejection, fallback, and promotion keep stable shapes across every CRSE episode. Negative and not-promoted outcomes remain first-class states rather than disappearing from the research timeline. After the change, the composition makes C7 independent-source transfer inspectable: Windows, independent Yosys source, and Linux lanes compare exact identities before any timing. The invariant is the example's meaning; the variable is C7 independent-source transfer.

If the same output is reached through a different path, the diagram must still identify whether C7 independent-source transfer changed. The accompanying view makes this step concrete: Windows, independent Yosys source, and Linux lanes compare exact identities before any timing. The accompanying view makes this step concrete: Core, gate, and production-path cards use separate status badges. Read C7 independent-source transfer from input to consequence through one matched view: Windows, independent Yosys source, and Linux lanes compare exact identities before any timing; every surrounding entity keeps its prior meaning.

The diagram treats C7 independent-source transfer as a located state, artifact, or boundary rather than as an unexplained method label. Before the consequence appears, predict what C7 independent-source transfer can change and what the stable example requires it to leave untouched. When evidence enters, its status badge binds the statement about C7 independent-source transfer to a source and scope, not to the conceptual drawing alone. The nearest neighboring lesson stays out of focus because its owned question is not needed to explain C7 independent-source transfer in this sequence.

The transfer rule carries C7 independent-source transfer forward only with the same input definition, exactness gate, and declared output contract. Use the algebraic normal form label as a checkpoint for C7 independent-source transfer: if the label moves to another layer, the narration has changed the question rather than explained the mechanism.

## c04 — C8 Linux exact transfer and backend sensitivity

Show set, packed, and bitset medians/tails changing order without changing exact partition identity. Now isolate C8 Linux exact transfer and backend sensitivity inside Conceptual recognition/decomposition graph. The example definition and output meaning stay fixed while only the state for C8 Linux exact transfer and backend sensitivity changes. That matched before-and-after view assigns the visible consequence to C8 Linux exact transfer and backend sensitivity, not to a substituted workload.

The nearest confusing lesson is C9-C11 exact routing: static trees, guarded restart, and one-pass conversion; it owns C9 static analytic routing, while this episode owns C6 packed exact source ANF. An accepted path produces an exact witness; a rejected path moves to the safe fallback without changing correctness. Holding that element fixed lets this episode isolate C8 Linux exact transfer and backend sensitivity. A conceptual badge stays on the graph even when a later panel introduces separately sourced measurements.

Task routing may select a different exact path while the admitted result and verification gate remain fixed. After the change, the composition makes C8 Linux exact transfer and backend sensitivity inspectable: Windows, independent Yosys source, and Linux lanes compare exact identities before any timing. The invariant is the example's meaning; the variable is C8 Linux exact transfer and backend sensitivity. If the same output is reached through a different path, the diagram must still identify whether C8 Linux exact transfer and backend sensitivity changed.

The accompanying view makes this step concrete: A backend portfolio triangle shifts by sparse median, tail protection, and machine without choosing one universal vertex. Read C8 Linux exact transfer and backend sensitivity from input to consequence through one matched view: Windows, independent Yosys source, and Linux lanes compare exact identities before any timing; every surrounding entity keeps its prior meaning. The diagram treats C8 Linux exact transfer and backend sensitivity as a located state, artifact, or boundary rather than as an unexplained method label. Before the consequence appears, predict what C8 Linux exact transfer and backend sensitivity can change and what the stable example requires it to leave untouched.

When evidence enters, its status badge binds the statement about C8 Linux exact transfer and backend sensitivity to a source and scope, not to the conceptual drawing alone. The nearest neighboring lesson stays out of focus because its owned question is not needed to explain C8 Linux exact transfer and backend sensitivity in this sequence. The transfer rule carries C8 Linux exact transfer and backend sensitivity forward only with the same input definition, exactness gate, and declared output contract.

## c05 — What advanced and what stayed guarded

Exact identity transferred; the fastest set, packed, or bitset representation did not become universal. Classify core, gate, transfer, and backend-ranking outcomes as advanced, held, or workload-sensitive. Exact transfer was stronger than any universal backend ranking. A common mistake is this: A packed-core improvement proves that packed ANF is the fastest exact representation on every source and machine.

Repair it by returning to the owned distinction: C6 packed exact source ANF.

*[Three-second retrieval pause.]*

Use the episode's central distinction to answer: Exact transfer was stronger than any universal backend ranking. Promote the exact component that passed, not the whole surrounding policy.
