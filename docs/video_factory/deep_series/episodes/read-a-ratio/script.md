# How to read a CM/comparator ratio

Video ID: `read-a-ratio`  
Episode content identity: `0b5d0f07b4773687e32abe3c524784035f8df15f1b2292749f2ffec2f553177d`  
Status: **complete narration draft; production candidate pending content approval**

## c01 — Question and definition: ratio direction

Does a ratio below one mean faster, slower, or nothing at all? This episode has one job: A ratio has no honest direction until numerator, denominator, favorable direction, workload, boundary, and uncertainty are visible. We build on Preparation, kernel, wrapper, and end-to-end time; their definitions stay fixed while this lesson adds a new layer. A ratio compares a named numerator with a named denominator; below one favors the numerator only for a time or cost ratio.

Watch for the label numerator; it stays attached to the artifact this episode means. When we say denominator, the highlight identifies its layer before we interpret it. The term ratio receives its own visual state so it cannot drift into a neighboring concept. We will keep parity visible whenever its definition controls the inference.

A persistent confidence interval label marks exactly where that object enters the example.

## c02 — Worked example: Four-variable truth layout

The next sequence uses Four-variable truth layout as a conceptual teaching example, not as measured benchmark evidence. Our stable example is Four-variable truth layout. F(A,B,C,D)=(A AND B) XOR (C OR D), row variables A,B, column variables C,D, MSB-first ordering. Carry assignments, live support, explicit CM, packed truth vectors, and exact output across episodes.

We now construct the Four-variable truth layout and name every part before interpreting it. Compute two toy elapsed-time ratios, then reverse numerator and denominator without changing the underlying times. Add the parity line and interval before introducing a retained CM/comparator result. Now isolate ratio direction inside Four-variable truth layout.

The example definition and output meaning stay fixed while only the state for ratio direction changes. That matched before-and-after view assigns the visible consequence to ratio direction, not to a substituted workload. The same point flips around one when the ratio direction reverses. Now isolate parity line inside Four-variable truth layout.

The example definition and output meaning stay fixed while only the state for parity line changes. That matched before-and-after view assigns the visible consequence to parity line, not to a substituted workload. Now isolate interval interpretation inside Four-variable truth layout. The example definition and output meaning stay fixed while only the state for interval interpretation changes.

That matched before-and-after view assigns the visible consequence to interval interpretation, not to a substituted workload. A retained ratio appears only after workload, boundary, interval, and favorable arrow are in place. Every ratio must state numerator, denominator, workload, measurement boundary, and uncertainty; below one favors CM only for CM/comparator. Read that statement only within this scope: video evidence contract.

Its declared measurement boundary is all numeric scenes. The uncertainty field says declared per ratio. The nearest confusing lesson is Why scopes and boundaries matter; it owns scope comparison, while this episode owns ratio direction. The example fixes F of A, B, C, and D as A AND B, exclusive-or C OR D.

Holding that element fixed lets this episode isolate ratio direction. A and B select the row, C and D select the column, and MSB-first order fixes both index conventions. Holding that element fixed lets this episode isolate parity line. Every four-bit assignment names exactly one of the sixteen cells, and every cell returns one exact output bit.

Holding that element fixed lets this episode isolate interval interpretation. Folding the truth-table output column into a four-by-four grid changes the layout, not the function. Reading a cell forward evaluates an assignment; reading it backward recovers the row and column bits that selected it. Live support can remove an input from active computation even when the ambient layout still reserves its axis.

A packed truth vector stores the same exact outputs in a different physical arrangement from the displayed matrix. The explicit matrix is dense by contract, so compactness and speed require separate evidence rather than definition alone.

## c03 — Boundary, retrieval, and transfer

Position and color cannot substitute for labels, scope, boundary, and uncertainty. Interpret two reversed ratios and identify which arm is favored before the arrows appear. A ratio becomes evidence only after its direction and scope are explicit. A common mistake is this: Every point below one favors CM regardless of numerator, metric, or boundary.

Repair it by returning to the owned distinction: ratio direction.

*[Three-second retrieval pause.]*

Use the episode's central distinction to answer: A ratio becomes evidence only after its direction and scope are explicit. Read the fraction before reading the dot.
