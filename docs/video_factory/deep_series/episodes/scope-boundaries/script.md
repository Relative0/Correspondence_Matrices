# Why scopes and boundaries matter

Video ID: `scope-boundaries`  
Episode content identity: `ce296842fec87a96683a3e600dbbc7cbeef1efa64d614cd37b0dc705c90dc544`  
Status: **complete narration draft; production candidate pending content approval**

## c01 — Question and definition: scope comparison

Why can't four honest numbers be averaged into one honest winner? This episode has one job: Workload, output contract, machine, reuse, and measurement boundary define a claim's scope; similar-looking ratios outside that scope cannot be pooled into one conclusion. We build on How to read a CM/comparator ratio; their definitions stay fixed while this lesson adds a new layer. Scope names the conditions under which a claim is supported; the boundary names the work that was measured.

Watch for the label scope; it stays attached to the artifact this episode means. When we say workload, the highlight identifies its layer before we interpret it. The term output contract receives its own visual state so it cannot drift into a neighboring concept. We will keep incomparable boundary visible whenever its definition controls the inference.

## c02 — Worked example: Small AND/INV cone

The next sequence uses Small AND/INV cone as a conceptual teaching example, not as measured benchmark evidence. Our stable example is Small AND/INV cone. A conceptual five-input AND/INV cone whose output has four-variable semantic support, paired later with separately labeled retained EPFL evidence. Teach cone support, fanout, lowering, exact digests, and the accepted EPFL mechanism.

We now construct the Small AND/INV cone and name every part before interpreting it. Place B2/B4 kernel, EPFL kernel, preparation, and wrapper panels on a two-axis workload/boundary grid. Attempt to pool them, surface the mismatches, and return to separate scoped conclusions. Now isolate scope comparison inside Small AND/INV cone.

The example definition and output meaning stay fixed while only the state for scope comparison changes. That matched before-and-after view assigns the visible consequence to scope comparison, not to a substituted workload. Allowed wording updates as the viewer drags a scope bracket across cells. Now isolate non-pooling rule inside Small AND/INV cone.

The example definition and output meaning stay fixed while only the state for non-pooling rule changes. That matched before-and-after view assigns the visible consequence to non-pooling rule, not to a substituted workload. Current evidence does not support a universal claim that CM beats CSE-flat. Read that statement only within this scope: cross-study synthesis.

Its declared measurement boundary is multiple boundaries kept separate. The uncertainty field says scopes are intentionally not pooled. Every ratio must state numerator, denominator, workload, measurement boundary, and uncertainty; below one favors CM only for CM/comparator. Read that statement only within this scope: video evidence contract.

Its declared measurement boundary is all numeric scenes. The uncertainty field says declared per ratio. CM, CM-IR, SAT, BDD, minimization, and symbolic algebra answer different questions and must be compared under matched output contracts. Read that statement only within this scope: implemented backend and task contracts.

Its declared measurement boundary is output contract. The uncertainty field says scope dependent. The nearest confusing lesson is Reuse and break-even economics; it owns one-time versus repeated cost, while this episode owns scope comparison. The cone starts from five named inputs, but the displayed output has four-variable semantic support.

Holding that element fixed lets this episode isolate scope comparison. The inert input stays outlined so nominal width cannot be mistaken for active support. Holding that element fixed lets this episode isolate non-pooling rule. A topological trace orders the gates without changing their connectivity or the output function.

## c03 — Boundary, retrieval, and transfer

B2/B4, EPFL, preparation, kernel, and wrapper evidence remain separate when their scopes differ. Reject the two results that cannot be pooled with a declared kernel/workload cell. Honest evidence gets narrower before it gets stronger. Now isolate boundary mismatch inside Small AND/INV cone.

The example definition and output meaning stay fixed while only the state for boundary mismatch changes. That matched before-and-after view assigns the visible consequence to boundary mismatch, not to a substituted workload. A pooling funnel rejects cards with mismatched output, boundary, machine, or dependence structure. AND and inversion nodes form a small exact dependency cone whose fanout remains visible throughout the lesson.

Holding that element fixed lets this episode isolate boundary mismatch. A common mistake is this: All CM/comparator ratios estimate one underlying universal performance number. Repair it by returning to the owned distinction: scope comparison.

*[Three-second retrieval pause.]*

Use the episode's central distinction to answer: Honest evidence gets narrower before it gets stronger. Keep each result inside the conditions that made it true.
