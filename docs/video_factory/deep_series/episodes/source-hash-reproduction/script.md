# How a video is bound to source hashes

Video ID: `source-hash-reproduction`  
Episode content identity: `b96bd649556fe94ddc5c5adfcf33607370b4ef066d3cf8cdcd69f883a985b463`  
Status: **complete narration draft; production candidate pending content approval**

## c01 — What reproducibility must bind

If one evidence file changes, how do we know which sentence, scene, and rendered chapter are stale? This episode has one job: Reproducible video production links source locator and hash to claim, content bible, script cue, storyboard, render job, output, and separate content and RunPod approval identities. We build on Conceptual animation versus measured result, Truth digests, alternating schedules, clustering, and intervals; their definitions stay fixed while this lesson adds a new layer. A content hash binds normalized inputs; provenance records which sources and transformations produced each downstream artifact.

Watch for the label source locator; it stays attached to the artifact this episode means. When we say content hash, the highlight identifies its layer before we interpret it. The term cache identity receives its own visual state so it cannot drift into a neighboring concept. We will keep content approval identity visible whenever its definition controls the inference.

A persistent RunPod authorization identity label marks exactly where that object enters the example.

## c02 — Source locator through claim and content

The next sequence uses Shared Boolean expression as a conceptual teaching example, not as measured benchmark evidence. Our stable example is Shared Boolean expression. S=A AND B; F=(S AND C AND D) OR (S AND E), with binary source syntax and stable variable order A,B,C,D,E. Carry AST, CSE, CSE-flat, CM-IR, lowering, and operation accounting across episodes.

We now construct the Shared Boolean expression and name every part before interpreting it. Now isolate source-to-release provenance chain inside Shared Boolean expression. The example definition and output meaning stay fixed while only the state for source-to-release provenance chain changes. That matched before-and-after view assigns the visible consequence to source-to-release provenance chain, not to a substituted workload.

A provenance graph grows source -> locator -> claim -> bible -> script -> storyboard -> render job -> output -> release. Begin with A and B, compute the shared subexpression S once, and keep that identity color on every outgoing use. Holding that element fixed lets this episode isolate source-to-release provenance chain. A raw syntax view may draw S twice; a shared graph draws one node with two outgoing edges.

Changing from bigint bits to word lanes changes storage and execution layout, not the meaning of S or F. The output check closes both paths against the same exact truth behavior before performance is interpreted. The starting state preserves the stable definition: S=A AND B; F=(S AND C AND D) OR (S AND E), with binary source syntax and stable variable order A,B,C,D,E. After the change, the composition makes source-to-release provenance chain inspectable: A provenance graph grows source -> locator -> claim -> bible -> script -> storyboard -> render job -> output -> release.

The invariant is the example's meaning; the variable is source-to-release provenance chain. If the same output is reached through a different path, the diagram must still identify whether source-to-release provenance chain changed.

## c03 — Script, storyboard, job, and media identity

Trace one retained claim from source path/locator/hash through the content bible, script cue, scene, job, result, and release manifest. Change the source, propagate invalidation through every dependent identity, and preserve unrelated cached episodes. Construct a content-approval manifest and a separate RunPod authorization, showing that neither implies the other. A video production artifact is reproducible only when source locators, source hashes, claim IDs, script/storyboard identities, render jobs, outputs, and approval identities remain linked.

Read that statement only within this scope: CM video factory and RunPod safety contract. Its declared measurement boundary is production provenance. The uncertainty field says hash identity does not itself establish scientific truth. Teaching diagrams, proposed mechanisms, and hypotheses must be visibly labeled and may not be presented as measured traces.

Read that statement only within this scope: video evidence contract. Its declared measurement boundary is editorial/visual status. The uncertainty field says not applicable. The nearest confusing lesson is How an audit changed the headline; it owns claim supersession, while this episode owns source-to-release provenance chain.

## c04 — Downstream invalidation and two separate approvals

Hash identity supports reproducibility and invalidation, but it does not establish scientific truth or authorize cloud spending. Identify which episodes invalidate after one shared source changes and which cached episodes remain valid. Reproducibility is a connected chain of identities, evidence, and approvals. Now isolate downstream invalidation inside Shared Boolean expression.

The example definition and output meaning stay fixed while only the state for downstream invalidation changes. That matched before-and-after view assigns the visible consequence to downstream invalidation, not to a substituted workload. One source hash changes and red invalidation travels only along dependent edges. Now isolate content approval versus RunPod authorization inside Shared Boolean expression.

The example definition and output meaning stay fixed while only the state for content approval versus RunPod authorization changes. That matched before-and-after view assigns the visible consequence to content approval versus RunPod authorization, not to a substituted workload. Content approval and RunPod authorization appear as two separate locks over different artifact sets and effects. The left branch combines S with C and D, while the right branch combines the same S with E.

Holding that element fixed lets this episode isolate downstream invalidation. Both branches meet at the final OR, so the shared node changes program structure without changing the Boolean output. Holding that element fixed lets this episode isolate content approval versus RunPod authorization. Lowering the graph assigns dependency-ordered slots, but slot numbers are not new Boolean operations.

Instruction count, primitive execution count, live buffers, and packed storage therefore remain separate ledgers. The stable variable order A through E prevents a convenient reordering from hiding inside a method comparison. Any claimed reduction must point to the repeated S path or another named transformation actually present here. After the change, the composition makes downstream invalidation inspectable: One source hash changes and red invalidation travels only along dependent edges.

The invariant is the example's meaning; the variable is downstream invalidation. If the same output is reached through a different path, the diagram must still identify whether downstream invalidation changed. After the change, the composition makes content approval versus RunPod authorization inspectable: Content approval and RunPod authorization appear as two separate locks over different artifact sets and effects. A common mistake is this: A valid SHA-256 alone proves the underlying scientific claim and authorizes its remote production.

Repair it by returning to the owned distinction: source-to-release provenance chain.

*[Three-second retrieval pause.]*

Use the episode's central distinction to answer: Reproducibility is a connected chain of identities, evidence, and approvals. Every claim and pixel should be traceable without placing a credential anywhere in the chain.
