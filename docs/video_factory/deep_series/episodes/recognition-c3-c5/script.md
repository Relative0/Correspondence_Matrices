# C3-C5 natural cuts: improvements without held-out promotion

Video ID: `recognition-c3-c5`  
Episode content identity: `d035ed2a2788a44ad58a344d53a3551042a44148c43c6b9041133b30ba352284`  
Status: **complete narration draft; production candidate pending content approval**

## c01 — One frozen recognition question

Can better natural data and a better readout turn local improvement into a reliable held-out cut? This episode has one job: C3-C5 improved natural data, cut supervision, ranking, and equivariant variable heads, but held-out recovery and charged learned paths remained below promotion criteria. We build on C2 variable-size decomposition: exact control, learned failure; their definitions stay fixed while this lesson adds a new layer. C3-C5 progressively changed data, target, and equivariant readout while retaining exact acceptance and held-out criteria.

Watch for the label natural positive; it stays attached to the artifact this episode means. When we say matched negative, the highlight identifies its layer before we interpret it. The term canonical cut receives its own visual state so it cannot drift into a neighboring concept. We will keep equivariance visible whenever its definition controls the inference.

A persistent accepted recall label marks exactly where that object enters the example.

## c02 — C3 natural positives and matched negatives

The next sequence uses Conceptual recognition/decomposition graph as a conceptual teaching example, not as measured benchmark evidence. Our stable example is Conceptual recognition/decomposition graph. A tiny labeled expression graph with a proposed partition, an exact witness path, a rejection/fallback path, and a promotion gate; it is never presented as a measured trace. Keep proposal, exact verification, fallback, and promotion visually stable across CRSE episodes.

We now construct the Conceptual recognition/decomposition graph and name every part before interpreting it. Now isolate C3 natural decomposition data inside Conceptual recognition/decomposition graph. The example definition and output meaning stay fixed while only the state for C3 natural decomposition data changes. That matched before-and-after view assigns the visible consequence to C3 natural decomposition data, not to a substituted workload.

A milestone timeline adds one methodological change at C3, C4, and C5 without resetting earlier evidence. A labeled input graph enters a proposal stage whose job is to suggest a partition or route, not to certify it. Holding that element fixed lets this episode isolate C3 natural decomposition data. Proposal, verifier, witness, rejection, fallback, and promotion keep stable shapes across every CRSE episode.

Task routing may select a different exact path while the admitted result and verification gate remain fixed. The starting state preserves the stable definition: A tiny labeled expression graph with a proposed partition, an exact witness path, a rejection/fallback path, and a promotion gate; it is never presented as a measured trace. After the change, the composition makes C3 natural decomposition data inspectable: A milestone timeline adds one methodological change at C3, C4, and C5 without resetting earlier evidence. The invariant is the example's meaning; the variable is C3 natural decomposition data.

If the same output is reached through a different path, the diagram must still identify whether C3 natural decomposition data changed. The accompanying view makes this step concrete: A milestone timeline adds one methodological change at C3, C4, and C5 without resetting earlier evidence. Read C3 natural decomposition data from input to consequence through one matched view: A milestone timeline adds one methodological change at C3, C4, and C5 without resetting earlier evidence; every surrounding entity keeps its prior meaning. The diagram treats C3 natural decomposition data as a located state, artifact, or boundary rather than as an unexplained method label.

Before the consequence appears, predict what C3 natural decomposition data can change and what the stable example requires it to leave untouched. When evidence enters, its status badge binds the statement about C3 natural decomposition data to a source and scope, not to the conceptual drawing alone. The nearest neighboring lesson stays out of focus because its owned question is not needed to explain C3 natural decomposition data in this sequence. The transfer rule carries C3 natural decomposition data forward only with the same input definition, exactness gate, and declared output contract.

Use the natural positive label as a checkpoint for C3 natural decomposition data: if the label moves to another layer, the narration has changed the question rather than explained the mechanism.

## c03 — C4 complete cuts and ranking

Advance one fixed example through C3 dataset construction, C4 direct cut supervision, and C5 variable-conditioned scoring. Keep confirmatory improvements, held-out square failures, exact acceptance, and charged inference time on separate axes. Now isolate C4 direct-cut/ranking inside Conceptual recognition/decomposition graph. The example definition and output meaning stay fixed while only the state for C4 direct-cut/ranking changes.

That matched before-and-after view assigns the visible consequence to C4 direct-cut/ranking, not to a substituted workload. Natural decomposition, direct-cut, and variable-conditioned learned arms improved some slices but failed the required held-out transfer/promotion criteria. Read that statement only within this scope: Milestones C3–C5 natural circuit-disjoint studies. Its declared measurement boundary is recognition plus exact acceptance.

The uncertainty field says circuit-family transfer remained limited. The CRSE recognition program is experimental; no learned model is promoted. Read that statement only within this scope: current recognition program. Its declared measurement boundary is scientific promotion.

The uncertainty field says per-milestone frozen splits and criteria. The candidate partition remains visibly provisional until the exact verifier checks its witness conditions. Holding that element fixed lets this episode isolate C4 direct-cut/ranking. A conceptual badge stays on the graph even when a later panel introduces separately sourced measurements.

Every measured promotion panel names its workload, boundary, uncertainty, and source independently of this teaching graph. After the change, the composition makes C4 direct-cut/ranking inspectable: A milestone timeline adds one methodological change at C3, C4, and C5 without resetting earlier evidence. The invariant is the example's meaning; the variable is C4 direct-cut/ranking. If the same output is reached through a different path, the diagram must still identify whether C4 direct-cut/ranking changed.

The accompanying view makes this step concrete: Predicted cut edges pass through an exact witness gate before acceptance. The accompanying view makes this step concrete: Matched positive/negative graph pairs expose why superficial graph cues are insufficient. Read C4 direct-cut/ranking from input to consequence through one matched view: A milestone timeline adds one methodological change at C3, C4, and C5 without resetting earlier evidence; every surrounding entity keeps its prior meaning. The diagram treats C4 direct-cut/ranking as a located state, artifact, or boundary rather than as an unexplained method label.

Before the consequence appears, predict what C4 direct-cut/ranking can change and what the stable example requires it to leave untouched. When evidence enters, its status badge binds the statement about C4 direct-cut/ranking to a source and scope, not to the conceptual drawing alone. The nearest neighboring lesson stays out of focus because its owned question is not needed to explain C4 direct-cut/ranking in this sequence. The transfer rule carries C4 direct-cut/ranking forward only with the same input definition, exactness gate, and declared output contract.

## c04 — C5 equivariant variable heads

End with exact ANF retained as the accepted control while learned fitting remains paused. Now isolate C5 variable-conditioned equivariant cuts inside Conceptual recognition/decomposition graph. The example definition and output meaning stay fixed while only the state for C5 variable-conditioned equivariant cuts changes. That matched before-and-after view assigns the visible consequence to C5 variable-conditioned equivariant cuts, not to a substituted workload.

The nearest confusing lesson is C6-C8 exact source ANF: packed cores and transfer; it owns C6 packed exact source ANF, while this episode owns C3 natural decomposition data. An accepted path produces an exact witness; a rejected path moves to the safe fallback without changing correctness. Holding that element fixed lets this episode isolate C5 variable-conditioned equivariant cuts. Changing the proposal mechanism never removes the exact verifier or the fallback path from the contract.

After the change, the composition makes C5 variable-conditioned equivariant cuts inspectable: A milestone timeline adds one methodological change at C3, C4, and C5 without resetting earlier evidence. The invariant is the example's meaning; the variable is C5 variable-conditioned equivariant cuts. If the same output is reached through a different path, the diagram must still identify whether C5 variable-conditioned equivariant cuts changed. The accompanying view makes this step concrete: Confirmatory and held-out panels remain side by side, with cost charged below both.

Read C5 variable-conditioned equivariant cuts from input to consequence through one matched view: A milestone timeline adds one methodological change at C3, C4, and C5 without resetting earlier evidence; every surrounding entity keeps its prior meaning. The diagram treats C5 variable-conditioned equivariant cuts as a located state, artifact, or boundary rather than as an unexplained method label. Before the consequence appears, predict what C5 variable-conditioned equivariant cuts can change and what the stable example requires it to leave untouched. When evidence enters, its status badge binds the statement about C5 variable-conditioned equivariant cuts to a source and scope, not to the conceptual drawing alone.

The nearest neighboring lesson stays out of focus because its owned question is not needed to explain C5 variable-conditioned equivariant cuts in this sequence. The transfer rule carries C5 variable-conditioned equivariant cuts forward only with the same input definition, exactness gate, and declared output contract.

## c05 — Held-out transfer, charged cost, and non-promotion

Improvement on confirmatory circuits did not satisfy the required held-out promotion or cost criteria. Match each C3-C5 change to data, supervision, or readout, then choose the final promotion status. The learned path improved, but the exact control remained the accepted path. Now isolate held-out non-promotion inside Conceptual recognition/decomposition graph.

The example definition and output meaning stay fixed while only the state for held-out non-promotion changes. That matched before-and-after view assigns the visible consequence to held-out non-promotion, not to a substituted workload. Confirmatory and held-out panels remain side by side, with cost charged below both. The promotion gate is separate from exact verification because a correct result need not satisfy a performance promotion rule.

Holding that element fixed lets this episode isolate held-out non-promotion. Negative and not-promoted outcomes remain first-class states rather than disappearing from the research timeline. After the change, the composition makes held-out non-promotion inspectable: Confirmatory and held-out panels remain side by side, with cost charged below both. The invariant is the example's meaning; the variable is held-out non-promotion.

If the same output is reached through a different path, the diagram must still identify whether held-out non-promotion changed. Read held-out non-promotion from input to consequence through one matched view: Confirmatory and held-out panels remain side by side, with cost charged below both; every surrounding entity keeps its prior meaning. The diagram treats held-out non-promotion as a located state, artifact, or boundary rather than as an unexplained method label. Before the consequence appears, predict what held-out non-promotion can change and what the stable example requires it to leave untouched.

When evidence enters, its status badge binds the statement about held-out non-promotion to a source and scope, not to the conceptual drawing alone. The nearest neighboring lesson stays out of focus because its owned question is not needed to explain held-out non-promotion in this sequence. The transfer rule carries held-out non-promotion forward only with the same input definition, exactness gate, and declared output contract. A common mistake is this: Any increase in confirmatory accuracy is sufficient evidence of held-out generalization.

Repair it by returning to the owned distinction: C3 natural decomposition data.

*[Three-second retrieval pause.]*

Use the episode's central distinction to answer: The learned path improved, but the exact control remained the accepted path. A better model can still be a retained negative result when the frozen question remains unanswered.
