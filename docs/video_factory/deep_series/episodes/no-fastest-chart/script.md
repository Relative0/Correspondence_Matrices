# Why one blended fastest-method chart is dishonest

Video ID: `no-fastest-chart`  
Episode content identity: `a2ee21fc1e0c47398a6b2fb981f14d28d536ac429db13b20228e0853ac5bae2c`  
Status: **complete narration draft; production candidate pending content approval**

## c01 — Question and definition: anti-podium argument

What had to be hidden to make one method look fastest everywhere? This episode has one job: A single podium destroys the output, workload, boundary, preparation, reuse, and uncertainty information needed to interpret method performance. We build on Why scopes and boundaries matter, Corrected B2/B4 V3 kernel result, EPFL AND/INV parity and its mechanism; their definitions stay fixed while this lesson adds a new layer. A fastest-method ranking is meaningful only for one matched output, workload, boundary, reuse pattern, and comparator set.

Watch for the label blended ranking; it stays attached to the artifact this episode means. When we say task-matched comparison, the highlight identifies its layer before we interpret it. The term decision grid receives its own visual state so it cannot drift into a neighboring concept.

## c02 — Worked example: Small AND/INV cone

The next sequence uses Small AND/INV cone as a conceptual teaching example, not as measured benchmark evidence. Our stable example is Small AND/INV cone. A conceptual five-input AND/INV cone whose output has four-variable semantic support, paired later with separately labeled retained EPFL evidence. Teach cone support, fanout, lowering, exact digests, and the accepted EPFL mechanism.

We now construct the Small AND/INV cone and name every part before interpreting it. Construct a tempting fastest-method podium by hiding scope labels. Restore output contract, workload, boundary, reuse, and uncertainty until the podium becomes a conditional decision grid. Now isolate anti-podium argument inside Small AND/INV cone.

The example definition and output meaning stay fixed while only the state for anti-podium argument changes. That matched before-and-after view assigns the visible consequence to anti-podium argument, not to a substituted workload. A glossy three-place podium appears with method logos but no task labels. Current evidence does not support a universal claim that CM beats CSE-flat.

Read that statement only within this scope: cross-study synthesis. Its declared measurement boundary is multiple boundaries kept separate. The uncertainty field says scopes are intentionally not pooled. Every ratio must state numerator, denominator, workload, measurement boundary, and uncertainty; below one favors CM only for CM/comparator.

Read that statement only within this scope: video evidence contract. Its declared measurement boundary is all numeric scenes. The uncertainty field says declared per ratio. CM, CM-IR, SAT, BDD, minimization, and symbolic algebra answer different questions and must be compared under matched output contracts.

Read that statement only within this scope: implemented backend and task contracts. Its declared measurement boundary is output contract. The uncertainty field says scope dependent. The nearest confusing lesson is Truth digests, alternating schedules, clustering, and intervals; it owns truth-digest exactness, while this episode owns anti-podium argument.

The cone starts from five named inputs, but the displayed output has four-variable semantic support. Holding that element fixed lets this episode isolate anti-podium argument. The inert input stays outlined so nominal width cannot be mistaken for active support. Structural counts describe nodes, edges, and operations; they do not directly measure hardware memory traffic.

The conceptual mechanism and EPFL evidence use matched colors but retain distinct status badges and source footers.

## c03 — Boundary, retrieval, and transfer

Current CM evidence deliberately contains a kernel advantage, a parity workload, and a slower wrapper. Name the missing label that invalidates each of three apparent rankings. The honest answer is conditional because the actual tasks are different. Now isolate task/boundary decision grid inside Small AND/INV cone.

The example definition and output meaning stay fixed while only the state for task/boundary decision grid changes. That matched before-and-after view assigns the visible consequence to task/boundary decision grid, not to a substituted workload. Missing labels return as physical layers and pull results into different grid cells. AND and inversion nodes form a small exact dependency cone whose fanout remains visible throughout the lesson.

Holding that element fixed lets this episode isolate task/boundary decision grid. A topological trace orders the gates without changing their connectivity or the output function. An exact output digest binds the conceptual cone to any separately labeled retained measurement panel. A common mistake is this: A blended chart can summarize unlike measurement boundaries without changing their meaning.

Repair it by returning to the owned distinction: anti-podium argument.

*[Three-second retrieval pause.]*

Use the episode's central distinction to answer: The honest answer is conditional because the actual tasks are different. A useful chart helps choose for a task; it does not manufacture one champion.
