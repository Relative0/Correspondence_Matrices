# C2 variable-size decomposition: exact control, learned failure

Video ID: `recognition-c2`  
Episode content identity: `dc67a1e05a8b2076a9d3d9d011f186817aa949fe1cc826c78fa51797ee0a76af`  
Status: **complete narration draft; production candidate pending content approval**

## c01 — Question and definition: C2 frozen question and split

What if the exact detector works perfectly but the learned detector fails on a larger size? This episode has one job: C2 produced an exact balanced-cofactor decomposition detector and witnesses, while the learned representation and size-transfer criteria failed and no model was promoted. We build on What the CRSE research program asks; their definitions stay fixed while this lesson adds a new layer. C2 asked whether a learned representation could transfer a balanced decomposition recognized by an exact control.

Watch for the label balanced cofactor decomposition; it stays attached to the artifact this episode means. When we say exact witness, the highlight identifies its layer before we interpret it. The term size transfer receives its own visual state so it cannot drift into a neighboring concept. We will keep no promotion visible whenever its definition controls the inference.

## c02 — Worked example: Conceptual recognition/decomposition graph

The next sequence uses Conceptual recognition/decomposition graph as a conceptual teaching example, not as measured benchmark evidence. Our stable example is Conceptual recognition/decomposition graph. A tiny labeled expression graph with a proposed partition, an exact witness path, a rejection/fallback path, and a promotion gate; it is never presented as a measured trace. Keep proposal, exact verification, fallback, and promotion visually stable across CRSE episodes.

We now construct the Conceptual recognition/decomposition graph and name every part before interpreting it. Now isolate C2 frozen question and split inside Conceptual recognition/decomposition graph. The example definition and output meaning stay fixed while only the state for C2 frozen question and split changes. That matched before-and-after view assigns the visible consequence to C2 frozen question and split, not to a substituted workload.

A small truth layout splits into row and column factors and recomposes through an exact witness gate. A labeled input graph enters a proposal stage whose job is to suggest a partition or route, not to certify it. Holding that element fixed lets this episode isolate C2 frozen question and split. The promotion gate is separate from exact verification because a correct result need not satisfy a performance promotion rule.

Changing the proposal mechanism never removes the exact verifier or the fallback path from the contract. Every measured promotion panel names its workload, boundary, uncertainty, and source independently of this teaching graph. The starting state preserves the stable definition: A tiny labeled expression graph with a proposed partition, an exact witness path, a rejection/fallback path, and a promotion gate; it is never presented as a measured trace. After the change, the composition makes C2 frozen question and split inspectable: A small truth layout splits into row and column factors and recomposes through an exact witness gate.

The invariant is the example's meaning; the variable is C2 frozen question and split. If the same output is reached through a different path, the diagram must still identify whether C2 frozen question and split changed. The accompanying view makes this step concrete: A small truth layout splits into row and column factors and recomposes through an exact witness gate.

## c03 — Mechanism: exact decomposition control; learned transfer failure

Construct a conceptual row/column partition and exact factor witness before introducing the frozen learned task. Show generated training sizes and held-out n=10 separately, then reveal exact-control success and learned criteria failure. Now isolate exact decomposition control inside Conceptual recognition/decomposition graph. The example definition and output meaning stay fixed while only the state for exact decomposition control changes.

That matched before-and-after view assigns the visible consequence to exact decomposition control, not to a substituted workload. Exact-control and learned-model result cards settle independently; only the learned promotion gate fails. Now isolate learned transfer failure inside Conceptual recognition/decomposition graph. The example definition and output meaning stay fixed while only the state for learned transfer failure changes.

That matched before-and-after view assigns the visible consequence to learned transfer failure, not to a substituted workload. Milestone C2's learned representation and size-transfer criteria failed while the exact CM detector remained perfect. Read that statement only within this scope: Milestone C2 frozen generated/held-out slices. Its declared measurement boundary is recognition evaluation.

The uncertainty field says limited target/domain scope. The CRSE recognition program is experimental; no learned model is promoted. Read that statement only within this scope: current recognition program. Its declared measurement boundary is scientific promotion.

The uncertainty field says per-milestone frozen splits and criteria. The nearest confusing lesson is C3-C5 natural cuts: improvements without held-out promotion; it owns C3 natural decomposition data, while this episode owns C2 frozen question and split. The candidate partition remains visibly provisional until the exact verifier checks its witness conditions. Holding that element fixed lets this episode isolate exact decomposition control.

An accepted path produces an exact witness; a rejected path moves to the safe fallback without changing correctness. Holding that element fixed lets this episode isolate learned transfer failure. Proposal, verifier, witness, rejection, fallback, and promotion keep stable shapes across every CRSE episode. A conceptual badge stays on the graph even when a later panel introduces separately sourced measurements.

Negative and not-promoted outcomes remain first-class states rather than disappearing from the research timeline. Task routing may select a different exact path while the admitted result and verification gate remain fixed. After the change, the composition makes exact decomposition control inspectable: Exact-control and learned-model result cards settle independently; only the learned promotion gate fails. The invariant is the example's meaning; the variable is exact decomposition control.

If the same output is reached through a different path, the diagram must still identify whether exact decomposition control changed. After the change, the composition makes learned transfer failure inspectable: Exact-control and learned-model result cards settle independently; only the learned promotion gate fails. The invariant is the example's meaning; the variable is learned transfer failure. If the same output is reached through a different path, the diagram must still identify whether learned transfer failure changed.

## c04 — Boundary, retrieval, and transfer

The exact detector's success does not rescue the failed learned size-transfer criterion. Predict which result can advance—the exact control, learned model, both, or neither—after the frozen criteria appear. Exact recognition advanced; learned transfer did not. A common mistake is this: A perfect exact control implies that the trained learner has discovered the same transferable rule.

Repair it by returning to the owned distinction: C2 frozen question and split.

*[Three-second retrieval pause.]*

Use the episode's central distinction to answer: Exact recognition advanced; learned transfer did not. C2 retained a useful exact teacher and an equally useful learned failure.
