# What CM does not claim to be

Video ID: `what-cm-does-not-claim`  
Episode content identity: `2fd23dd3b66aefc2cfff2a3a31d832750c5e8182403099459f52838ab2b3ee15`  
Status: **complete narration draft; production candidate pending content approval**

## c01 — Question and definition: CM non-claims

What goes wrong when one method name is used as the answer to three different questions? This episode has one job: CM is not automatically a compact representation, a satisfiability solver, CM-IR, or a universally fastest execution method. We build on What a correspondence matrix is; their definitions stay fixed while this lesson adds a new layer. A representation or solver is defined partly by the output contract it satisfies.

Watch for the label complete truth layout; it stays attached to the artifact this episode means. When we say satisfiability question, the highlight identifies its layer before we interpret it. The term universal performance claim receives its own visual state so it cannot drift into a neighboring concept.

## c02 — Worked example: Four-variable truth layout

The next sequence uses Four-variable truth layout as a conceptual teaching example, not as measured benchmark evidence. Our stable example is Four-variable truth layout. F(A,B,C,D)=(A AND B) XOR (C OR D), row variables A,B, column variables C,D, MSB-first ordering. Carry assignments, live support, explicit CM, packed truth vectors, and exact output across episodes.

We now construct the Four-variable truth layout and name every part before interpreting it. Ask for a complete vector, one satisfying assignment, and a reusable program, then show the different output each task requires. Pair a scoped kernel result with the slower public wrapper to reject method-label reasoning. Now isolate CM non-claims inside Four-variable truth layout.

The example definition and output meaning stay fixed while only the state for CM non-claims changes. That matched before-and-after view assigns the visible consequence to CM non-claims, not to a substituted workload. A dense grid, SAT witness, and CM-IR DAG are aligned by requested output rather than ranked. Now isolate output-contract mismatch examples inside Four-variable truth layout.

The example definition and output meaning stay fixed while only the state for output-contract mismatch examples changes. That matched before-and-after view assigns the visible consequence to output-contract mismatch examples, not to a substituted workload. CM, CM-IR, SAT, BDD, minimization, and symbolic algebra answer different questions and must be compared under matched output contracts. Read that statement only within this scope: implemented backend and task contracts.

Its declared measurement boundary is output contract. The uncertainty field says scope dependent. An explicit dense CM and CM-IR are different artifacts with different construction, storage, and evaluation boundaries. Read that statement only within this scope: current implementation.

Its declared measurement boundary is representation and evaluation. The uncertainty field says none. Current evidence does not support a universal claim that CM beats CSE-flat. Read that statement only within this scope: cross-study synthesis.

Its declared measurement boundary is multiple boundaries kept separate. The uncertainty field says scopes are intentionally not pooled. The nearest confusing lesson is Explicit dense CM versus CM-IR; it owns dense CM versus CM-IR artifact distinction, while this episode owns CM non-claims. The example fixes F of A, B, C, and D as A AND B, exclusive-or C OR D.

Holding that element fixed lets this episode isolate CM non-claims. A and B select the row, C and D select the column, and MSB-first order fixes both index conventions. Holding that element fixed lets this episode isolate output-contract mismatch examples. Every four-bit assignment names exactly one of the sixteen cells, and every cell returns one exact output bit.

Folding the truth-table output column into a four-by-four grid changes the layout, not the function. Reading a cell forward evaluates an assignment; reading it backward recovers the row and column bits that selected it. Live support can remove an input from active computation even when the ambient layout still reserves its axis. A packed truth vector stores the same exact outputs in a different physical arrangement from the displayed matrix.

## c03 — Boundary, retrieval, and transfer

Current evidence supports scoped comparisons, not a claim that CM wins every task or boundary. Match matrix, program graph, and SAT witness to three output requests. CM is one exact representation family, not a universal answer label. A common mistake is this: Calling an approach CM establishes compactness, solver behavior, or speed.

Repair it by returning to the owned distinction: CM non-claims.

*[Three-second retrieval pause.]*

Use the episode's central distinction to answer: CM is one exact representation family, not a universal answer label. Ask what must be returned before asking which method should return it.
