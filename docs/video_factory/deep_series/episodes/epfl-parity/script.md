# EPFL AND/INV parity and its mechanism

Video ID: `epfl-parity`  
Episode content identity: `8cc669a08fcea2fcb9df663faeb5bfa9a71edd184bcbfd8aef90eba7bcc0fba1`  
Status: **complete narration draft; production candidate pending content approval**

## c01 — Question and definition: EPFL parity result

What happens when the strong comparator has already captured every associative merge available in the circuit? This episode has one job: On accepted EPFL AND/INV cones, CSE-flat already captured the available associative reduction, instruction and operation counts matched, and the compiled kernels measured parity while CM preparation cost more. We build on Why scopes and boundaries matter, Plain CSE versus sharing-aware CSE-flat, Preparation, kernel, wrapper, and end-to-end time; their definitions stay fixed while this lesson adds a new layer. On this AND/INV workload, CM and CSE-flat had equal instruction structure and measured compiled-kernel parity.

Watch for the label AND/INV cone; it stays attached to the artifact this episode means. When we say mechanism prediction, the highlight identifies its layer before we interpret it. The term parity interval receives its own visual state so it cannot drift into a neighboring concept.

## c02 — Worked example: Small AND/INV cone

The next sequence uses Small AND/INV cone as a conceptual teaching example, not as measured benchmark evidence. Our stable example is Small AND/INV cone. A conceptual five-input AND/INV cone whose output has four-variable semantic support, paired later with separately labeled retained EPFL evidence. Teach cone support, fanout, lowering, exact digests, and the accepted EPFL mechanism.

We now construct the Small AND/INV cone and name every part before interpreting it. Now isolate EPFL parity result inside Small AND/INV cone. The example definition and output meaning stay fixed while only the state for EPFL parity result changes. That matched before-and-after view assigns the visible consequence to EPFL parity result, not to a substituted workload.

The timing interval settles across the parity line while preparation remains on a separate cost band. The cone starts from five named inputs, but the displayed output has four-variable semantic support. Holding that element fixed lets this episode isolate EPFL parity result. A topological trace orders the gates without changing their connectivity or the output function.

The conceptual mechanism and EPFL evidence use matched colors but retain distinct status badges and source footers. The comparison closes with the same inputs, output contract, and exactness gate on every path. The starting state preserves the stable definition: A conceptual five-input AND/INV cone whose output has four-variable semantic support, paired later with separately labeled retained EPFL evidence. After the change, the composition makes EPFL parity result inspectable: The timing interval settles across the parity line while preparation remains on a separate cost band.

The invariant is the example's meaning; the variable is EPFL parity result. If the same output is reached through a different path, the diagram must still identify whether EPFL parity result changed.

## c03 — Mechanism: instruction/operation equality mechanism; preparation penalty

Lower the binary AND/INV cone through CSE-flat and CM-IR, showing that no additional mergeable chain remains. Set instruction and executed-operation ratios to one before revealing the kernel parity interval and separate preparation result. Now isolate instruction/operation equality mechanism inside Small AND/INV cone. The example definition and output meaning stay fixed while only the state for instruction/operation equality mechanism changes.

That matched before-and-after view assigns the visible consequence to instruction/operation equality mechanism, not to a substituted workload. Matched lowered instruction streams align one for one and lock both mechanism counters at 1.000. Now isolate preparation penalty inside Small AND/INV cone. The example definition and output meaning stay fixed while only the state for preparation penalty changes.

That matched before-and-after view assigns the visible consequence to preparation penalty, not to a substituted workload. On the accepted EPFL AND/INV workload, CM/CSE-flat was 0.9998 with a circuit-clustered 95% interval [0.9747, 1.0249]. Read that statement only within this scope: EPFL AND/INV cones, semantic support 8–16, one Windows machine. Its declared measurement boundary is compiled evaluator kernel after compilation.

The uncertainty field says circuit-clustered bootstrap, 4000 draws. On the EPFL AND/INV workload, CM and CSE-flat had equal instruction and executed-operation counts, matching the parity mechanism prediction. Read that statement only within this scope: EPFL AND/INV cones. Its declared measurement boundary is compiled program structure and kernel.

The uncertainty field says exact count equality on accepted corpus. EPFL CM preparation was 4.11x CSE-flat preparation; 55 of 129 cases never broke even under the retained calculation. Read that statement only within this scope: accepted EPFL workload. Its declared measurement boundary is preparation and modeled reuse break-even.

The uncertainty field says descriptive retained calculation. A circuit cone's semantic support and gate structure determine the exact function under study; nominal circuit size is not a substitute for cone support. Read that statement only within this scope: accepted EPFL AND/INV cone workload. Its declared measurement boundary is workload definition.

The uncertainty field says corpus and support range scoped. The nearest confusing lesson is Reuse and break-even economics; it owns one-time versus repeated cost, while this episode owns EPFL parity result. AND and inversion nodes form a small exact dependency cone whose fanout remains visible throughout the lesson. Holding that element fixed lets this episode isolate instruction/operation equality mechanism.

The inert input stays outlined so nominal width cannot be mistaken for active support. Holding that element fixed lets this episode isolate preparation penalty. Structural counts describe nodes, edges, and operations; they do not directly measure hardware memory traffic. An exact output digest binds the conceptual cone to any separately labeled retained measurement panel.

Lowering or packing may change the execution artifact while the cone's admitted Boolean result remains fixed. Any mechanism claim points to a visible change in the cone rather than to a benchmark ratio by itself. After the change, the composition makes instruction/operation equality mechanism inspectable: Matched lowered instruction streams align one for one and lock both mechanism counters at 1.000. The invariant is the example's meaning; the variable is instruction/operation equality mechanism.

## c04 — Boundary, retrieval, and transfer

EPFL parity is scoped to these cones and does not contradict the distinct B2/B4 kernel result. Predict the instruction ratio before the matched streams and parity interval are revealed. No extra structural reduction meant no accepted kernel advantage on this workload. A common mistake is this: EPFL parity proves CM and CSE-flat are universally identical on every workload.

Repair it by returning to the owned distinction: EPFL parity result.

*[Three-second retrieval pause.]*

Use the episode's central distinction to answer: No extra structural reduction meant no accepted kernel advantage on this workload. When the mechanism predicts no structural difference, parity is an informative result.
