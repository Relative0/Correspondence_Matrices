WEBVTT

q0001
00:00:00.000 --> 00:00:06.297
If two formulas look different, are they necessarily different computations?

q0002
00:00:06.297 --> 00:00:17.092
This episode has one job: An expression is syntax, a truth table enumerates evaluations, and the Boolean function is the mapping they denote.

q0003
00:00:17.092 --> 00:00:27.887
We build on Why Boolean computation matters; their definitions stay fixed while this lesson adds a new layer.

q0004
00:00:27.887 --> 00:00:38.214
The expression is the written structure; the Boolean function is the output assigned to every input assignment.

q0005
00:00:38.214 --> 00:00:47.390
Watch for the label Boolean expression; it stays attached to the artifact this episode means.

q0006
00:00:47.390 --> 00:00:55.990
When we say truth table, the highlight identifies its layer before we interpret it.

q0007
00:00:55.990 --> 00:01:06.317
The term Boolean function receives its own visual state so it cannot drift into a neighboring concept.

q0008
00:01:06.317 --> 00:01:13.766
We will keep semantic equivalence visible whenever its definition controls the inference.

q0009
00:01:13.766 --> 00:01:24.093
The next sequence uses Four-variable truth layout as a conceptual teaching example, not as measured benchmark evidence.

q0010
00:01:24.093 --> 00:01:28.663
Our stable example is Four-variable truth layout.

q0011
00:01:28.663 --> 00:01:39.458
F(A,B,C,D)=(A AND B) XOR (C OR D), row variables A,B, column variables C,D, MSB-first ordering.

q0012
00:01:39.458 --> 00:01:48.058
Carry assignments, live support, explicit CM, packed truth vectors, and exact output across episodes.

q0013
00:01:48.058 --> 00:01:56.658
We now construct the Four-variable truth layout and name every part before interpreting it.

q0014
00:01:56.658 --> 00:02:01.804
Now isolate syntax-versus-semantics distinction inside Four-variable truth layout.

q0015
00:02:01.804 --> 00:02:11.556
The example definition and output meaning stay fixed while only the state for syntax-versus-semantics distinction changes.

q0016
00:02:11.556 --> 00:02:21.308
That matched before-and-after view assigns the visible consequence to syntax-versus-semantics distinction, not to a substituted workload.

q0017
00:02:21.308 --> 00:02:27.605
Expression tree and assignment row stay synchronized as gates evaluate.

q0018
00:02:27.605 --> 00:02:38.400
The example fixes F of A, B, C, and D as A AND B, exclusive-or C OR D.

q0019
00:02:38.400 --> 00:02:44.697
Holding that element fixed lets this episode isolate syntax-versus-semantics distinction.

q0020
00:02:44.697 --> 00:02:55.492
Every four-bit assignment names exactly one of the sixteen cells, and every cell returns one exact output bit.

q0021
00:02:55.492 --> 00:03:06.287
Reading a cell forward evaluates an assignment; reading it backward recovers the row and column bits that selected it.

q0022
00:03:06.287 --> 00:03:17.082
A packed truth vector stores the same exact outputs in a different physical arrangement from the displayed matrix.

q0023
00:03:17.082 --> 00:03:26.258
Holding variable order fixed makes matrix, packed-vector, and program views comparable without an indexing ambiguity.

q0024
00:03:26.258 --> 00:03:37.053
The starting state preserves the stable definition: F(A,B,C,D)=(A AND B) XOR (C OR D), row variables A,B, column variables C,D, MSB-first ordering.

q0025
00:03:37.053 --> 00:03:47.848
After the change, the composition makes syntax-versus-semantics distinction inspectable: Expression tree and assignment row stay synchronized as gates evaluate.

q0026
00:03:47.848 --> 00:03:54.721
The invariant is the example's meaning; the variable is syntax-versus-semantics distinction.

q0027
00:03:54.721 --> 00:04:05.516
If the same output is reached through a different path, the diagram must still identify whether syntax-versus-semantics distinction changed.

q0028
00:04:05.516 --> 00:04:15.843
The accompanying view makes this step concrete: Expression tree and assignment row stay synchronized as gates evaluate.

q0029
00:04:15.843 --> 00:04:26.638
Read syntax-versus-semantics distinction from input to consequence through one matched view: Expression tree and assignment row stay synchronized as gates evaluate; every surrounding entity keeps its prior meaning.

q0030
00:04:26.638 --> 00:04:37.433
The diagram treats syntax-versus-semantics distinction as a located state, artifact, or boundary rather than as an unexplained method label.

q0031
00:04:37.433 --> 00:04:48.228
Before the consequence appears, predict what syntax-versus-semantics distinction can change and what the stable example requires it to leave untouched.

q0032
00:04:48.228 --> 00:04:59.023
When evidence enters, its status badge binds the statement about syntax-versus-semantics distinction to a source and scope, not to the conceptual drawing alone.

q0033
00:04:59.023 --> 00:05:09.818
The nearest neighboring lesson stays out of focus because its owned question is not needed to explain syntax-versus-semantics distinction in this sequence.

q0034
00:05:09.818 --> 00:05:20.613
The transfer rule carries syntax-versus-semantics distinction forward only with the same input definition, exactness gate, and declared output contract.

q0035
00:05:20.613 --> 00:05:30.365
Watch us build an expression tree and evaluate it row by row into a truth column.

q0036
00:05:30.365 --> 00:05:38.965
Watch us rewrite the syntax without changing the truth column, making semantic equivalence visible.

q0037
00:05:38.965 --> 00:05:45.838
Now isolate equivalent expressions with one function inside Four-variable truth layout.

q0038
00:05:45.838 --> 00:05:56.633
The example definition and output meaning stay fixed while only the state for equivalent expressions with one function changes.

q0039
00:05:56.633 --> 00:06:07.428
That matched before-and-after view assigns the visible consequence to equivalent expressions with one function, not to a substituted workload.

q0040
00:06:07.428 --> 00:06:16.604
A second differently shaped tree grows beside the first and produces the same truth column.

q0041
00:06:16.604 --> 00:06:25.204
A Boolean expression maps each complete assignment of its variables to one Boolean output.

q0042
00:06:25.204 --> 00:06:32.077
Read that statement only within this scope: current Boolean expression implementation.

q0043
00:06:32.077 --> 00:06:36.071
Its declared measurement boundary is semantics.

q0044
00:06:36.071 --> 00:06:39.489
The uncertainty field says none.

q0045
00:06:39.489 --> 00:06:50.284
An expression is syntax; its Boolean function is the assignment-to-output mapping it denotes, so different expressions may denote the same function.

q0046
00:06:50.284 --> 00:06:57.733
Read that statement only within this scope: Boolean expression and exact-output contracts.

q0047
00:06:57.733 --> 00:07:08.528
The nearest confusing lesson is Live support versus ambient variables; it owns live support, while this episode owns syntax-versus-semantics distinction.

q0048
00:07:08.528 --> 00:07:19.323
A and B select the row, C and D select the column, and MSB-first order fixes both index conventions.

q0049
00:07:19.323 --> 00:07:27.347
Holding that element fixed lets this episode isolate equivalent expressions with one function.

q0050
00:07:27.347 --> 00:07:36.523
Folding the truth-table output column into a four-by-four grid changes the layout, not the function.

q0051
00:07:36.523 --> 00:07:47.318
Live support can remove an input from active computation even when the ambient layout still reserves its axis.

q0052
00:07:47.318 --> 00:07:58.113
The explicit matrix is dense by contract, so compactness and speed require separate evidence rather than definition alone.

q0053
00:07:58.113 --> 00:08:08.908
An exact equality check across the views is the gate before any timing ratio is allowed to matter.

q0054
00:08:08.908 --> 00:08:19.703
After the change, the composition makes equivalent expressions with one function inspectable: A second differently shaped tree grows beside the first and produces the same truth column.

q0055
00:08:19.703 --> 00:08:28.303
The invariant is the example's meaning; the variable is equivalent expressions with one function.

q0056
00:08:28.303 --> 00:08:39.098
If the same output is reached through a different path, the diagram must still identify whether equivalent expressions with one function changed.

q0057
00:08:39.098 --> 00:08:49.425
The accompanying view makes this step concrete: Both trees recede while the shared assignment-to-output mapping remains centered.

q0058
00:08:49.425 --> 00:09:00.220
The accompanying view makes this step concrete: A second differently shaped tree grows beside the first and produces the same truth column.

q0059
00:09:00.220 --> 00:09:09.396
Matching one or two examples is not equivalence; the declared assignment universe must match exactly.

q0060
00:09:09.396 --> 00:09:19.723
Choose which of two rewritten expressions denotes the same function before the last truth rows are revealed.

q0061
00:09:19.723 --> 00:09:24.869
Syntax can change while the function remains identical.

q0062
00:09:24.869 --> 00:09:34.045
A common mistake is this: A changed expression tree necessarily means a changed Boolean function.

q0063
00:09:34.045 --> 00:09:40.342
Repair it by returning to the owned distinction: syntax-versus-semantics distinction.

q0065
00:09:43.940 --> 00:09:53.116
Use the episode's central distinction to answer: Syntax can change while the function remains identical.

q0066
00:09:53.116 --> 00:09:59.989
Representations may change the structure, but exact semantics must stay fixed.
