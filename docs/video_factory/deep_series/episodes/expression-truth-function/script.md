# Expression, truth table, and Boolean function

Video ID: `expression-truth-function`  
Episode content identity: `9a797f53345205996f6a1499b06bcc0c4c3c79c257160158f7bf2352c190f756`  
Status: **complete narration draft; production candidate pending content approval**

## c01 — Question and definition: syntax-versus-semantics distinction

If two formulas look different, are they necessarily different computations? This episode has one job: An expression is syntax, a truth table enumerates evaluations, and the Boolean function is the mapping they denote. We build on Why Boolean computation matters; their definitions stay fixed while this lesson adds a new layer. The expression is the written structure; the Boolean function is the output assigned to every input assignment.

Watch for the label Boolean expression; it stays attached to the artifact this episode means. When we say truth table, the highlight identifies its layer before we interpret it. The term Boolean function receives its own visual state so it cannot drift into a neighboring concept. We will keep semantic equivalence visible whenever its definition controls the inference.

## c02 — Worked example: Four-variable truth layout

The next sequence uses Four-variable truth layout as a conceptual teaching example, not as measured benchmark evidence. Our stable example is Four-variable truth layout. F(A,B,C,D)=(A AND B) XOR (C OR D), row variables A,B, column variables C,D, MSB-first ordering. Carry assignments, live support, explicit CM, packed truth vectors, and exact output across episodes.

We now construct the Four-variable truth layout and name every part before interpreting it. Now isolate syntax-versus-semantics distinction inside Four-variable truth layout. The example definition and output meaning stay fixed while only the state for syntax-versus-semantics distinction changes. That matched before-and-after view assigns the visible consequence to syntax-versus-semantics distinction, not to a substituted workload.

Expression tree and assignment row stay synchronized as gates evaluate. The example fixes F of A, B, C, and D as A AND B, exclusive-or C OR D. Holding that element fixed lets this episode isolate syntax-versus-semantics distinction. Every four-bit assignment names exactly one of the sixteen cells, and every cell returns one exact output bit.

Reading a cell forward evaluates an assignment; reading it backward recovers the row and column bits that selected it. A packed truth vector stores the same exact outputs in a different physical arrangement from the displayed matrix. Holding variable order fixed makes matrix, packed-vector, and program views comparable without an indexing ambiguity. The starting state preserves the stable definition: F(A,B,C,D)=(A AND B) XOR (C OR D), row variables A,B, column variables C,D, MSB-first ordering.

After the change, the composition makes syntax-versus-semantics distinction inspectable: Expression tree and assignment row stay synchronized as gates evaluate. The invariant is the example's meaning; the variable is syntax-versus-semantics distinction. If the same output is reached through a different path, the diagram must still identify whether syntax-versus-semantics distinction changed. The accompanying view makes this step concrete: Expression tree and assignment row stay synchronized as gates evaluate.

Read syntax-versus-semantics distinction from input to consequence through one matched view: Expression tree and assignment row stay synchronized as gates evaluate; every surrounding entity keeps its prior meaning. The diagram treats syntax-versus-semantics distinction as a located state, artifact, or boundary rather than as an unexplained method label. Before the consequence appears, predict what syntax-versus-semantics distinction can change and what the stable example requires it to leave untouched. When evidence enters, its status badge binds the statement about syntax-versus-semantics distinction to a source and scope, not to the conceptual drawing alone.

The nearest neighboring lesson stays out of focus because its owned question is not needed to explain syntax-versus-semantics distinction in this sequence. The transfer rule carries syntax-versus-semantics distinction forward only with the same input definition, exactness gate, and declared output contract.

## c03 — Mechanism: equivalent expressions with one function

Watch us build an expression tree and evaluate it row by row into a truth column. Watch us rewrite the syntax without changing the truth column, making semantic equivalence visible. Now isolate equivalent expressions with one function inside Four-variable truth layout. The example definition and output meaning stay fixed while only the state for equivalent expressions with one function changes.

That matched before-and-after view assigns the visible consequence to equivalent expressions with one function, not to a substituted workload. A second differently shaped tree grows beside the first and produces the same truth column. A Boolean expression maps each complete assignment of its variables to one Boolean output. Read that statement only within this scope: current Boolean expression implementation.

Its declared measurement boundary is semantics. The uncertainty field says none. An expression is syntax; its Boolean function is the assignment-to-output mapping it denotes, so different expressions may denote the same function. Read that statement only within this scope: Boolean expression and exact-output contracts.

The nearest confusing lesson is Live support versus ambient variables; it owns live support, while this episode owns syntax-versus-semantics distinction. A and B select the row, C and D select the column, and MSB-first order fixes both index conventions. Holding that element fixed lets this episode isolate equivalent expressions with one function. Folding the truth-table output column into a four-by-four grid changes the layout, not the function.

Live support can remove an input from active computation even when the ambient layout still reserves its axis. The explicit matrix is dense by contract, so compactness and speed require separate evidence rather than definition alone. An exact equality check across the views is the gate before any timing ratio is allowed to matter. After the change, the composition makes equivalent expressions with one function inspectable: A second differently shaped tree grows beside the first and produces the same truth column.

The invariant is the example's meaning; the variable is equivalent expressions with one function. If the same output is reached through a different path, the diagram must still identify whether equivalent expressions with one function changed. The accompanying view makes this step concrete: Both trees recede while the shared assignment-to-output mapping remains centered. The accompanying view makes this step concrete: A second differently shaped tree grows beside the first and produces the same truth column.

## c04 — Boundary, retrieval, and transfer

Matching one or two examples is not equivalence; the declared assignment universe must match exactly. Choose which of two rewritten expressions denotes the same function before the last truth rows are revealed. Syntax can change while the function remains identical. A common mistake is this: A changed expression tree necessarily means a changed Boolean function.

Repair it by returning to the owned distinction: syntax-versus-semantics distinction.

*[Three-second retrieval pause.]*

Use the episode's central distinction to answer: Syntax can change while the function remains identical. Representations may change the structure, but exact semantics must stay fixed.
