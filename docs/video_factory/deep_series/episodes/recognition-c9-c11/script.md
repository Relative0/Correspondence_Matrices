# C9-C11 exact routing: static trees, guarded restart, and one-pass conversion

Video ID: `recognition-c9-c11`  
Episode content identity: `0778fc8c78742de8f6db664d09e507a1914179e60f3f6cf583ed4cea2df6681d`  
Status: **complete narration draft; production candidate pending content approval**

## c01 — Why exact routing can still lose

Can an exact router solve a catastrophic tail and still be the wrong default? This episode has one job: C9-C11 retained three exact routing designs that protected tails or removed duplicate prefix work, yet each remained slower than the best fixed exact arm on its declared evaluation splits. We build on C6-C8 exact source ANF: packed cores and transfer; their definitions stay fixed while this lesson adds a new layer. C9 chooses from a frozen static tree, C10 restarts after a product budget, and C11 converts the existing set prefix in one pass.

Watch for the label static analytic router; it stays attached to the artifact this episode means. When we say product-budget restart, the highlight identifies its layer before we interpret it. The term one-pass conversion receives its own visual state so it cannot drift into a neighboring concept. We will keep best fixed exact arm visible whenever its definition controls the inference.

## c02 — Stable source example and fixed exact arms

The next sequence uses Conceptual recognition/decomposition graph as a conceptual teaching example, not as measured benchmark evidence. Our stable example is Conceptual recognition/decomposition graph. A tiny labeled expression graph with a proposed partition, an exact witness path, a rejection/fallback path, and a promotion gate; it is never presented as a measured trace. Keep proposal, exact verification, fallback, and promotion visually stable across CRSE episodes.

We now construct the Conceptual recognition/decomposition graph and name every part before interpreting it.

## c03 — C9 static tree, C10 guarded restart, and C11 one-pass conversion

Freeze the C9 source-only timing tree before held-out evaluation and show its exact route losing after transfer. Stop C10 set ANF at a frozen product budget, restart with packed ANF, and separate p95 protection from sparse-case overhead. Convert C11's set prefix in place to packed coefficients so no DAG prefix repeats, then compare its full cost with restart and the best fixed arm. Now isolate C9 static analytic routing inside Conceptual recognition/decomposition graph.

The example definition and output meaning stay fixed while only the state for C9 static analytic routing changes. That matched before-and-after view assigns the visible consequence to C9 static analytic routing, not to a substituted workload. The C9 tree chooses an exact arm, then its held-out cost token lands below the profitability gate. Now isolate C10 guarded exact restart inside Conceptual recognition/decomposition graph.

The example definition and output meaning stay fixed while only the state for C10 guarded exact restart changes. That matched before-and-after view assigns the visible consequence to C10 guarded exact restart, not to a substituted workload. C10's product counter triggers a guarded restart while median and p95 gauges move independently. Now isolate C11 one-pass set-to-packed conversion inside Conceptual recognition/decomposition graph.

The example definition and output meaning stay fixed while only the state for C11 one-pass set-to-packed conversion changes. That matched before-and-after view assigns the visible consequence to C11 one-pass set-to-packed conversion, not to a substituted workload. C11 transforms one live set prefix into packed bits without a second DAG traversal, but its complete-cost bar still misses one. C9 static routing, C10 guarded restart, and C11 one-pass conversion preserved exact labels and partitions but remained slower than the best fixed arm on their retained evaluation splits; C10 nevertheless protected the catastrophic p95 tail.

Read that statement only within this scope: Milestones C9-C11 exact representation-routing progression. Its declared measurement boundary is exact recognition and overhead-inclusive routing. The uncertainty field says bounded C6/C7 and fresh Yosys-derived splits. The current CRSE program spans learned proposals, exact subclass recognition, task routing, proved rules, representation dispatch, BDD/SAT guidance, exact verification, negative controls, and explicit promotion decisions.

Read that statement only within this scope: current recognition register. Its declared measurement boundary is research-program structure. The uncertainty field says track completion varies. The nearest confusing lesson is C12-C16 exact dispatch, tail guards, and GF(2) artifacts; it owns C12 exact adaptive dispatch, while this episode owns C9 static analytic routing.

A labeled input graph enters a proposal stage whose job is to suggest a partition or route, not to certify it. Holding that element fixed lets this episode isolate C9 static analytic routing. The candidate partition remains visibly provisional until the exact verifier checks its witness conditions. Holding that element fixed lets this episode isolate C10 guarded exact restart.

An accepted path produces an exact witness; a rejected path moves to the safe fallback without changing correctness. Holding that element fixed lets this episode isolate C11 one-pass set-to-packed conversion. The promotion gate is separate from exact verification because a correct result need not satisfy a performance promotion rule. Proposal, verifier, witness, rejection, fallback, and promotion keep stable shapes across every CRSE episode.

A conceptual badge stays on the graph even when a later panel introduces separately sourced measurements. Changing the proposal mechanism never removes the exact verifier or the fallback path from the contract. Negative and not-promoted outcomes remain first-class states rather than disappearing from the research timeline. Task routing may select a different exact path while the admitted result and verification gate remain fixed.

Every measured promotion panel names its workload, boundary, uncertainty, and source independently of this teaching graph. The starting state preserves the stable definition: A tiny labeled expression graph with a proposed partition, an exact witness path, a rejection/fallback path, and a promotion gate; it is never presented as a measured trace. After the change, the composition makes C9 static analytic routing inspectable: The C9 tree chooses an exact arm, then its held-out cost token lands below the profitability gate. The invariant is the example's meaning; the variable is C9 static analytic routing.

If the same output is reached through a different path, the diagram must still identify whether C9 static analytic routing changed. After the change, the composition makes C10 guarded exact restart inspectable: C10's product counter triggers a guarded restart while median and p95 gauges move independently.

## c04 — Tail protection, overhead, and the bridge to C12

All three preserved exact outputs; none beat the best fixed arm on every retained split, so tail protection and median profitability remain separate. Match static transfer failure, restart tail protection, and one-pass prefix reuse to C9, C10, or C11. C9-C11 explain why C12 needed a robust guarded policy rather than a more confident universal router. A common mistake is this: Eliminating duplicate prefix work or improving p95 automatically makes an exact router profitable on sparse cases.

Repair it by returning to the owned distinction: C9 static analytic routing.

*[Three-second retrieval pause.]*

Use the episode's central distinction to answer: C9-C11 explain why C12 needed a robust guarded policy rather than a more confident universal router. An exact routing mechanism advances only when its charged whole path clears the declared gate.
