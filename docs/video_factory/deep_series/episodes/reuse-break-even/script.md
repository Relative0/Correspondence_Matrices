# Reuse and break-even economics

Video ID: `reuse-break-even`  
Episode content identity: `b5b1beb88c7be6e90bd1afdad3dc240e92ca0dc11fb1ee0a16d203cb2d7611b7`  
Status: **complete narration draft; production candidate pending content approval**

## c01 — Question and definition: one-time versus repeated cost

How many evaluations does it take to repay a more expensive compilation? This episode has one job: Extra preparation is repaid only when a per-evaluation advantage accumulates across enough genuinely reusable requests; some cases never break even. We build on Preparation, kernel, wrapper, and end-to-end time, How to read a CM/comparator ratio; their definitions stay fixed while this lesson adds a new layer. Break-even reuse is the request count where accumulated preparation plus execution costs cross under a declared model.

Watch for the label one-time preparation; it stays attached to the artifact this episode means. When we say per-evaluation cost, the highlight identifies its layer before we interpret it. The term break-even reuse receives its own visual state so it cannot drift into a neighboring concept. We will keep never break even visible whenever its definition controls the inference.

## c02 — Worked example: Small AND/INV cone

The next sequence uses Small AND/INV cone as a conceptual teaching example, not as measured benchmark evidence. Our stable example is Small AND/INV cone. A conceptual five-input AND/INV cone whose output has four-variable semantic support, paired later with separately labeled retained EPFL evidence. Teach cone support, fanout, lowering, exact digests, and the accepted EPFL mechanism.

We now construct the Small AND/INV cone and name every part before interpreting it. Now isolate one-time versus repeated cost inside Small AND/INV cone. The example definition and output meaning stay fixed while only the state for one-time versus repeated cost changes. That matched before-and-after view assigns the visible consequence to one-time versus repeated cost, not to a substituted workload.

Two cumulative-cost lines grow from different preparation intercepts. The cone starts from five named inputs, but the displayed output has four-variable semantic support. Holding that element fixed lets this episode isolate one-time versus repeated cost. A topological trace orders the gates without changing their connectivity or the output function.

The conceptual mechanism and EPFL evidence use matched colors but retain distinct status badges and source footers. The comparison closes with the same inputs, output contract, and exactness gate on every path. The starting state preserves the stable definition: A conceptual five-input AND/INV cone whose output has four-variable semantic support, paired later with separately labeled retained EPFL evidence. After the change, the composition makes one-time versus repeated cost inspectable: Two cumulative-cost lines grow from different preparation intercepts.

The invariant is the example's meaning; the variable is one-time versus repeated cost. If the same output is reached through a different path, the diagram must still identify whether one-time versus repeated cost changed. The accompanying view makes this step concrete: Two cumulative-cost lines grow from different preparation intercepts. Read one-time versus repeated cost from input to consequence through one matched view: Two cumulative-cost lines grow from different preparation intercepts; every surrounding entity keeps its prior meaning.

The diagram treats one-time versus repeated cost as a located state, artifact, or boundary rather than as an unexplained method label. Before the consequence appears, predict what one-time versus repeated cost can change and what the stable example requires it to leave untouched.

## c03 — Mechanism: finite break-even; never-break-even case

Watch us build cumulative cost lines from preparation intercepts and per-use slopes under a toy model. Replace the toy values with the scoped EPFL preparation and break-even summary, preserving never cases separately. Now isolate finite break-even inside Small AND/INV cone. The example definition and output meaning stay fixed while only the state for finite break-even changes.

That matched before-and-after view assigns the visible consequence to finite break-even, not to a substituted workload. The retained EPFL distribution separates finite break-even cases from 55 never cases. Now isolate never-break-even case inside Small AND/INV cone. The example definition and output meaning stay fixed while only the state for never-break-even case changes.

That matched before-and-after view assigns the visible consequence to never-break-even case, not to a substituted workload. EPFL CM preparation was 4.11x CSE-flat preparation; 55 of 129 cases never broke even under the retained calculation. Read that statement only within this scope: accepted EPFL workload. Its declared measurement boundary is preparation and modeled reuse break-even.

The uncertainty field says descriptive retained calculation. The nearest confusing lesson is Why scopes and boundaries matter; it owns scope comparison, while this episode owns one-time versus repeated cost. AND and inversion nodes form a small exact dependency cone whose fanout remains visible throughout the lesson. Holding that element fixed lets this episode isolate finite break-even.

The inert input stays outlined so nominal width cannot be mistaken for active support. Holding that element fixed lets this episode isolate never-break-even case. Structural counts describe nodes, edges, and operations; they do not directly measure hardware memory traffic. An exact output digest binds the conceptual cone to any separately labeled retained measurement panel.

Lowering or packing may change the execution artifact while the cone's admitted Boolean result remains fixed. Any mechanism claim points to a visible change in the cone rather than to a benchmark ratio by itself. After the change, the composition makes finite break-even inspectable: The retained EPFL distribution separates finite break-even cases from 55 never cases. The invariant is the example's meaning; the variable is finite break-even.

If the same output is reached through a different path, the diagram must still identify whether finite break-even changed. After the change, the composition makes never-break-even case inspectable: The retained EPFL distribution separates finite break-even cases from 55 never cases. The invariant is the example's meaning; the variable is never-break-even case. If the same output is reached through a different path, the diagram must still identify whether never-break-even case changed.

The accompanying view makes this step concrete: The retained EPFL distribution separates finite break-even cases from 55 never cases. The accompanying view makes this step concrete: A reuse slider moves the crossing point or reveals that no crossing exists.

## c04 — Boundary, retrieval, and transfer

Some retained cases never break even, and a modeled crossing is not an end-to-end deployment guarantee. Predict whether three pairs of intercepts and slopes cross, cross late, or never cross. Preparation is a debt; reuse determines whether the debt is ever repaid. A common mistake is this: Any per-evaluation advantage guarantees a finite and useful break-even point.

Repair it by returning to the owned distinction: one-time versus repeated cost.

*[Three-second retrieval pause.]*

Use the episode's central distinction to answer: Preparation is a debt; reuse determines whether the debt is ever repaid. A kernel advantage matters only when reuse can repay the work that created it.
