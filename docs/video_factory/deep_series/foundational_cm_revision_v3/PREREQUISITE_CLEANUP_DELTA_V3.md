# Prerequisite cleanup delta — foundational CM v3

Status: proposed, not applied  
Purpose: remove premature CM instruction and repetitive factory prose from
episodes 2–4 when their next versioned revisions are authored

## `why-boolean-computation`

Keep:

- the Boolean decision rule;
- complete assignments;
- one exact output per assignment;
- the stable assignment-to-output mapping;
- the retrieval check involving one changed input.

Move or cut from the current script:

- line 15: row variables, column variables, explicit CM, and packed-vector
  preview;
- lines 25–29: row/column selection, sixteen matrix cells, truth-table folding,
  reverse cell reading, live-support mechanism, packed truth vector, and dense
  matrix boundary.

Replacement bridge:

> “Once the complete assignment-to-output mapping is fixed, later lessons can
> choose how to display or evaluate it.”

Do not display a labeled CM. Let the truth mapping fan toward dimmed, unnamed
future representation silhouettes.

## `expression-truth-function`

Keep:

- expression syntax;
- row-by-row truth-table evaluation;
- two different expressions denoting the same Boolean function;
- the complete-universe equivalence gate.

Move or cut from the current script:

- line 15: ordered R/C split and the list of later artifacts;
- lines 19–21: sixteen matrix cells, forward/reverse cell reading, packed truth
  vector, and matrix/program comparison;
- lines 37–39: row/column indexing, 4×4 folding, live-support mechanism, dense
  matrix, and compactness/speed boundary.

Replacement bridge:

> “The expression may change while the complete assignment-to-output mapping
> remains the same. Representation comes next.”

## `live-support-ambient`

Keep:

- semantic live support;
- ambient variable universe;
- fixed-variable reduction;
- paired rows showing independence from one variable;
- the boundary that smaller support does not select a fastest engine.

Move or cut from the current script:

- line 15: explicit CM and packed-vector continuity list;
- line 27: AB/CD row and column indexing;
- lines 29–31: sixteen CM cells, truth-table folding, forward/reverse cell
  reading, packed truth vectors, and dense-matrix claims.

Use paired truth-table rows and dependency wires instead of a CM whose paper and
repository definitions have not yet been taught.

Replacement bridge:

> “Live support tells us which variables can affect the function. It does not
> yet decide how a later representation arranges the full assignment space.”

## Shared anti-repetition edit

Remove learner-facing instances of:

- “this episode has one job”;
- “watch for the label”;
- “now isolate”;
- “holding that element fixed lets this episode”;
- “matched before-and-after view”;
- spoken source-scope, measurement-boundary, or uncertainty-field metadata.

Keep such metadata in claim maps, captions metadata, and review artifacts rather
than narration.

## Application boundary

This delta does not overwrite the released first-five masters. Revised scripts,
captions, and media require new versioned paths and a separate content review.

