# Count and historical evidence closure

Base: published commit 643b5b6c9614ee1d1daed4eb374a431a67514587.
Work in build/cm-closures; preserve all prior seals and the dirty shared parent.

1. Inspect a separately implemented projected model counter and pin its source,
   binary, invocation and semantics. Run exhaustive controls before the sixteen
   fixed QIF3/decisionmaking contexts. Independently check the eight completed
   Ganak-only values and attempt the eight remaining contexts under new deadlines.
   Count agreement is not a certified proof or a timing comparison.
2. Implement and prospectively freeze a different bounded representation: recursive
   conditioning with residual-component decomposition and exact memoization.
   Count only selected branches and use existential search on hidden-only residuals.
   Preserve free selected variables, impose explicit limits and exhaustive controls,
   and retain all outcomes on the unchanged nine feature and six independent cases.
3. Inspect every remaining historical failure. Recover source/platform closures or
   original receipt bytes only against existing independent hashes. Do not rewrite
   expected hashes, infer natural consumer use, or rerender videos for research.
   Investigate the EPFL source-origin discrepancy against immutable upstream history.
4. Run meaningful focused and broad checks on RunPod, seal new evidence, update the
   existing GitHub Pages site with dated conclusions, and verify its exact live files.

The existing $10 authorization persists. Earlier nonrefunded reservations total
$7.75; this audit reserves at most $2.25, with $0.25 per attempt and no reservation
refunds. Actual conservative prior lifetime/rate estimate is $0.592875834852457.
Use ownership-bound cleanup and the existing one-hour watchdog. Reserve room for
validation and retries. All substantial computation runs on RunPod.
