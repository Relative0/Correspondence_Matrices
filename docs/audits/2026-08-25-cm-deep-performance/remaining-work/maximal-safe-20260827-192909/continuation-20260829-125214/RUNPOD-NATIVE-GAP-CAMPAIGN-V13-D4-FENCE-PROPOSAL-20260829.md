# Native-gap campaign V13 d4 child-fence proposal

Date: 2026-08-29  
Status: authorized under the aggregate `$10` campaign instruction

V12 proved native CUDD BDD execution, fixed and group-sift exactness, native
JSON dump/reload, and whole-tree RSS. It identified the native CUDD ZDD module.
The next d4 `ldd` process was too short-lived for the strict procfs sampler, so
d4 functional cases did not run. Pod `omsz7w8fmlhqn0` was deleted; both
inventories were empty. Estimated compute was `$0.0015198645`.

V13 runs `ldd`, d4 help and each d4 count as a child of a bounded Python
measurement-fence wrapper that remains alive for 100 ms after the native child
returns while relaying its exact stdout, stderr and exit status. The native
child remains in the owned process group. This is readiness only and cannot be
used for performance ranking.

The exact V9 upload manifest has 37 files and 5,508,763 bytes. Focused tests
remain 65; P5 remains 144 cells. Resource, dependency, transport, deadline,
zero-volume, cleanup, `$0.10` phase and `$10` aggregate caps are unchanged.
This controller performs exactly one create request.
