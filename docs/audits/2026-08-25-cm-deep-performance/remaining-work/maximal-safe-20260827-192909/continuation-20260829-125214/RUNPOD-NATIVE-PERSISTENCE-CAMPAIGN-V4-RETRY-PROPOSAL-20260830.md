# RunPod native persistence campaign V4 retry proposal

This retry preserves the exact V3 workload, 136-file/5,494,221-byte external upload, system packages, identities, cost limits, exactness gates, and cleanup controls.

V3 pod `cgazict5szhbg5` refused the public PaToH header before writing or compiling it because the reviewed local Windows checkout had CRLF line endings. The canonical GitHub blob at the same exact commit instead contains 11,944 bytes with SHA-256 `a403cadf44785c773cc77895294f6674b7e2c7006a29bd41b401a81e8aff443c`. An independent in-memory HTTPS fetch established that identity. V3 retained partial evidence, left uploaded source unchanged, deleted the pod with HTTP 204, and reconciled both inventories as empty. Estimated compute cost was $0.0013231476306915283.

V4 changes only the required size and SHA-256 for that public upstream download. No project file or additional internal data is exported. This separately frozen controller may create exactly one replacement under the user's explicit external-upload approval, automatic-retry instruction, and aggregate $10 cap. All acceptance and mandatory-cleanup requirements remain unchanged.

