# Native-gap campaign V18 d4v2 progress-line proposal

Date: 2026-08-29  
Status: authorized under the aggregate `$10` campaign instruction

V17 ran the static d4 binary successfully with the corrected positional-input
command, then the strict parser refused one undocumented non-comment progress
line of the form `time: N/N  number: N/N`. Pod `gj1k7p3gjywald` was
deleted and both inventories were empty. Estimated compute was
`$0.0020546862`.

V18 admits only that bounded numeric progress-line grammar in addition to the
already strict competition records. Arbitrary non-comment text remains
refused. Parse failures now preserve bounded stdout/stderr excerpts and exact
hashes, so another dialect mismatch will be diagnosable without inference.

The exact V13 upload manifest has 37 files and 5,518,172 bytes. Focused tests
remain 68; P5 remains 144 cells. This is readiness only, not performance
evidence. All prior resource, dependency, transport, deadline, zero-volume,
cleanup, `$0.10` phase and `$10` aggregate caps are unchanged. This
controller performs exactly one create request.
