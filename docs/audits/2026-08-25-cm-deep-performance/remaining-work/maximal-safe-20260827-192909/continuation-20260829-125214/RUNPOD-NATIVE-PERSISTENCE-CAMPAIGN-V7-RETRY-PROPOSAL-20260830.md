# RunPod native persistence campaign V7 unchanged retry proposal

V7 is an unchanged rerun of the frozen V6 remote program, exact 136-file/5,494,221-byte upload, dependencies, 22-test gate, 16-cell native workload, resources, cost limits, exactness requirements, and cleanup controls.

V6 pod `7ucx4lcpvaj8j6` compiled d4, passed all 22 applicable focused tests, and completed 13 cells with exact fresh-process CUDD ZDD or d4 d-DNNF build/reload/independent-replay agreement. The 14th cell was refused by the Linux supervisor when a live process-group entry exposed no `VmRSS` throughout the bounded 20 ms appearance window. This is the intended fail-closed response to an incomplete procfs measurement; it did not report a semantic mismatch. V6 retained partial evidence, left all uploaded source unchanged, deleted the pod with HTTP 204, and reconciled both inventories empty. Estimated compute cost was $0.002760778180758158.

Because the same d4 cells previously completed with whole-tree RSS measurement and the failure is a bounded sampling race, one unchanged retry is appropriate before altering the reviewed supervisor contract. This separately frozen controller may create exactly one replacement under the user's explicit upload approval, automatic-retry instruction, and aggregate $10 cap.

