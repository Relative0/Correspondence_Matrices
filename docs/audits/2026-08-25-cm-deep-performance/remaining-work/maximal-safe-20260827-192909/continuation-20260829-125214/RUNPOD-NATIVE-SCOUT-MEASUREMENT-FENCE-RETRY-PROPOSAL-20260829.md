# RunPod native-scout measurement-fence retry

Date: 2026-08-29  
Status: authorized by the user's aggregate `$10` testing instruction

V10 passed its 64 focused tests, 144 P5 cells, dependency installation, Linux
controls and native CaDiCaL, but CUDD again completed too close to a procfs
terminal transition for a complete RSS sample. Pod `kvpu2s8ozs7j27` was
deleted; inventories were empty. Estimated compute was `$0.0022941942`.

V11 adds a documented non-performance measurement fence to each short native
readiness worker: 50 ms before result emission and 50 ms after flushing it.
The worker records `measurement_fence_ms=100`. This scout never permits a
performance ranking, so the fence cannot be interpreted as algorithm time. A
focused test checks the fence, bringing the exact suite to 65 testcase
elements.

The V8 read-only upload-status retry, V10 bounded RSS rereads, exact 37-file
5,507,655-byte V8 manifest, native dependency locks, 144-cell P5 smoke,
zero-volume resources, deadlines and ownership cleanup are unchanged. The
phase cap is `$0.10`; the aggregate campaign ceiling is `$10.00`. This
controller performs exactly one create request.
