# Externally owned native-gap V13 result audit

Date: 2026-08-29  
Status: safely reconciled regression/partial readiness result; no d4 result

## Scope and authority

V13 was launched by a separate workspace task under a record citing a newer
cross-task `$10` continuation instruction. This task cannot inspect that source
conversation and therefore neither adopts nor rejects the authority claim. It
made no V13 cloud write and queued no replacement.

## Verified result

Pod `4ovdjr0todatgl` matched the Secure `cpu3c` two-CPU, at-least-4-GB,
12-GB-container, integer-zero-volume, no-network-volume contract. Eleven
bounded chunks uploaded the exact frozen 37-file package and the remote source
identities remained unchanged.

The 65 JUnit testcase elements (185 suite-reported tests) passed with no
failures, errors, or skips. All 144 P5 cells, the read-only P5 verifier, locked
dependency closure, and four Linux control probes passed. The native scout then
failed at the first SAT/CaDiCaL worker with
`process_tree_measurement_incomplete`. It produced no CaDiCaL, CUDD, d4, perf,
or accepted timing result.

V13 therefore did not test its intended d4 child fence and regressed relative
to V12's accepted CaDiCaL/CUDD readiness evidence. V12 remains the stronger
technical record.

Evidence archive: 136,828 bytes, 61 members, SHA-256
`bc472b3cb272975a6987014cc013a5bfbe6bc1ea0ce2ef1abbb54cc51cc0e43e`.

## Cleanup and cost

The controller deleted the owned pod with HTTP 204. At 16:22:06 UTC, both API
inventories were empty and all 14 known pod detail requests returned 404. The
watchdog reported `controller_cleanup_verified` and both host guards exited.

Billing had not yet published a V13 row. The rate/time reserve gives an attempt
bound of `$0.0018082656`; the corrected conservative attributable campaign
bound is `$0.0147740342`, within the stricter `$0.10` phase and `$0.20` campaign
ceilings applied by this verifier. Billing may lag.

Final read-only reconciliation:

`EXTERNAL-NATIVE-GAP-V13-FINAL-VERIFICATION-20260829-162224-598946.json`

SHA-256:

`ee6ba004a56b5fd7848274f19fb177a4541896d9ef87598080787ae2ca110d2f`

## Interpretation

V13 adds no comparative benchmark and does not close d4 readiness. A later
proposal must preserve V12's successful CaDiCaL/CUDD path while testing any d4
measurement correction. This audit grants no create or replacement.
