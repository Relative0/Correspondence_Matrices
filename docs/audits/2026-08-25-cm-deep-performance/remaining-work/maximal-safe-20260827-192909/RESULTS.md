# CM maximal safe campaign — results

> **2026-08-28 current result:** The approved zero-volume Runpod smoke passed: 70 focused tests, 312 successful/exact window rows and 72 recorded representation calls. All 13 locked package versions and retrieved evidence were verified; the pod was deleted and both API inventories independently confirmed empty. See [the smoke result](runpod-authorized-20260827-213104/HTTP-EPHEMERAL-RESULT-20260828.md) and [continuation](RUNPOD-CONTINUATION-20260828.md). No default/model coefficient changed. Larger calibration, corpus replay and full regression remain unrun; no further create is authorized. The statements below retain the original August 27 preparation snapshot.

Date: 2026-08-27. Status: safe preparation and tiny validation complete; nontrivial validation awaits Runpod authorization.

## What changed

Implemented three reliability corrections: malformed numeric limits no longer truncate into valid budgets; estimator arithmetic is bounded before large shifts; refusal diagnostics now clear prior output state and report the artifact actually refused. Remote validation occurs before compilation. Added one diagnostic memory-study driver and expanded the existing budget tests.

Rejected/deferred: no production memory-model replacement, policy activation, new cache/family/context/selector/native lane, or broad protocol extension. No numeric default, estimator formula for supported inputs, output ordering, artifact, or routing changed. Invalid/nonintegral inputs and unrealizable estimator counts are now rejected explicitly; this is an observable input-validation correction.

No owner-declared metrics-ready application workload was found. The retained template validates but has 13 blockers. Newly added website benchmark manifests were searched only for the intake schema; their results were not audited.

**Next authorization:** approve the single bounded Runpod safety smoke in [RUNPOD-AUTHORIZATION.md](RUNPOD-AUTHORIZATION.md). It specifies the image, 65-file upload manifest, 13 exact wheel hashes, no source builds, price/lifetime/cost caps, and teardown. No cloud resources were created or inspected; no upload, package download/install, commit, push, or publication occurred.

## Validation actually performed

| Check | Result |
|---|---|
| Pre-change regression reproduction | 6 failures, preserved in prechange_pytest.xml |
| Final focused budget/refusal/protocol/helper tests | 70 passed in 2.55 s; release_focused_pytest.xml |
| Virtualenv syntax compilation | Passed for changed implementation and driver |
| Workload template | Structurally valid; metrics-ready=false; 13 blockers |
| Final tiny driver smoke | 13 per-window rows, 3 representation calls, all exact |
| Source hashes during final smoke | Unchanged; source snapshots retained |
| Wheel dependency metadata closure | Pass for Python 3.13.15 Linux; installation NOT tested |
| Full pytest, representative calibration, held-out study, corpus replay | NOT RUN: reserved for authorized Runpod |

Final smoke uses one mixed-chain case at output k=6, one cold-process repetition per representation. It is a functional check, not calibration or compatibility evidence. Dense whole-call, bigint and actual word-packed results agree with CSE-flat. Raw data, source snapshots, environment, profiles and CSV are under local-smoke-final/.

The first smoke was superseded after detecting that a preceding dense IR probe warmed the materialization window. The driver now measures whole-call dense materialization first. Coefficients and held-out families were not tuned. See MEASUREMENT-AMENDMENTS.md.

## Important limits

The legacy dense model remains inaccurate as a traced-allocation bound. Candidate coefficients are provisional and intentionally conservative; three tiny cases cannot clear the production acceptance gates. Process working set and lifetime high-water are reported separately from tracemalloc and are not per-window RSS peaks.

Public low-level materialize_ir, bitset conversion, and result serialization still lack independent budget parameters. This campaign does not add silently stricter defaults to those APIs. Parallel aggregate memory, compiler retained caches, and request-body size are not covered by a temporary-output estimate.

Benchmark-corpus compatibility remains unmeasured; real caller compatibility is unknown. All settled negative findings remain in force.
