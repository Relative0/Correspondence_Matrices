# Optimization backlog update

## Implemented and retained in the working tree

- Strict, bounded budget arithmetic without altering supported-input estimator formulas.
- Refusal state clearing and correct refused-artifact diagnostic estimate.
- Worker malformed-limit validation before compilation, with its cost still charged to execution.
- Expanded existing budget tests (70 passing) and a diagnostic three-representation study driver.
- Dated readiness research, wheel lock, explicit upload manifest and single-smoke authorization package.

These reliability changes pass tiny focused tests, not the full release validation gate. No commit is authorized.

## Ready for validation / implementation after evidence

- Run the prepared Runpod safety smoke after exact approval.
- If it passes, separately authorize representative cold/warm calibration and held-out/context cases, accepted-corpus replay and full regression.
- Correct any additional reproducible refusal/state defects before drawing performance conclusions.
- Only then decide whether candidate allocation models justify a production estimator; current evidence does not.

## Needs a real workload

Owner-declared caller, artifact/order, lifecycle, repetition/budget and capture approval are missing. No cache, family, partial-context, selector, Numba or CUDD prototype is eligible.

## Needs policy approval

production-balanced-v1 numeric profiles and formerly admitted calls becoming typed refusals. Independent low-level output/serialization limits also require an explicit API compatibility plan. No silent default is proposed.

## Needs corpus/dependency/cloud authorization

Only one safety smoke is ready for approval. Another selector family needs per-design licensing and workload opportunity. Numba/SIMD/CUDD remain ineligible; PLY 3.10 is still a source-only standard-install blocker. Prior RP-D0 approvals do not carry forward.

## Theoretically blocked / different artifact

Complete packed output retains Omega(2^k/w) storage/work. BDD restrictions or symbolic queries, structural quotients and streaming contracts are different artifacts. Engineering cache keys are not a global semantic canonicality proof.

## Tested and rejected / superseded

- Legacy dense estimate as a conservative traced-memory bound.
- Treating the tiny smoke as calibration or real-workload compatibility.
- First smoke's warmed dense materialization ordering; preserved and superseded.
- Universal words thresholds below 16, the failed BX1 selector's i10 transfer, DP-R1 rational order labels, synthetic cache/family dominance, and optimization of the CM/CSE-flat parity residual remain rejected.
