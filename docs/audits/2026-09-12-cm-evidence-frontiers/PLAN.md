# Evidence-frontier continuation

Pursue all four requested avenues from published revision d7b94d1941266daddc3bb2a7cda8277e1d6595b0 in build/cm-frontiers on codex/cm-evidence-frontiers. Preserve the shared dirty worktree and all earlier seals. No synthetic trace will be described as an observed production session.

1. Reconstruct the final 73 historical failures/errors from the last JUnit report. Classify missing data, platform binaries, frozen source drift and actual behavior. Restore only identifiable, hash-verifiable research artifacts. Keep historical validation strict; separate immutable historical closures from current implementations. Reproduce fixes with focused checks, then a RunPod broad-suite replay.
2. Inspect existing trace provenance/validation tools and the independently maintained video caller. Request an available real trace source. Admit only causally recorded caller events with documented ownership, capture boundaries, ordering and evidence of use outside benchmarking. If none exists, deliver a concrete bounded capture/intake workflow and an honest source-admission report, not invented observations.
3. Retrieve pinned upstream feature-model originals and conversion metadata for the nine previously consumed public models. Parse explicit DIMACS names and original feature identities; refuse guessed auxiliary naming conventions. Independently validate the projection contract and conversion semantics where feasible. Keep original product semantics distinct from arbitrary projection subsets.
4. Freeze a broader independent projected-count corpus before timings. Inspect publicly licensed whole instances from the original projected-count benchmark collection (dfremont/counting-benchmarks), using its supplied projection sets. Select deterministically by domain, size and path under declared file/process limits; retain admission refusals and do not select on observed speed. Use independent exact controls, fixed contexts, isolated methods and fully charged setup.

Cloud work reuses the reviewed ownership-bound RunPod controller and cleanup/watchdog infrastructure, with new paths and source freezes. Keep total conservative spending within the existing $10 authorization, reserving against the prior $0.4365057953596115 bound; use at most $3 in new nonrefunded reservations initially. Heavy computations run on RunPod. No unrelated pod may be stopped. No credentials, personal histories or private consumer data enter a cloud payload.

Finalize with exact source/evidence manifests, failed-attempt custody, all cost/cleanup receipts, focused and regression results, and a continuation prompt if genuine input provenance or irrecoverable historical artifacts prevent a lane from completing. Publish new numerical results only after full source/data verification and website freshness checks under the existing site authorization.
