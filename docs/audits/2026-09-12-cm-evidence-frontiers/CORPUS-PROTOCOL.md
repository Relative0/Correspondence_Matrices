# Prospective mapping and independent-corpus selection

Recorded before any new counting measurements. Feature-mapping inspection uses the same nine previously consumed systems and their same-version upstream originals at SoftVarE commit afa60ee2c836e7bdc4068e0f4f128ea31158d2ad. Missing or ambiguous pairings are refusals, not substitutions. Metadata and original XML/UVL/DIMACS can establish feature identity; auxiliary variables are never inferred merely from a suggestive name.

The independent counting source is dfremont/counting-benchmarks commit daff1084d7487cd85f5a49f6588966275332e73f. For each of its three projection/application collections (maxcount-qif, pmc-symbolic-markov, symbolic-sygus), select the three smallest whole gzip instances, ordered by stored byte count then full path. Retain all nine selections, including trivial, oversized, malformed or intractable cases. No replacement after outcome inspection. Read the author's format and per-collection descriptions before decoding projection sets.

Download limit: 128 KiB compressed and 2 MiB expanded per instance. Parse at most 16,384 variables and 65,536 clauses. Measurement admission and algorithm guards are separate: the existing bucket width/work bounds remain unchanged; unmeasured or timed-out native controls cannot become zeros or remove their competing methods from the outcome table. Use original projection variables exactly; generated contexts are explicitly generated controls. Author-provided application encodings are independent workload families, not observed consumer sessions or necessarily full real deployments.
