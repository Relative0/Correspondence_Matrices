# Repair execution freeze

Authorized by the pasted CM-family repair plan, Phases 0–6.
Implementation branch: codex/cm-family-repair-20260915.
Starting HEAD: 4d9b869fb7b22eb6263ef613b5a9d41fcbbb78dd.
Implementation baseline: e334de594262059cc18cf37eaab56b0f79e94843.

Read the full plan and both sealed audits from the sibling evidence worktree
tmp/cm-time-attribution-20260915. Its untracked follow-on audit is evidence,
not implementation input to be edited. The repository root and consolidation
worktree remain protected.

## Order

0. Record source/input/protected-worktree hashes; independent semantic checks.
1. Explicit evaluator flags and provenance for family and reproduced neighbors.
2. Default-off versioned exclusive timing and oracle accounting, legacy fields.
3. Shared wrapper planning with preserved guards and diagnostic semantics.
4. Per-call sharing-plan reuse and optional disjoint cache telemetry.
5. Bounded additional opportunities; retain negative/unresolved results.
6. Required regression checks, matched diagnostics, manifests and final review.

## Frozen diagnostic cells

Existing fixtures only: implication of conjunction/disjunction k4/k16;
random_expr seed2026 k8 fallback; Or(x0,x19) reduced k20;
shared XOR DAG from persistent-path tests; equivalent roots, related subtrees;
family shared_block_mix seed2 size8 and composition_mix seed3 size5, plus eight
copies of the existing simple root. Keep full generated metadata.
Raw recursive/memo BitSet and structural CSE-flat controls use the same outputs.
Restriction q1/q64 and all-fixed/permuted bases are correctness sentinels.

Seven measured blocks per primary cell, at most 21; two warmups; two independent
baseline/candidate orders. Time blocks of 64 resident calls for small wrapper
cells and four family calls for families; report normalized times and raw block
totals. Cold compile and cache-miss cells reset state outside the measured span.
Record API and caller setup separately, including process/import lifecycle.
Measure phase/call counts and allocation/retention in separate passes.

No more than three candidates per area. At most two hours of performance
execution plus required tests, 10 seconds per call, 64 KiB per explicit result,
eight variants; routine widths <=16 and k20 only refused/reduced.
Stop a mismatching cell; diagnose without discarding its failed record.
Stop on corruption, output breach, unexplained retained growth or protected-file
change. Retain noisy comparisons as unresolved.

Review material regressions exceeding both 5% and 2 us per API call, or 5% and
0.05 ms per family, reproducible in both runs. Timings are diagnostics, never
pytest thresholds or confirmation claims. Config fixes and measurement changes
are not same-engine speedups. Preserve legacy fields and immutable evidence.

No optional installation, cloud, held-out inputs, routing/capacity policy change,
commit, push, merge, or scientific-disposition change.

## Diagnostic amendment after candidate 1

The first three records have large host/order variation, including unchanged
bare controls. Preserve them as exploratory. Final comparisons use 21 blocks
and two warmup blocks (64 API calls or four family calls per block), within the
declared maximum; cold compile still uses one call after an external reset.
Run the final independent workers in baseline/candidate then candidate/baseline
order. Use two pairs, report disagreeing effects as unresolved. No timing-based
pytest gate is introduced. This changes observation precision, not inputs.

The initial record's cProfile fields named self_cpu_s/inclusive_cpu_s were
misnamed: the default profiler clock is elapsed time. Successor records use
self_profile_s/inclusive_profile_s. Process CPU is only the block_cpu_s field;
Windows CPU accounting is coarse and zero does not imply no CPU work.

## Follow-up on the material-regression trigger

Candidate 2 added >5% and >0.05 ms to identical cached families in both workers.
Candidate 3 binds observers once so disabled tracing avoids per-variant phase
forwarding. Its fresh workers also show large changes in unchanged controls and
normal compilation. Investigate that new failure with two resident alternating
baseline/candidate module instances, independent CM caches, 21 blocks and two
warmup blocks. This is a distinct treatment, not a replacement for inconvenient
fresh-worker records. Keep all records and report unresolved disagreement.

## Closing record

Three implementation candidates completed. Final correctness repairs distinguish
root hit position from cache regime and reverse the optional tracer dependency
to preserve standalone core packaging. No new speedup candidate followed.
Final-source fresh and resident verification records retain both independent
runs; the latest separate mechanisms are in MECHANISMS-final-v3.json and the
summary/ledger v2 files. The final origin-counter correction handles eviction
and reinsertion without changing policy. Full checked outputs,
source/key/program identities and both predecessor seals remain valid. Further
opportunities are closed as keep/defer/reject in OPPORTUNITIES.md.
