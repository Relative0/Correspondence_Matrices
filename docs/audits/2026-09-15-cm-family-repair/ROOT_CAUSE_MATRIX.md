# Cause classification after repairs

“Measured” applies to the named local treatment; it does not assert a universal
speedup. A phase is unresolved if only its inclusive parent was observed.

| Observation | Responsible mechanism/class | Evidence and limit |
|---|---|---|
| Configuration selected a different CM engine from the direct control | Wrapper/configuration contract; **measured** by opposite-default tests | Explicit flat/words forwarding repaired for families, partial workloads and equivalence. A corrected route is not an unchanged-engine speedup. |
| Shared root-only cold miss paid for sharing twice | Current implementation/runtime; **measured** | One prepass now; 9–11% lower resident compile time. No output/key/program change. |
| Zero-hit composition family regressed with persistent reuse | CM-family reuse management + representation construction; **measured** | Five root misses still digest/store graphs, but removing duplicate preparation lowers final resident family time 6–13%. It remains slower than its no-cache treatment. |
| High hit count did not create a family win | CM-family reuse management + CM representation; **measured** counts, **code-supported** parent-building explanation | 52 subtree hits, no root hits; adoption/interning and 58 entries/61 reachable nodes remain. Full per-hit benefit is not independently identifiable. |
| Repeated identical family amortizes compilation | CM-family reuse management; **measured** | Seven root hits under subtree policy; each still pays eligibility/digest. Caller cost increases 41–51 us in final resident pairs. |
| Fast-enabled fallback repeated admission work | Wrapper/lifecycle + runtime; **measured** | One admission now; 8–14 us local saving. Fast/generic difference-in-differences supports the mechanism without summing overlapping spans. |
| Packed fast calls become slightly slower after consolidation | Wrapper/lifecycle; **measured** | About 0.7–1.6 us in final resident pairs. Shared planning enters some generic control before fast delivery. Kept for single ownership of guards; not a packed speedup. |
| Family metadata adds fixed work | Output delivery/harness contract; **measured** total increments, **code-supported** counters/JSON attribution | Final pairs remain below the declared repeatable material-regression gate. Exact metadata-only percentage is unresolved; changes interact with compile savings and host drift. |
| Public caller time greatly exceeds bare k4 CM-flat | Wrapper/lifecycle, output-contract guards and result delivery; **measured** distinct API spans | Budget, basis, fixed values and object construction recur. This study does not assign all parent residual to one helper. |
| Words wrapper versus bigint bare control differs | Task/algorithm comparison | Same complete logical relation, different execution engine. Do not call the difference pure wrapper overhead. |
| Fallback truth vector versus packed bigint control differs | Output conversion/delivery + algorithm comparison | 256-byte uint8 output versus 32-byte packed relation at k8; worker metadata contains packed-equivalent size, documented in results. |
| Explicit relation grows with output width | Inherent explicit-output cost; **code-supported**, predecessor scaling measured | k16 packed payload is 8192 bytes, independent of live support if full width is requested. This repair does not remove exponential output size. |
| SAT/count/equivalence or supplied assignments can avoid full enumeration | Task/algorithm mismatch; predecessor measured/code-supported | No new task-specific claim or kernel routing; historical evidence remains governing. |
| CM/CSE-flat retain different representations | CM representation cost; predecessor measured/code-supported | CSE shares source structure with fewer execution fields. No CMNode redesign or new CSE promotion here. |
| Frozen caller/task gaps and Y02–Y05 residuals | Comparator preparation, delivery and unresolved lifecycle | Existing records cannot separately measure imports/transport/shutdown or exhaustively assign validation shares. |
| Fresh worker medians disagree with resident controls | Unresolved host/lifecycle interaction | Preserved all records. Alternating resident pairs improve isolation but do not identify scheduler/frequency causes or establish cross-machine performance. |

No identified mechanism authorizes changing cache policy, route thresholds,
output guards, a held-out gate or scientific disposition.
