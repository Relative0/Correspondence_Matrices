# Application evidence continuation

Start from published commit 6f38fa521619aa0cbf91ee3c294ffca304e313ba in the isolated build/cm-applications checkout. Preserve existing scientific artifacts and source implementations; new mechanisms get separate modules and new evidence identities.

1. Install the official, SHA-256-pinned Ganak 2.6.3 Linux release on RunPod. Confirm deterministic integer projected-count semantics with exhaustive small Boolean controls, then check every unchanged context of the two previously completed QIF cases. Preserve failures and discrepancies. Agreement is an independent solver cross-check, not a certified proof.
2. Freeze concrete-feature counting cases from the eight previously verified original mappings. Add a separately documented pc-richmond Boolean abstraction only if primary FeatureIDE source establishes the scope of its attributes and independent semantic checks pass. Never claim price/attribute configurations were counted by a Boolean abstraction.
3. Develop a bounded, count-preserving projected-CNF simplifier in a new module: condition assignments, unit propagation, hidden pure-literal elimination, and explicitly bounded hidden-variable resolution. Keep free selected variables and distinguish existential elimination from counting. Exhaustive controls precede public measurements. Include unsimplified exact/native controls, refused workloads and preparation cost.
4. Investigate remaining historical test failures without changing old hashes. Recover exact pinned d4/logikbench inputs and source closures when available. Separate historical replay from current implementation tests; do not convert failed historical replay to passing by weakening assertions.
5. A real consumer session needs an independently motivated job and documented downstream use. Await optional owner context while continuing other work. No synthetic or research-triggered run is a natural trace.
6. Run focused and broad regression checks on RunPod, then seal results, regenerate the website and reading editions, publish with repository-local Relative0 authentication, and check exact live files. No global account switch.

All substantial computation runs on RunPod; local work is source inspection, edits, small checks and publication I/O. Existing $10 authorization persists. Prior nonrefunded reservations are $3.50 + $1.75. Reserve at most $4.00 for this continuation ($9.25 combined reservations); additionally enforce the $10 cumulative reservation ceiling from on-disk ledgers. Each attempt reserves $0.25, uses the existing one-hour ownership-bound watchdog, and deletes only its own pod. Prior conservative rate/lifetime estimate is $0.5025470124602317; this is not posted billing.
