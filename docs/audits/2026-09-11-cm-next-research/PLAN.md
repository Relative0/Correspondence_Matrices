# Integrated research continuation

Authorization: Brian requested all seven next-work avenues, including parallel RunPod work where applicable, with a new aggregate $10 USD cap covering runs and retries. No expensive experiment will use the Windows workstation. Existing audit seals, unrelated work and failed promotion gates remain intact.

The integration checkout is `build/cm-next`, based on published revision `d15cb618f4eb99b6621e4ab24773a00f03ae484e`. Copy only reviewed implementation files from the five preceding research manifests, preserving their exact input identities. New source changes live in the integration checkout and receive their own tests and source freeze.

## Execution sequence

1. Establish source lineage, clean integration and a broad remote regression baseline. Keep optional-platform/tooling failures separate from implementation regressions. Review Windows compatibility with bounded relevant checks.
2. Add explicit scalar-plan adapters to an existing task boundary where its count/context contract permits them. Do not silently change default routing or claim an invented application trace is production evidence.
3. Exercise sustained plan/cache lifetimes, simultaneously retained plans, concurrent callers and cancellation with aggregate process-memory measurements. Extend safety tests only where they cover a meaningful untested contract.
4. Exercise an incremental file/pipe consumer under backpressure and cancellation, preserving exact ordered output and charging its actual completion obligations. Distinguish first accepted bytes from durable delivery.
5. Measure preparation reuse and validated inert reload. Implement a bounded structural serialization format only where it provides a concrete reusable facility; measure whether reload actually helps.
6. Predeclare independent public-corpus selection and query traces before measurements. Preserve refusals and unfavorable native comparisons. Separate development, consumed diagnostics and fresh confirmation; do not use repetitions as evidence of application diversity.
7. Investigate the missing projected-count contract: count distinct assignments to selected variables after existentially eliminating auxiliaries. Use independent brute-force and native symbolic controls, reject unsupported resource demands explicitly, and avoid claims of a general-purpose knowledge compiler.

These streams may overlap when source dependencies and cloud ownership permit. Gate new code on exactness before performance work. Run full comparisons on RunPod and keep raw schedules, sources, host identities, uncertainty, errors and cleanup receipts. Preserve earlier native-batch/router/incremental no-go results; GPU work requires a workload that can plausibly amortize transfer and launch.

## Budget and ownership

Every paid attempt receives an exclusive recorded reservation before pod creation. Reservations include the maximum quoted compute rate, storage margin and hard lifetime. Their aggregate may never exceed $10, including failed/uncertain attempts; do not refund reservations optimistically. Each pod has a separate controller, watchdog, identity, deadline and verified deletion. Parallel jobs must be recognized as this campaign's owned resources, and foreign resources must remain untouched. Credentials stay on the controller and are never included in payloads, logs or pod environments.

The final report will distinguish completed implementations, passing/failed/skipped tests, measured benefits and regressions, conditional research, exact cost estimates versus posted billing, and publication state. New verified results should be incorporated into the existing website after the research is sealed, with the same deployment and live-file checks as the previous release.
