# CM neural continuation: unfinished implementation and verification

Work in `C:\Users\brian\Documents\CM_Computation` using PowerShell and the
project virtual environment. Continue the CM learning/neural evidence project
and its website. Implement and test the highest-value genuinely unfinished work;
do not just produce another plan. First reconcile newer exact-side findings so
you neither repeat completed work nor reopen a consumed negative experiment.

## Scope and safety

- Read applicable `AGENTS.md` instructions. This is a shared dirty working tree.
  Record its initial status; preserve all existing changes and untracked files.
  Other threads own ongoing non-neural computation. Do not modify their active
  experiments, controllers, running jobs, or sealed audit directories.
- Scoped reversible local implementation, read-only investigation, and short
  functional tests are authorized. This prompt does not authorize new training,
  benchmarks, cloud work, spending, production changes, commits, pushes, or
  publication. Prepare concrete packages before requesting any missing approval.
  Do not inherit spending authority from historical prompts or budget estimates.
- Never overwrite frozen artifacts, change expected hashes to obtain a pass,
  weaken exactness requirements, or silently replace historical source snapshots.
  Put new evidence in a new directory. Do not access secrets or credential stores.
- Do not reset, clean, bulk-stage, or blindly synchronize the root repository.
  Inspect source differences and use an isolated working area when needed.
  Do not spawn subagents unless subsequently authorized or required by applicable
  higher-priority instructions.

## Read first

Paths are relative to the workspace unless otherwise stated:

1. `docs/audits/2026-09-11-cm-neural-publication/HANDOFF.md`, especially its first,
   completed-publication section, which supersedes historical login blockers below.
2. `docs/research/CM_LEARNING_NEURAL_WEBSITE_AGENT_PROMPT_2026_09_04.md` for original
   scope and scientific standards. Treat old action lists as historical context;
   this prompt's scope and later evidence govern continuation.
3. `docs/research/CM_REMAINING_RESEARCH_GATES_2026_09_11.md` and
   `docs/research/CM_CONTINUATION_RESEARCH_DISPOSITIONS_2026_09_11.md`.
4. `docs/audits/2026-09-12-cm-closures-publication/NEXT-IMPLEMENTATION-PLAN.md` and
   `docs/continuations/2026-09-12-component-and-historical/HANDOFF.md`.
5. Newer evidence under `docs/audits/2026-09-12-cm-component-execution/`, including
   `attempt-002/evidence/paired/RESULTS.json`,
   `attempt-002/evidence/partition/RESULTS.json`,
   `attempt-003/evidence/transport/HISTORICAL-RESULTS.json`,
   `BACKEND-SEARCH-SUMMARY.json`, and any newer finalized report or manifest.

Discover later dated handoffs and receipts before treating this list as current.
Read relevant source JSON and verifiers, not just prose conclusions. Do not run
remote/controller scripts as part of discovery. A partial execution directory is
not a finalized scientific result; distinguish observed rows from verified closure.

## Established baseline to preserve

- Neural site: https://relative0.github.io/Correspondence_Matrices_Neural/ .
  The results already open at the root, with positive and negative graphics and
  evidence links. Do not recreate a placeholder landing page or repeat publication.
- September 11 publication recorded main source revision
  `22ad063cedebd2a1324ee0ac82409a0dbd53972b` and standalone revision
  `f2e095c30f7951dfeccdde552432832da652046a`. These are historical baselines, not
  an assertion about today's remote HEAD. The isolated published source is at
  `build/neural-publish-checkout-2026-09-11`; it contains audit improvements that
  may not be integrated into the shared root. The standalone artifact checkout is
  nested at `build/neural-pages-audited-2026-09-11` inside that source checkout.
  Reconcile these with newer exact-side site changes before editing or rebuilding.
- Publication verified 114 manifest files per site, including 110 research files,
  without checksum mismatches. Recorded focused checks passed 74 tests plus 127
  subtests. These are historical results, not tests you have just run.
- Frozen q64: 18,432 exact timing cells; 18 stable labels / 72 cases, 54 abstentions.
  The only stable winning arm was already the best fixed exact arm.
  Fully charged ratios were approximately 0.94934 on Windows and 0.97797 on Linux,
  below the 1.10 gate. This is a verified learning-opportunity no-go, not a failed
  fitted model: no learner was run. Do not refit or retune this consumed cohort.
- Independent native batching follow-up: 2,592 exact cells. q96 sum-based ratio
  approximately 1.07814 also missed its frozen 1.10 gate. Do not repeat it with
  tuned thresholds, selectively chosen cases, or a renamed workload.
- Retain positive C6 packed-source ANF and C16 screened-GF2 findings with their
  exact baselines and scope. They do not establish a promoted learned router or
  enabled production default. Keep negative outcomes equally accessible.

## Highest-value work, in order

### 1. Reconcile current evidence and finish missing regression coverage

Produce a small status matrix: completed, implemented but unverified, unfinished,
blocked, scientifically closed. Include artifact path, source identity, actual
test/verification status, and the next justified action. Separate neural results
from exact-side improvements and historical replay repairs.

Important freshness traps already observed on September 12:

- Component diagnostics and candidate paired runs now have saved results; the old
  instruction to start implementing diagnostics may already be obsolete.
- The Decisionmaking context-7 partition result is recorded as `refused`; partial
  completed leaves do not independently confirm the total count.
- Historical C38 and ladder-freeze replay receipts report successful unchanged
  assertions. Do not keep listing those as untouched work.
- The expanded exact-backend search still reports no match. Do not repeat that
  search without a genuinely new archive or backup, and do not synthesize a file.
- A local portfolio replay receipt reports exit code 1. Inspect its assertions,
  restored prerequisites and source identities before classifying the cause.

Then implement a demonstrated gap in current-code evidence handling or regression
coverage. Suitable targets, only if absent, include fail-closed handling of missing
or mismatched artifacts, stale summaries, duplicate/incomplete timing cells,
partial experiment results, invalid exactness flags, and premature learning-gate
promotion. Use synthetic small fixtures or saved evidence, not fresh benchmarks.
Preserve historical verifiers unchanged; repair current tooling separately.

### 2. Make the neural site's evidence update reproducible

Inspect the source, template, exporter and focused tests before adding code:

- `deliverables_n22_24/master_explainer_2026_08_03/cm_learning_neural_evidence.py`
- `deliverables_n22_24/master_explainer_2026_08_03/cm_learning_neural_template.html`
- `scripts/cm_neural_site_export.py`
- `tests/test_cm_learning_neural_website.py`
- `tests/test_cm_neural_site_export.py`
- `tests/test_post_benchmark_neural_gate.py`
- `tests/test_neural_reassessment.py`
- `build/neural-publish-checkout-2026-09-11/tests/test_cm_neural_publication_numeric_audit.py`

Reuse existing arithmetic and export checks instead of creating parallel pipelines.
If needed, integrate the published checks into the selected current source without
overwriting another thread's edits. Add missing tests for source-to-graph numeric
agreement, positive/negative disposition, linked evidence completeness, manifest
hashes, portable relative links and line-ending preservation. Existing tests may
already cover some or all of these; prove the gap first.

Prepare a local site update only for finalized newer evidence that materially
changes the neural project's status or dependencies. Clearly label exact-side
progress as exact-side progress. Do not imply it reopens learning automatically.
Keep important findings immediately visible, graphs tied to audited values, and
refusals/regressions linked. Label denominator, aggregation, host and charged-cost
boundary; case-geometric confidence intervals are not intervals for a sum gate.
Generate into a new build directory. Publication needs separate authorization.

### 3. Prepare a new learning opportunity only if evidence supports one

If finalized new exact measurements show a genuinely different workload with
multiple stable winning arms and plausible fully charged headroom over the best
current fixed exact implementation, implement or extend the offline admission
validator and short tests. Otherwise retain the no-go and explain the missing
prerequisite; do not invent a model-training task to fill the queue.

Any proposed experiment must freeze workload identity, independently motivated
consumer/output contract, exact oracle, current baseline, candidate arms, split
and leakage rules, all overheads, resource limits, failure policy and acceptance
criteria before outcomes are inspected. Charge feature extraction, dispatch,
fallback, preparation and cleanup with an explicit startup/amortization policy.
Previously measured cases can inform development, not independent validation.
Freeze only a justified new design; preparation is not permission to execute it.

Exact component optimization belongs to the ongoing non-neural thread unless an
unowned, clearly scoped dependency is identified. Do not duplicate its experiments.
Presentation-only video production is not natural backend demand.

## Verification and handoff

Use `.venv/Scripts/python.exe` with `-X utf8 -B` where appropriate. Run only the
focused short functional checks for changed surfaces. Inspect test behavior first
so a named test does not inadvertently launch timing, training, or remote work.
Do not run a broad historical suite as a substitute for bounded verification.

Deliver small reviewable implementation changes, the evidence/status matrix, exact
commands and observed results, and a concise handoff identifying remaining blockers.
Review final `git status --short` and scoped diffs; distinguish your changes from
pre-existing work. Do not claim public-site updates from local builds or infer
scientific success from a passing serialization/replay test.

If the worthwhile next action requires fresh benchmark/training/cloud authority,
finish useful local preparation first and present its concrete protocol and budget
request. If no actionable gap remains, say so rather than manufacturing work.
