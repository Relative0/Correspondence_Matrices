# Continue after component execution and exact-count closure

Workspace: `C:\Users\brian\Documents\CM_Computation`.
Isolated checkout: `build/cm-diag`, branch `codex/cm-component-historical`,
base `f2178bd4e2f5e76e4857527c5ca809d65e044273`.
The user initially spelled the location `CM\_Computation`; the actual working
root above is confirmed by the environment and retained evidence.

## Read first

1. `docs/audits/2026-09-12-cm-component-execution/REPORT.md`
2. `docs/audits/2026-09-12-cm-component-execution/NEXT-IMPLEMENTATION-PLAN.md`
3. `docs/audits/2026-09-12-cm-component-execution/COST-AND-CLEANUP.json`
4. For remaining execution, that audit's `WINDOWS-REPLAY.md` and
   `HISTORICAL-STATUS.json`.

The new audit is sealed by `FINAL-MANIFEST.json`, SHA-256:
`30d309367d64cb08fb10ab0cdd77f018fa513ba727116b5c96ed929c5814c015`.
It binds 23 execution sources and 1,232 artifacts. Do not modify the sealed audit
or bound sources. Version future source changes and retain their originals.
`SEAL-VERIFICATION.json` beside this handoff records the independent rehash.

## Completed work: preserve, do not repeat

- Counter diagnostics report bounded aggregate/optional timed phase statistics,
  including partial statistics on the unchanged refusal exception. The only
  optimization combines clause satisfaction and residual construction into one
  scan. Exact projected semantics, arbitrary integers, fresh per-query caches,
  conservative work accounting, and production defaults are preserved.
- The frozen 180-cell paired study finished. All 1,344 completed cold/warm query
  outputs matched independent counts. Every arm completed the same 7/15 cases;
  two cases still reach node caps and six reach work caps. Paired median counting
  lifecycle time decreased 13.7–25.2% on completed cases. End-to-end improvement
  is negligible on two small cases, and memory gains are not uniform. This is
  exposed development evidence from one host, not prospective validation.
- The EXACT missing 38,464-byte backend was found at root
  `docs/audits/2026-09-11-cm-performance/baseline_bitset_backend.py` and copied to
  the new audit's `recovered/bitset_backend.py`. SHA-256:
  `4b80a27fa4de67bf35fb13d76ea9d6cd679bfb6dafc8b554741f4987c49bcbdc`.
  It has 891 CRLF and 80 bare LF line endings. Preserve the mixed original bytes.
  `BACKEND-RECOVERY.json` supersedes the earlier intermediate not-found search
  summary; both are retained. Do not repeat the completed backend searches.
- Eight more unchanged historical IDs passed in isolated restored snapshots.
  Together with the three preserved earlier passes, 11 of the original 21 IDs
  have passing historical snapshots. Three new passes used Linux Python 3.13.15
  as source compatibility replays; do not describe those as the exact historical
  Python 3.13.5 runtime. The published broad current-code suite remains unchanged.
- Decisionmaking, additional-09 context 7, now has independent Ganak agreement
  with d4. The verified hierarchy has 34 terminal disjoint cubes, 36 leaf solver
  calls including two superseded timeouts, and 99 oracle controls. An independent
  enumeration verifies unique coverage of all 1,024 assignments to ten split axes.
  All 120 fixed admitted contexts now have independently implemented agreement.
  `INDEPENDENT-COUNT-CLOSURE.json` and the bound verifier retain the details.
  No more count retries are needed. This is not a proof certificate.
- 49 remote component/protocol controls and 26 local execution/supervision/
  analysis/refinement controls passed. The latter includes real Windows descendant
  cleanup. Completed research and tests should not be rerun without a new reason.

## Remaining historical execution

Nine original DLL IDs require the matching remote Windows environment.
`packages/windows-supervised-replay.zip` is prepared with original DLLs and
inputs. The dependent native-portfolio reassessment is the tenth pending ID,
prepared separately in `packages/reassessment-supervised-replay.zip`.

The reassessment's additional retained inputs and four missing source identities
were recovered. Its prior assessment now reproduces exactly, but its full
unchanged test hit the 6.5-second local limit. It has not passed. Preserve all
three successive recovery snapshots and their results.

Both new Windows packages default to read-only preflight. They require the
recorded Windows 10.0.19045 / Python 3.13.5 x64 / NumPy 2.3.2 runtime; the
reassessment additionally requires the original interpreter executable identity
at its recorded path. Retain the 60-second per-ID, 2 GiB committed-memory,
32-process Windows Job Object supervision, output limits, and cleanup checks.
Execution evidence must go outside the snapshots and outside sealed audits.

No matching remote Windows host has been established. Do not run substantial DLL
work locally or fake the Windows interpreter path on Linux. Any needed unrelated
Windows service purchase or external CI publication requires its own concrete
authorization; the current additional budget is specifically for RunPod.

## Budget, cleanup, and retained failures

The user authorized up to $5 additional RunPod spending in this execution turn.
Six nonrefunded $0.50 reservations total $3, leaving $2 of the new allowance.
Including the previous $10, cumulative reservations are $13 of $15 authorized.
The new conservative compute/storage estimate is $0.037537507647275925; this is
not posted billing or a refund of reservations. The prior estimate remains
$0.7929903217852114. Check the ledger before reserving another run.

All five created pods were deleted, all controller sessions completed, and both
final RunPod inventories were empty. No pod remains to resume or clean up.

Attempt 001 created no pod: the existing watchdog rejected the original name
prefix. The corrected controller preserves its required `cm-c7-linux-` prefix.
Attempt 004 stopped before solving because sorted JSON changed regenerated unit
clause order. A versioned wrapper recovered the original bound parent order
without altering frozen CNFs. Both failures and their corrections are retained.

## Workspace and publication constraints

All current changes remain uncommitted. The root still has 31 unrelated tracked
modifications; do not stage, revert, or attribute them to this continuation.
The isolated checkout has two tracked modifications from the prior preparation
plus untracked scoped sources/evidence. The published `build/cm-closures`
checkout still has only its earlier untracked `_site/` and `build/` outputs.
Use command-local Git `safe.directory` and explicit checkout paths.

Preserved prior seals:

- Preparation: `build/cm-diag/docs/audits/2026-09-12-component-diagnostics/FINAL-MANIFEST.json`,
  SHA `2ddd1ee881f60cff7cde9564e1aec359bb68385cd8685335f0e0faf6b9e9ee57`.
  All 13 bound sources and 396 artifacts rehashed unchanged this turn.
- Science: `build/cm-closures/docs/audits/2026-09-12-cm-count-closures/FINAL-MANIFEST.json`,
  SHA `df889ae125afc114917460a32ede7e82f6145d7dfb559dacbc6b72f3258d5548`.
- Publication: `docs/audits/2026-09-12-cm-closures-publication/FINAL-MANIFEST.json`,
  SHA `125bd30ee08c11ac6cbd4e1f7a7fdad5a6a450d2a45a66e8bea3a003a388f89f`.

No commit, push, deployment, production switch, or publication was performed or
newly authorized. Preserve user restrictions on secrets and destructive actions.
Do not spawn subagents unless subsequently authorized. Prefer the root
`.venv/Scripts/python.exe -X utf8 -B`; heavy work belongs remotely.
No independently needed consumer job arose, so no integration claim was added.

## Reasoning effort guidance already given

For GPT-6 Astra in these CM threads, recommend XHigh by default, High for routine
implementation/test fixes, and Max for difficult exact-count semantics or final
evidence audits. This is workload-specific judgment, not an official CM setting.
Official model reference: https://developers.openai.com/api/docs/models/gpt-6-astra
