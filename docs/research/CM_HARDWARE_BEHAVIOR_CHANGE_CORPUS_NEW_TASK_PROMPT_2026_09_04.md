# Prompt for a new task: hardware behavior-change corpus

Status: executed through the frozen source gate on 2026-09-04; do not rerun as a fresh
confirmation task. See
[the result](CM_HARDWARE_BEHAVIOR_CHANGE_CORPUS_RESULT_2026_09_04.md).

Continue the exact, non-neural CM architecture investigation in
`C:\Users\brian\Documents\CM_Computation`. This is a new experimental task, not a
neural-network, learned-selector, website-redesign, or video-production task.

Read these files before changing code or designing the experiment:

- `docs/research/CM_COMPUTATION_DEEP_TECHNICAL_DOSSIER.md`
- `docs/research/CM_NEXT_OPTIMIZATION_IMPLEMENTATION_PROGRAM.md`
- `docs/CM_ARCHITECTURE_AND_SPEED_INVESTIGATION_PROMPT_2026-09-02.md`
- `docs/research/CM_ARCHITECTURE_AUDIT_DISPOSITION_AFTER_C38_2026_09_03.md`
- `docs/research/CM_INCREMENTAL_REVISION_LOCAL_GATE_PROTOCOL_2026_09_04.md`
- `docs/research/CM_INCREMENTAL_REVISION_LOCAL_GATE_RESULT_2026_09_04.md`
- `docs/research/CM_HARDWARE_REVISION_FEASIBILITY_PROTOCOL_2026_09_04.md`
- `docs/research/CM_HARDWARE_REVISION_FEASIBILITY_RESULT_2026_09_04.md`
- the exact evidence under
  `docs/research/verification/hardware-revision-feasibility-retry-002-2026-09-04/`

Treat both 2026-09-04 stop decisions as binding prior evidence. The feature-model
incremental prototype was slower than the existing persistent CM cache and CSE-flat on
the tested exact lifecycle, and only three of 42 confirmation cases had a normalized
clause change. The subsequent hardware feasibility audit admitted four public histories
and independently replayed 48 mechanically selected transitions, but its held-out
histories changed only 7 of 670 comparable stable source seeds (1.04%); total parser
coverage was 11,912/18,728 (63.61%). Those regions were pre-synthesis source seeds, not
synthesized cones. Do not promote the prototype, time that rejected corpus, alter
production defaults, or update a public performance claim from either result.

## Objective

Determine whether a **new, preregistered, legally usable corpus whose sampling unit is
an actual HDL-behavior change** can pass a source/activation gate strongly enough to
justify a later Yosys correctness experiment. Do not benchmark CM in the feasibility
phase.

Use non-performance metadata to define candidates—for example path-qualified production
RTL histories, issue/PR-linked functional fixes, or tagged adjacent releases. The rule
may require an HDL semantic change according to an independently specified source or
tool screen, because activation is the object of this phase, but it must never select on
CM, CSE, raw-flat, query, timing, memory, or changed-cone performance. Freeze the rule,
repositories, immutable cutoff, development/confirmation split, licenses, maximum scan
depth, refusal behavior, and admission thresholds in a committed protocol before
inspecting the new confirmation transitions. Do not reuse favorable individual
transitions from the prior development partition or silently replace refused projects.

## Required feasibility work

1. Use several independent public hardware projects with immutable upstream URLs and
   revisions. Verify declared permissive licenses from repository files. Record every
   candidate and refusal.
2. Define production-RTL path rules and the behavior-change screen precisely. Retain
   screened-out, zero-change, ambiguous, renamed, and parser-refused counts so the
   selection mechanism is auditable.
3. Improve source parsing only if its contract remains conservative and independently
   testable. Never call source regions synthesized cones. Macros, generate constructs,
   cross-file elaboration, aliases, multiple drivers, and unstable identity must fail
   closed unless explicitly resolved and tested.
4. Split by project/history, not by individual transition. Develop the rule using only
   the development partition, then freeze it before opening new confirmation data.
5. Emit deterministic raw evidence, manifest, inventory, checksums, per-history
   summaries, and an offline verifier that recomputes selection and metrics from cached
   immutable objects. Aggregate within histories before combining them.
6. Stop if confirmation activation, parser coverage, provenance, or independent replay
   misses any frozen threshold. A negative result is a valid completion.

Only if the new source trace passes every feasibility gate, write and commit a separate
protocol for an elaborated Yosys correctness phase. That later protocol must pin the
Yosys/container identity, synthesis scripts, stable cell/net/cone matching, earlier,
later, and XOR/delta artifacts, direct oracle, ambiguity refusals, and complete
provenance. Do not time algorithms in the correctness phase.

Only after the Yosys correctness phase passes may a third, separately frozen local
timing protocol compare cold CM rebuild, current persistent CM, the research incremental
prototype, CSE-flat, raw-flat where feasible, and a direct packed oracle. Charge parsing,
elaboration, matching, invalidation, construction/lowering, evaluation, delivery, and
retained state consistently. Report changed-cone size and q-amortization without output
caching. Stop unless the incremental path beats both the current persistent cache and
the task-matched control with acceptable worst-case memory.

Do not use RunPod, GitHub Actions as compute, or another paid service without a passed
local gate and separate exact authorization. If a paid replication becomes justified,
prepare an authorization request with the exact image, command, inputs, duration,
maximum charge, output retrieval, and guaranteed cleanup, then stop for authorization.
Do not push, publish, deploy, or change website claims without separate exact
authorization.

Use the repository virtual environment, add focused tests, run the maintained non-neural
checks relevant to touched surfaces, and inspect `git status --short` and the staged
diff before claiming completion. Preserve all unrelated work, especially the concurrent
learning-handoff/query-ladder files and `docs/video_factory/`. Commit only reviewed files
belonging to this experiment if the user has asked for commits. Finish with an
evidence-backed go/no-go decision and identify the precise next gate.
