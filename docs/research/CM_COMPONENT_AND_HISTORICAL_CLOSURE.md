# Component and historical closure: September 12, 2026

All 120 fixed admitted contexts have independently implemented exact-count
agreement. The final Decisionmaking context agrees between d4 and a Ganak
decomposition with 34 disjoint terminal cubes. This is not a proof certificate.
No count retry remains required.

All 21 original historical test IDs now have passing restored snapshots.
The ten final Windows checks passed locally on Windows 10.0.19045 x64,
Python 3.13.5 and NumPy 2.3.2, with unchanged assertions, interpreter identity
checks, 60-second per-ID limits, 2 GiB Windows Job Object limits, a 32-process cap,
and verified cleanup. Preparation and execution took 77.3 seconds. Three earlier
passes remain Linux Python 3.13.15 source compatibility replays. This does not
reclassify or rerun the published broad current-code suite.

The later single-pass component candidate completed the same 7/15 development
cases in every arm of a 180-cell, four-arm, three-repeat study. All 1,344 completed
cold/warm outputs matched independent counts. Paired median counting-lifecycle
time fell 13.7–25.2% on those completed cases. End-to-end improvement was below
0.1% on two small cases, and peak RSS did not improve uniformly. Two cases still
reach node limits and six reach work limits. The candidate preserves projected
semantics, fresh query caches, arbitrary integers and conservative work charging.
These are exposed single-host development results, not prospective validation.
The published counter and production defaults are unchanged.

The current webpage derives its additions from
`docs/audits/2026-09-12-cm-final-public-evidence/PUBLIC-RESULTS.json`, bound by that
directory's `FINAL-MANIFEST.json`. Selected exact source summaries are retained
in `inputs/`; their original audit seals and hashes are recorded. Operational
receipts and cloud account inventories are not part of the public export.
Earlier published studies retain their original counts, dates and failures.

No further historical execution or count retry is required. Further work is
conditional on a concrete need:

- Capture backend use only during an independently needed consumer job. Existing
  video builds establish presentation use, not natural CM backend demand.
- Freeze new independent workloads before any prospective performance claim.
- For a real workload blocked by the eight resource-limited cases, use retained
  diagnostics to choose one bounded change and freeze a new paired protocol.
- Revisit unsupported original feature mappings only when that contract is needed.

Repeating completed benchmarks without one of these triggers adds little value.
No production promotion is supported by this closure.
