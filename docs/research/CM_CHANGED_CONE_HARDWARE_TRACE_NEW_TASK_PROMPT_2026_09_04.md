# Prompt for a new task: changed-cone hardware revision trace

Continue the exact, non-neural CM architecture investigation in
`C:\Users\brian\Documents\CM_Computation`. This is a new experimental task, not a
neural-network or learned-selector task.

Read these files before changing code or designing the experiment:

- `CM_COMPUTATION_DEEP_TECHNICAL_DOSSIER.md`
- `CM_NEXT_OPTIMIZATION_IMPLEMENTATION_PROGRAM.md`
- `docs/CM_ARCHITECTURE_AND_SPEED_INVESTIGATION_PROMPT_2026-09-02.md`
- `docs/research/CM_ARCHITECTURE_AUDIT_DISPOSITION_AFTER_C38_2026_09_03.md`
- `docs/research/CM_INCREMENTAL_REVISION_LOCAL_GATE_PROTOCOL_2026_09_04.md`
- `docs/research/CM_INCREMENTAL_REVISION_LOCAL_GATE_RESULT_2026_09_04.md`
- the retry-003 evidence under
  `docs/research/verification/incremental-revision-local-gate-retry-003-2026-09-04/`

Treat the retry-003 result as binding prior evidence. The experimental radix
incremental compiler was about 0.442x cold-CM update time, but the existing persistent
CM cache was faster; the incremental/current-persistent update ratio was about 1.104x,
the incremental retained-memory worst case was about 3.793x, and CSE-flat remained
faster through q64. The feature-model confirmation cohort was inadequately activated:
only three of 42 cases changed after CNF normalization. Do not promote the prototype,
alter production defaults, update public performance claims, or request cloud compute
on the strength of that run.

The objective is to determine whether incremental exact CM construction has a useful
regime on natural adjacent hardware-design revisions with explicit changed cones and
stable identity across versions. Find or construct a legally redistributable,
source-closed revision trace from real version history. Prefer several independent
projects or histories. Record upstream URLs, immutable revisions, licenses, downloaded
file hashes, tool versions, extraction commands, and every admission/refusal reason.
Use primary sources for any external research.

Before any decision-bearing timing:

1. Define the exact requested artifact and require every arm to return the same earlier
   vector, later vector, and XOR/delta vector. Include all parse, elaboration,
   normalization, cross-version matching, invalidation, construction/lowering,
   evaluation, delivery, and retained-state costs required for that artifact.
2. Define a deterministic node/net/cone matching contract. Never infer reuse merely
   from an unstable traversal number. Detect ambiguous identities and fail closed.
3. Freeze corpus admission, development/confirmation splitting by history, changed-cone
   activation thresholds, widths or slice rules, query counts, repetitions, aggregation,
   confidence intervals, memory accounting, and promotion/stop criteria in a committed
   protocol before implementing to the benchmark or examining timed results.
4. Select transitions without looking for CM wins. Retain and report admitted
   zero-change cases and all refusals. Require the held-out confirmation partition to
   contain normalized changed regions across multiple independent histories; otherwise
   stop for inadequate activation rather than time an unrepresentative benchmark.
5. Preserve independent exactness checks from the unnormalized source representation,
   artifact hashes, raw per-case rows, manifests, inventories, checksums, and a separate
   summary-replay verifier.

Compare at minimum: cold CM rebuild, the current association-preserving persistent CM
cache, the research-only incremental prototype, structural CSE-flat, and raw-flat where
feasible. Include a direct packed oracle. Report construction/update time separately
from end-to-end task time, retained memory and worst case, changed-cone size, reused and
invalidated regions, and bounded q-amortization without an output cache. Aggregate
within histories first so large projects do not dominate.

Start with a small read-only feasibility and activation audit. Run a local functional
and performance gate only if the corpus passes the frozen activation criteria. Do not
use RunPod or any paid service unless a local result passes every preregistered gate;
if it does, prepare an exact authorization request with image, command, inputs, expected
duration, maximum charge, and cleanup behavior, then stop for authorization. Do not push,
publish, deploy, or change the website without separate exact authorization.

Use the repository virtual environment, add focused tests, run the maintained
non-neural suite, inspect `git status --short` and `git diff --stat`, and preserve all
unrelated dirty or untracked files, especially `docs/video_factory/`. Commit only this
task's reviewed files if the user has asked for a commit. Finish with an evidence-backed
go/no-go decision and state whether the next step belongs in this task or a distinct
task.
