# Research disposition after bounded overlapping-CNF counting

This ledger supersedes the remaining-work entries in the earlier sealed reports without modifying their evidence. Completed facilities remain opt-in unless their original audit explicitly integrated them. A public-input feasibility result is not evidence of an actual service's request mix.

| Priority | Work and current disposition | Concrete prerequisite for another implementation or performance campaign |
| --- | --- | --- |
| 1 | Overlapping CNF scalar counts: bounded natural/min-fill bucket plans, exact CUDD reference, and exact object-array evaluation implemented and exercised on full public CNFs. | A real count/existence consumer whose CNFs fit the declared limits. Preserve CUDD natural/dynamic controls, ingress costs, worst cases and refusals. See the new bucket audit for the measured array tradeoff. |
| 2 | Consumer integration for independent, factorized, affine and bounded-CNF scalar plans. Each now has exact tests and workload evidence. | An actual count/existence call site with declared basis and query lifetime. Public feature-model conversions count all CNF variables; projected feature-product counts require a proven conversion contract or a different exact algorithm. |
| 3 | Streaming and cache facilities: implementation, real file/pipe sink, cancellation and retained-memory checks already completed in the preceding campaign. | A consumer accepting incremental ordered bytes or an independently captured cache/request-lifetime trace. More repeated synthetic sweeps do not establish admission. |
| 4 | Reuse of preparation and elimination structure. Existing explicit plans already retain reusable structure with private query state. | A request trace making plan preparation material, including misses, invalidation, concurrent workspace pressure and useful lifetime. Any persisted form needs a bounded checked structural schema, never executable deserialization. |
| 5 | Richer compilation: projected counting, hierarchical decision diagrams, sparse set families or knowledge compilation beyond syntactic CNF. | A separately defined output contract and independently selected application corpus that exposes headroom over current native symbolic methods. The present prototype does not implement a general d-DNNF, CFLOBDD or projected model counter. |
| 6 | Incremental revisions / hardware changed cones / compact keys / rewrite saturation. Prior activation and profile gates remain negative. | Distinct prospective active revision traces or a newly measured preparation bottleneck. Do not retune or relabel previously rejected corpora. |
| 7 | Native batching / M4RI integration / parallel or GPU execution / automatic backend selection. Earlier batching missed its fixed gate; indexed Python affine binding addressed the measured cost without native promotion. | A materially new mechanism and admitted task that amortizes launch, transfer, conversion and selection. Then prospective end-to-end controls, tails, retained memory and independent-host confirmation. No default router or hardware backend is justified now. |

The actionable algorithm/implementation branch identified by the preceding scalar audit has now been tested. The remaining entries depend on a new consumer, output contract or independently admitted workload; they are not unfinished retries. There is no scheduled background work or running cloud workload. The same renewed $5 authorization is carried forward in the bucket audit's budget ledger.

See [the bounded-CNF audit](../audits/2026-09-11-cm-bucket-counts/REPORT.md), [API guide](CM_BUCKET_COUNT_API_2026_09_11.md), [preceding scalar audit](../audits/2026-09-11-cm-scalar-research/REPORT.md), and [earlier dispositions](CM_CONTINUATION_RESEARCH_DISPOSITIONS_2026_09_11.md).
