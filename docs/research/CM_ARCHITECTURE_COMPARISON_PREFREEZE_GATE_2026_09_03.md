# Architecture comparison prefreeze gate

Date: 2026-09-03

The next comparison-campaign step is now represented by a machine-readable, fail-closed
prefreeze artifact. It binds source checkpoint
`186a9df88dfa2e7c27046cbcbebbeb51893200b7`, the verified native-portfolio closure, the
C38 cross-machine adjudication, and the byte-identical four-lane functional admission.

The present decision is `ready_for_corpus_freeze`. C38 exactness passed and the
four-lane functional admission produced no timing evidence. This permits source-blind
selection and identity-freezing of a fresh task-matched benchmark corpus; it does not
permit timing or cloud execution.

The best fixed q64 method equals the per-case oracle at exactly 1.0000x, below the
required 1.10x development headroom for a new selector surface. That result blocks
selector fitting and neural training for the present q64 contract. It does not block
non-neural comparisons for complete relations, repeated restrictions, related roots,
or smaller task-specific queries.

The dormant blueprint records:

- the observed-regression, future tree-like, and future high-sharing cohorts;
- the complete-relation, repeated-restriction, related-root, and smaller-task lanes;
- q1/q4/q16/q64 scheduling and full arm counterbalancing requirements;
- stage timing, output, memory, source/runtime identity, and refusal fields;
- exactness, unfavorable-cell retention, task-matching, cross-machine, native case-floor,
  and selector-headroom publication gates.

No fresh cohort identity, schedule seed, or schedule digest has yet been assigned. The
gate permits protocol maintenance, source-blind structural selection, and corpus
identity freezing. Prospective performance-data consumption, timing, RunPod
authorization or execution, selector fitting, neural training, production routing, and
website publication remain disabled.

Evidence:

- `docs/recognition/architecture_comparison_prefreeze_20260903/PREFREEZE.json`
- `docs/recognition/architecture_comparison_prefreeze_20260903/VERIFICATION.json`
- Prefreeze SHA-256: `19cd56126636dc5d6cc50041454a6e3bb63b893e24763cd5a563824553a80f44`
- Independent result: `verified_ready_for_corpus_freeze`, byte-identical replay

The selector headroom gate can open only when supplied a complete, independently
verified, source-bound development result for a genuinely new exact decision surface
with optimized exact baselines and oracle headroom of at least 1.10x. That requirement
is intentionally independent of the comparison-corpus freeze. Timing, cloud execution,
training, routing, and publication all require later, separate decisions.
Any later partition-learning proposal additionally requires a sound early-termination
or certificate mechanism that materially avoids exact global-best completion work.

Verification completed on 2026-09-03:

- focused prefreeze and four-lane harness suite: 9 passed;
- broader comparative, multi-root, native-portfolio, and C38 regression suite: 18 passed;
- stored prefreeze replay: byte-identical, validated read-only without rewriting the
  artifact.
