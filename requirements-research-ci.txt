# Focused offline research checks only; not the native/full benchmark environment.
# Versions match the tested local environment. Binary wheels only in CI.
numpy==2.3.2
requests==2.34.2
certifi==2026.6.17
charset-normalizer==3.4.9
idna==3.18
urllib3==2.7.0
pytest==9.1.1
